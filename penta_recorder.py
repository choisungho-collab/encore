#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
myPENTA — 리그 오브 레전드 자동 녹화 (한 번 실행하면 다 됨)
=====================================================
이 파일 하나만 실행하면:
  · 필요한 파이썬 패키지 자동 설치
  · ffmpeg(레코더) 자동 다운로드 (처음 한 번)
  · 게임을 감지해 판마다 자동 녹화, Riot 공식 기록으로 전적 분석
  · 영상+전적을 Supabase에 업로드 → 웹 갤러리에 자동 등록, 브라우저 자동 오픈

그 다음부턴 리그 오브 레전드를 켜서 게임하면 → 판마다 자동 녹화 → 영상+전적이 갤러리에 자동 등록.
OBS 필요 없음. NVIDIA NVENC 하드웨어 인코딩이라 게임 성능 저하 거의 없음.

실행:  penta_recorder.exe 더블클릭
"""
import os, sys, json, time, socket, subprocess, datetime, traceback, threading, hashlib, secrets

# --windowed(콘솔 없는 exe) 실행 시 sys.stdout 이 None → print 크래시 방지
class _NullIO:
    def write(self, *a): pass
    def flush(self): pass
if sys.stdout is None: sys.stdout = _NullIO()
if sys.stderr is None: sys.stderr = _NullIO()
# 윈도우 콘솔을 UTF-8 로 (한글·기호 깨짐/크래시 방지) — bat 없이 직접 실행해도 적용됨
if sys.platform == "win32":
    try: os.system("chcp 65001 >nul 2>&1")
    except Exception: pass
    for _s in (sys.stdout, sys.stderr):
        try: _s.reconfigure(encoding="utf-8", errors="replace")
        except Exception: pass
# 콘솔 없이(pythonw / --windowed) 실행돼 stdout 이 없을 때 print 가 죽지 않도록
for _nm in ("stdout", "stderr"):
    if getattr(sys, _nm, None) is None:
        try: setattr(sys, _nm, open(os.devnull, "w", encoding="utf-8"))
        except Exception: pass
def _safe_input(prompt=""):
    try: return input(prompt)
    except Exception: return ""
def _run(args, **kw):
    kw.setdefault("creationflags", getattr(subprocess, "CREATE_NO_WINDOW", 0))
    return subprocess.run(args, **kw)

FROZEN     = getattr(sys, "frozen", False)
APP_DIR    = os.path.dirname(sys.executable) if FROZEN else os.path.dirname(os.path.abspath(__file__))  # 쓰기 가능(exe 옆)
HERE       = APP_DIR

# ===================== 0. 의존성 자동 설치 =====================
def ensure_deps():
    if getattr(sys, "frozen", False): return   # exe(번들)면 패키지가 이미 포함됨
    need = []
    for mod, pkg in [("psutil", "psutil"), ("requests", "requests")]:
        try: __import__(mod)
        except ImportError: need.append(pkg)
    if need:
        print(f"[setup] Installing Python packages: {', '.join(need)} …")
        try:
            subprocess.check_call([sys.executable, "-m", "pip", "install", "--quiet", *need])
        except Exception as e:
            print("[!] Automatic package install failed. Please run this manually:")
            print(f"    {sys.executable} -m pip install {' '.join(need)}")
            print("Details:", e); _safe_input("\nPress Enter to exit...")
            sys.exit(1)
ensure_deps()

import psutil
import penta_lol
import urllib.request, zipfile, io, webbrowser, shutil

# ===================== 경로 / 전역 =====================
def _data_root():
    # 업데이트(새 폴더에 압축 해제)해도 자료가 유지되도록 사용자 폴더에 고정 저장.
    # config.json 의 "data_dir" 로 바꿀 수 있음 (기본: 윈도우 %USERPROFILE%\ReplayCast).
    try:
        cfgp = os.path.join(HERE, "config.json")
        if os.path.isfile(cfgp):
            dd = (json.load(open(cfgp, encoding="utf-8")) or {}).get("data_dir")
            if dd: return os.path.expanduser(os.path.expandvars(dd))
    except Exception: pass
    if sys.platform == "win32":
        return os.path.join(os.environ.get("USERPROFILE") or HERE, "ReplayCast")
    return os.path.join(os.path.expanduser("~"), ".replaycast")
DATA_DIR   = os.path.join(_data_root(), "data")
UPLOAD_DIR = os.path.join(DATA_DIR, "uploads")
REC_DIR    = os.path.join(DATA_DIR, "recordings")
CONFIG_PATH= os.path.join(HERE, "config.json")
FPS        = 30
APP_VERSION = "dev"   # 빌드 시 GitHub Actions 가 릴리즈 태그(vX.Y.Z)로 치환. 소스 실행(dev)이면 업데이트 확인 생략.
_PENTA_ICON = ""
# --- Top/bottom scrim overlay so status text stays legible over the scene (transparent middle keeps the crystal) ---
_SCRIM_B64 = ""
# --- Summoner's Rift ambient scene (behind the status bar; warms while recording) ---
_RIFT_COOL_B64 = (
"iVBORw0KGgoAAAANSUhEUgAAAdIAAACWCAYAAABq3gn4AAAAIGNIUk0AAHomAACAhAAA+gAAAIDoAAB1MAAA6mAAADqYAAAXcJy6UTwAAAAGYktHRAD/AP"
"8A/6C9p5MAAAAJcEhZcwAAD2AAAA9gAXp4RY0AAAAHdElNRQfqBhsRGxGtMZVVAABtkklEQVR42u29SZMlSZIe9qm7v4jIzFq7urp6X6bRzemGzIIhOACF"
"IjjwwguPFP4C/iQeyRuPXG4UXEgKBIRAOMBwBqAAA3ZzZrqnl+pac8+MeO+5Kw9ui6qamru/Fy8iI6tcu7Piubu5mZqamX6qaovT/QcfMFZaaaWVVlpppa"
"OoI6JXzcNKK6200korvbbUEVYgXWmKpvoHO0luoT95MZS1G690Knpl/YudnwvG30qvnDpC86p5WOm1pTDI78J4ftU8rEB+WnrV7flK+Dm0E62d7q5QhzW0"
"+6Wl16vlXy9uV7om3enmvjlU5RNX/a7ZI19UWj3SLwldd3DGAXkz+u0WtOadVswr3TrdCsIcXsiSQO4hXZkm8lnpdLR6pF8Iim1YHyKs0h1Pd20QXqdGp6"
"7LOpKuT7fWJgsa6/q83EyPuB5fc2/P65KVSloXG702VGsn2eG/WNEFKn5MU33o08RdXpDysBQrHU/T0uUDnx6+UIcWvh1ff/3gZkn/jQHmJTpnJWAN7d4o"
"3XtwDwDw8vnLGyyFFt57jWnhuD2u1nQNcZ1GztfL5SaV2vGcnZarZUFNqr124rqdPpe7QFJYc7X64tT6VNStQrk5ynt0l8iYQLIzU/l8imYXKdzsJOchUn"
"nVDHzBanC3uLkZrsaRQU2IIQw3alndML1ib66qKGj68VQdCu+cXn09b5k6otUjvSl6+fwKALBcxseP/Nk3yV5eU8vcSSV1CkZrCuC1qfAXkhoivPf+OwCA"
"zz55COZTtNOrUPZH9KMTsMnunu9juSP3smtHPdf3w3H1fI1pPdno5GTl+WpdQZq8e4dA41V2Q9VEZKJcN2jcrLQQIxgNNSBqAQANtRgw+NHIg4bbDbXQK1"
"nFxpMZkPP8lGw2bYNvfOsDAMCHv/kIQ99jfo61pitfP+rA63C/EUpTb8vCupFuF3avW4o3EJbcc+jAMXQI5w/efAAAeP70+eE8nHRsr2NtJB0EnCcCM+Pz"
"Tx6Obw9cRlRsO51YJ9+Oiq+Nn2UyOvT5qXrjyDWlyBuFhUr1/I2+W3SK092mDnc5tHtn5vWm6ZQ+C518S/bJmMX83Mchc8InZGs280b/PV3GJ8zrlKr6GL"
"7u6magzFeK5r6CKNrtBIznzia85tzjDVkDBGDoGR/++mMAwNADhHZRwZT+cyusHkVvBEP82YQhfqe3v3RnHQBgv9u/Qi58D+vQrRkuuQvljgejW5PHxGKF"
"mTeXyYSc38fyCuDFs8vxilonzR0Kb690JF23DY9U29LQF311rtT50nji1jX65Y12acbQx93qoqAaUi5llWPVPa/qduBWe9o+3ck5UgbQdR1+8oc/BgD81b"
"/9Gfb7/R1RbSdcGn5whSqd6KAwcknXw6sTt4oxwE+R+/RwE5I7eQe7Gz32btMplKGQMy/LkSauFnO+eNPpVNlTKfiQl6QIrlMrE26d4Mk8vz6UOHlP6rbb"
"GV9xSmgKK+9kaJeYQURomy5VgEC3GtI5rKSb4qseyonzEEuIi3ed68VVOLCuC1D6pk4nOo1vf9N97pD9e68D3WZ9phTv0Tks5v72v8uyLFfi+beneNf6gM"
"27Jww1TyqHw/J91aFgev/dP3zVPFSp29yF0K6lm/Dd6p7mdcu8Tj1fnVpfcELogftsb4vLlU5Pt6+gePLyLp1ga2HutkvWRc+HXa/P6d2Eqzt91m4C0Bvg"
"8dV7nH74hk7kR12fr9t4q6R6WOoVn+D0CofJXR2ht6XS3PrfaOEGEGYawJkZPLLYcCgCL6+cDnveJsjICWEB58VcrvZar9+XD6/rbUjlbp9sdG0ArcyEHJ"
"vt5OTDsQB0ijlXqsdzrsnfWO+4mXupslgarrm5JflHs3hkwjs8im6MbqrOixQfHZD6hjXp3JljZQDT6fcEfPDNrwEAPvrtxy6Ynq4afK3HZcL6OoPjPpYx"
"sdjqCCUusP7GdtPeycVGx9N16rLgIAWaf2s68U3u5yk92VOVcnF/PDP48uXlTK5l/WL3OtXXZw4cPiu9hnTyaNFskpv0bkrjs1yRGu6n9Sr++gc6GZSW+9"
"bV1QmNzKIk99wC38CuRo2L9xbsva2uqD7NoRB32yNdTKcHD9S7/4K0qDTcMdxNTeifprZT/uO9+xf4h//ZPwAA/Ot/8Rd4+eLSWd4+z88hnL76sPvN0gns"
"81uu92HfVVleh7tCyxe8nHSBkVkY9dFvPkkPyB32y+dmjzvG4TQLfyZL0gus87OiCJ6Q9ZxDcvzuhWO313Sv86e3qFrpUymZJaFMD1QO8zyLWswupJnh62"
"jH1wwrZhCavHoazfivEsU4fNvBkkSnBIzXM0x71/i5q3VYetzgTZU4N7syV3IetvUNqTw7uA+M/SxQm4sXNInp5OW6QHrBFlCXfDbPMXaOmGvWwC396IUr"
"pT947z+528ZijfGyznMpjy1hNtlthRuXe7eH7Az1uz6J/1zcOwdAuHx5eZAaogPuLpk/vSm6rfY78Y7JW6e7wf/tw+VxpS3r59cbTzyxb/bQrSlzOoOdX8"
"v5PqbWvDip8G2J8M3vfB0A8Nu/+90Ipou8jvqjJSy8RnOkQuEf6rHMWF2n3eixYAENTTVOffvxcg6O2SleT/fy5Tble5CsDqrEXM63Ob9ap/r6Q62IRuVR"
"GieHD7dT7LK9qSmAA1Uqe+aSnQ+rn2BzyMi4+T2edaWy5ACQdHUQM46xOWnIz8lK12Ep3/UA84xWEyt66ynNCDs4hJXP/AWR143MKzSTLQve62Ff+vr7/+"
"gOeqRT1tzSVa4xrbMA5tCyacl7c3nZify5NfWVxUoMHGf81PxZnr3r50aoZHjjdNOAe+sVuiF67/13AYyfHfti06lU2JJQ4k3x704eiltzprfHKVWyO7wm"
"LFbul3i+JD/p3h2/wIdnX2E3tOvLwMOHIxcb8Z37+ouwUQvP8wDv0/mo9rTPU/MwOIHa4iU/sSELkKIl29LqHHKj67jopbn5zEOXAVVszzBATt2bbhY0j7"
"aQXhOSq0C/yOROJtofx+UzE4M4Jf/TXp3Uf3OB0oXOxvLYaR7Z7ED6AgzhCu/qhLZJ4JvOXe5ayABaOk/lHPMhi0od7sLPOxLa9YS8EDhrWYWLxYq4CJHU"
"BF3JQ8cuwq25hVyUO7MT/z3GA67D5nToxgYlx5/ePEuDwoK+Jh02l3pgbjfcvW8u+0PCcn76zz99ND456hjQQxbvvcoD8Sq0JPLDszcm35+K3xzDez107X"
"lPFnz0WJ02+uX49sLS05qiNCi8k6BI6cR6PFAeSGOdjUPXHvMI2Fwvg4pWO76lxp/j+90RuVyTKnOIkwHxmsUp0e+4VbuHzC8UQEvuLNhhXJB955B6WPSX"
"AKettOOl4tVnqWKek+EJ5vBOgmQ3EA4+doHDYp6WLz07jKJ6WWpIHiEbVZb0jrRsbnxpF83cWKxjvX5ejuPjN6pMQDbNjdUl/Gag1h7iEv7kt2Gpsutg+u"
"1CFsIIItACT9Wsd7ZdatJUrzlxmHBydJnAK9z+Mm8wzoRhCwBd6MHSYZ2svJr2mhcsNZokrubgMT7Fi1fulLd5rGK+jj+50HC6Ua/yZl1WX1ffVJnXCT7G"
"/uAYXbPZHFcf1deXrh04opSjaYoVnru5VBMcArJ+xGnaHxZRr0J/Wc4MqBFQ4IMA12Ubbea07dSCJ6mrZP2tL1kLG7tsz/DpeMiulHXu3d1YJDKtxLl6t7"
"aKtFTER38wu6pRal7eMbN6E5KZzO6QOV/v/gERgGun9N+k8lZBywM8B3AzVXDFYLmWkeSNw+vGLk8+diuRn5vQEYQFa1SOCZkaxXfgGpDF+wbdbEkxeMiW"
"j5peOHg/ppebMt5q8li4/cx3+RbkN7VNp1ZjC4UGKHk6L5/tGP6tcVcL/U7zeUOh3UqIYQYUCqhkiB5r7Y8FnjRVOZljZrZeS9IeHsbUoYnl71lbbRmrx0"
"D+4eTA24FZX9fLr72yPAi/pDw6/LEJIixxgA6LGJ+gDU/UDVwlStUn1cLdmRx4oEVH8U8LWmGRY2OLZjo0pwPYr1s7i201zobtMmOCi90DOQLr81MPdy+Z"
"zvI8xqWVk+9lHau91IlIoLORjUXImQjoTrrOMnw3tDhRoma92V9phTQ5D5bvYawNNt/zLecZuZp2tlJLuFL35td6eSXTTFJtyR3Ou/QWD1uAsYjqWnVBfU"
"IG5tlxUFdRPrPj+jrhbJvwekFwXyYnpEO9Zulpy5mEkxYCcNgKloo9VKa8pFTrXi7pa0tXwS7kczLfGhjVJGHnGiXq5+0p9SWLM6FrMyZHoFoS7l4ih9yp"
"PF+3QBO2HBeBXudOjewZerFTN2M/4pOdtUsgIvzwxz8AwPjrn/1ihrVaCCmKdenqXZH+ALen3ngMoDGzRfP5LUvhhZsPoVr40eNSym/ec6fJLtXMvCt/nF"
"qT17wKKv57XN6VsM2S6PdBNagZAlRv1gNLOew4tIV0UFhXrAynQ70G0tlMpFAtdthErk5LbquYbObCBTdgZAqEr8/46fJ1H5sCVy8GE9M09gEsI9N6wpcE"
"e/tOJ97lydziL20QkNc7SHJcm1OFmycQ8d+ff1HlhZ/d9YafURNCOZTjumxEVTEnzDPrgVLNFqPJq9q0CRkepwVfUi19zbE6zAqaKj12p6j5DglV8qylqe"
"9qcJvrP3WPew5BFvlgy/J2Hx94StMRNGfKgQhH7UwR+aR+OtxiBXjuFd8SWRQyrJRZDwFG4GmK1IdXl+enVJWTShPP55fiVH3IqnKZAwQ7j7gE+KwukG6E"
"HOu2VGuyz3YKUYzvVS6XE1WeUF2mzqrwUqdaA3AubBFlwKCvf/BPjuh1jmVDmDgsWKgsFl2siMEeptpcQEYFxhZOYc11/0OeHOqkeacF8YQ2W+aR1azQpY"
"wtd51nA540nfLYEnQSRypzjsUxPCxhZUkUoyE9zj1aMEJ5uAGP9GiSfXOar7oj7cSHJ9MvZc32/fntHdNXTvC0ukSUJ3vEQVXjQ97KfB6+fIurXFMFaJYu"
"2AJq7blsDbN9SovSyURe22oEWFqTI0K7xh4RaJGFEtOYAEIaG9bf02GJiWKdiuuHBYguyHK0444BT7fEA4FnLmydG1jXcvq9ad6nGHRkRnM5iJqQ3y6R5h"
"fNTPNW83APjywvS6xb4eB4fJmZXSRDhHjs2lLnPb0r8qTJJbA3FPadkdok8/FpdCQlMBQdSfT8ahRYhiGprsxpQefm8oIxBsDy6xPGqOqMghPr7s54r8tE"
"LEND815nWcacUeGdhiz09WQkcR5S5VrSUm/U43Cel1qbAa4uyWDP0MteZpWcR91hp2hTZld1FnJTFgJq3BQVUc3dLZ/mvuWHPqdzOFS1L5ncWuAzT4Y07R"
"zIPOgeRhVgclBpactMLXmYNZccWSwCSLWye8ZIOKj8BQ+58myhSzxuVTsOnUnUm5sZlbXI86s9duY8p7RazRGVUUP5srq3PHzg9r4UMNBu/mQIkqcypuyT"
"qeE8NSJ0eNXFPJWFGOdG1Iu9OztQisU2S2RIE3LKlS81QwWyuLJIx+QXL223ONwvXFLHmB9pmTGbbjtRthNBWrD9JSofEX+nWicSHqjsLRMx9nqVORSzPA"
"RZt8emrdC5GULPXjyEN/0h7MP820PnfxflreLxB/Bu5SLqUurXGeitGvNLvb56/6kDJc3ixlgxkyNZ/S/b0wHvSaPKedKMxy4uDlumRYNBbQ+M+fUEZR68"
"JMRPQD4WUr4vFI1nDBqQUPnJV8hr7zrY2WUm9XL1i9P9cg5wau3rrgrSspkSjVcPMuHOYzGExDKcqkFRc2Ui0MRII7vvTHu3jthcPqa8+jElTxz5B/eJNx"
"q0li+X8NTKmDaYop1ZAVKa/KW5oBmBcrIulng1agn1ggVLdSH4StULFmSbak7L0gT2TJnhLtOTJcHN7cDlMURekRO5uC6DUx3jYS4BJyE/VZ9DnTHzwtKo"
"xtLHJZu5rn53JC3nI+pD1IzZLJjnZAbe+sbbAICnHz8eY0R02LyU7sdzCyqcihUe5GHfJpnv9PV3msoYrYbvSiuoLJAs4GQFOc1u7SQe+dzIt3Dufe2Z/Z"
"Cat1cRnSdkcmCLJ8LfGHdh/N6PvgcA+Juf/zKAqX7HtkRd1xoHTNlec+EMyvbvgoUEU5/lI5VrVYw5BbOTxo82dLMHq09Y14XguESZJd7nIXspCzVaWL5U"
"CG18LDp9NXo0HXysq/BK2VR/W95d6kn73iq7e+jsnq6xIJorsJRBSl+GWSdtHsHEtNHjyXCB3BdgfqWCk4rRcjP9Qlh1S/NcT5U4AimNeyPn0hNA1AIAmq"
"YbBzstGUN14kFXeBaUD8RGU9ps3uWCHdku5SIkGVys2bIFDIh+nX436hV/JYxxQscxMdF3WU6NCvXuzMPKPMgdqBqFLUiTnDRXbzgQR0K/mLna/I7+elB9"
"9BwWN9OuqQQ+rUdl/lL38AIedO/IMs/5sEkn84k6hoKovPlhU97Xv/6fKzMqVWvSxDaxbwb8E4imAXQ+iucA6JyzZK5qHug80QL+KjkvANCltfDlxtGQ1i"
"VbpDukbPOulWXNnirkW4jAk4mnOJbwXBtAc0bZVISBq49U7af0f0P1b8QuiZ42DaihsPJ2iXUQvKjwzrVW7PIR7x9cHB/x9IBCakkrX0aZy2RZyaUnzxPP"
"yre9NEujAz6nygBaMBXs3pWgWoR25+R4aMfw+D0stsLzrqopqRZU9rzPBeyGX52e6+OKQtZFE6DmQMeVd1alkvtuvpxXGEvPx51XwgcAKC0Pn9bBcxlXka"
"pxfaWcjYHS6Dfc7lH1vMVdCxjePAjVu58FkFokSlPjOPRhIdsS18p4zpMe5gHRjsnnkwacsaAX2DIqSQNQG0JYk9GCKK8A3A0JM/tAUrbMgRlctzwnO67e"
"XQCCZfgpyamayI3Tax/GbQvpfToqLc07zzhNY0jeCWtKIHO9Vlsn67iwrjrb5+M9fzwbB4qtNpgDLBElm0lZeMpCrnQAqEr84glDRGo0FnlbLU1TgFoM8c"
"xfx2iy60pUVtA2AqShJ4MlpNJNxevHq1pI2YBeAersp5vLZ5JsONTh2/Gi9HROHTj9tnU2/dDYBesxAD1A2XTxb33n6wCA3/7qdypT30mre5/F0LF1dORU"
"F92CFkjfK68rvuJKLqaoJ1zy4LD0VLlsjpjvlT2AWqBh0JIVR2M8GEQNGHssOP3dz4ODt3GNsPBI8wp21is32bk5LlqNJUNzE9wWBRrAXmTDG4+Q6s/qd7"
"mSyo519R9Xci7giTAmlA1ARZ7F2hYDqjKTaXvPcrKk3eTUm2cI2Xz8CGhuxrlZ+2ndXubuGDJipW+XgGouJCYnF8hXsFOqL1PjCNepWlUr2SXVM+HjGZIe"
"FU/kQVUNuqzWbl0dT8/zSkvnreJfxk151DjR5ZL/WVND4ZoZsHFuIxq57utUyXPO/KnItaiU8FoWN3npdc/yIhur9hojuJZTQqy8SoQmvLs0zBpX7BZtvZ"
"BGXcOVrrQcmA+DcJ64p819V83xRD7VOUcU7Ubey8GI9afq2c3W7aNJiVM5NxrtP3HPX+kqBxWb29lTtBZH6WWayhh+YlAwH59R2/NagqrwfVUZ3heaDzwq"
"QfBrK5fHbr2uWU7Lj82sRT28uVH5Wr7XpURss9Yv2nlQTvVsRGG2cigXvTgTynWlr8HW85Ox6F5NBtPvz+vEpV4vlX25WieRSo6nBR/w/e2vPsq5Wg1ZLP"
"wxnWfSs9arE0uLfqo+TgisdiWqzeb9OobVDa763eaQVwoZ1ohjyPUQ7yvVcXx36eCnhsKYPHJ+dODJdiwr5zyeCdeWGSwwKpyMqf4jaxPF0xJvsKzQt789"
"RnR+8+vfTfKj8nAdxDzWYhMxsQCunKduPvPBDDv9lQOHiNNe0pEuvdaaRyf1i4AI1mMxgWIFVKNBXXNAbHxzDlBL40lULr1a8xhLMKRw8M+yIeWBab6neS"
"v7jjnZyAGGuNLToN0sqFENJjw1Ou0Rz/t8C8FzlO5Cnmo3JoBWJavw7gG41TEqjWeU1CHY3x7iy7cILk8oqqm3dRauxVC5RaVsndfnsYL8ayevZZ+nnPOY"
"J57TMjAt32vQNA3GJbSWygHeNA14GCpe8DRx+vzTAS+S0xeyC1YpqJTcgmJQzI1Wi3DO5alEX4AYCwvKtahQ/iyYPJlHGdxRMadXyMhFAqtWwCTWBZB4bj"
"1fXYJIKXSEXFORPtjBueryE1/q/TkLiDRvhGacT03lOsBOEoh17naairx6VfjxjXgzDcG2Rl4cQ0+5TBuq/vv2HqHch9pZz6ywTpqpqHx5Nd5a5qlNKV03"
"3WTFp3MosWROWbo/FnHle38lcWRKOZAC3ArxeDPQ5POZbk95ZqXC5xkpue/RXE3jK/opw5lX9PRL/cKtwzwzWppHPaf6O+MBC5VcvPc4v9cA7lnV6orGtB"
"xw9CBiAMOw6HO+KX3Q1IdFBOD0rYoSKyKxeQxk9eRPHqir2Xk8L9/8zm9/8/H4tHEOwC8jh0lFykV6xbaWhD2GJ/Z1CmAnv/ICUD96nb1XZf6KOReyxQk3"
"1p7nk/PMoGE9SlaDlEw9PcFL6IeTaE6PVzxs8yk9T5ZFWeprNMeDqjGp0HHQ5GVkJCsoNoJgtzD5ikynmVCWxqIxWD/Rvxa4UKncOdBpsOCK5rM1sAZ35C"
"iHmUxaC3jSUkpzoTO8pcLqoDlpxqQ2rXhx3l1TVvWdiahCKlNmxVmI1dInQXLa7XWnGpQwabKIKTHaREYng9pmMn0xuOMqXDT6Sy5RM8s2aDCmJRyMpMxD"
"WH8wVTEHNaTCPJJcw0SFO8cbvgqs8MuWV5vWmS+tcFe4VuF+PSTsrPNQ/U/kKUOuUyF5BgalaSua0HrCCsRFueI7o06WwbvSnrQMq2ZjIeeRPk6S3jMrkE"
"m0i1B8fgh4yi9VGTrpqKyfm5cdbyFN3TE3eXnGhqbOUwKQgkoZT6OexWqPtUkHb/6RSGM9GydNTdFOARX5aaf8rViOmqORm4y9PJXT6UHlzErbqqc6DZ75"
"txyFNWOoWmX44eN6folDHi3lipjhDzjJ0xzq6XBSUW378mIMWgC40mAIBkJaEFShr/1onJP7+OcfhRdCPxJK6ezBBQBg++Iql0TBT2DTFDMGNg8MDBAb91"
"G+LMeNH8t1BbFEHU45lf4Yc0DVKswlB3xID2SR86OVvFYlEkSmTfnCCUTc4gWtI4wxQKpkVmPVmKXpfma7ENCYgnL7srgfV54WypnlV1/Y5D9uUUlwT5z6"
"Y05LWd4SxAvSta0mE+m++71vAQD+7pe/yffNy2VeJS7V5lCt/0lJ2nXPvUsVV15o/kZljgf7MeMIJHog+ZaU9kg8T9VMtpt0plkL4iQcTPNsc3eQY6wWZ+"
"45/iIr4TEfNS9v648CBLVkPEmYtGTnG3SCIphF+T11I3U04x3K1iverZVqW6h8qN5QbVOmciqo5VGl2NXrIM1eZYuk5oFxPiZxynoYYf6ymIeU9WrCPFeq"
"PgOtBGTGj/6L3weowb/7X/4iz+G14ezbhkRHmQCJ2DeHQbVDhTFza94F1ZGyipyNJ5Tf44kMHT0wEwTx5gPZ9CXSCYsMPeVrRUHCG5Lh2rr3avJUtiwV/O"
"SoJYW0uaza58ti7eycotKprOXIsmyljLJXHOdcPX4ir7lE6SWSTlDoS08+8xK0T3VkcImHau4k0cwDeq3PdqnCSeF48WxTYpYn3M7u2ZcTg9cLY8g0S4xI"
"AlUWVdb9yffe/woA4LNPP89PTb2S9e6BbWGUGt/MBU8hCwlqXhbGY13kdTqre2v5S/3ClXcrbFkmnbQ+n9Ey98ThSEOKqagwFZnMgCXLNq3Iz8hlmhbs0Y"
"0h2KRA87NP//rjUWF145F/aBpQ24BDbHdo9sDXGnQP7oHvNaBd8IzbBtwPoNZpFTfyyBj6ULZnWE9KWsrV107x6btffRsA8PCzxxX5zgjeSTsZ2o0qQ9Sz"
"7F7OYXtJ1ZgnqT5laLe0ERjxMH99RoN2THxPvTSD9bgo3QmCDt2yOj+XSxwUXCpTlMyBdym07uhxzvLTnjKLSACXslWrf4VeF3uWFegrUC1lYHvN6Inmp6"
"7UDKD6Hqq5I05yqg19bazkdzuIeL2uStkskiuqDa7YQKpSnqBsh/XLJJePmDpAKPnvOuLVjwvQij9lR/AGuvCwTQbkpHPziaER46F7Xz9xgYly/Qvwn4Gi"
"jONSydvso3lSkWsR3jVt454/qhd51AC3qm6pzM9PiCLsWalmnQSa0lxCibyU24QBcAOxeKVsW3nNDUAdJcav3rzC/mKH9p030X+7RfOrYXzWBqXappMstO"
"Qs3g2hrzhraPLvKdu/8qzmcVYFRiq9Hc1lWm/DhONpVi0f6zUZb8xjzyIm22NPcsxN68OcCcUCU2RTo5wXkVGgKr06KS8BShlYdfoknfBMTnRoraVDvZLn"
"tKI58ZD7lRR5LFGdoqSawHrOZb2y11sVDiZPGzLpyjadA9Sy4BiVZjBqulQDPMcDGaY80VKB+6uvJpQaao/qXmj5egmitDBERd4PAj799PPR3vYUu5hlZ9"
"VJzei1K3QnB7cAOgP+JPKysJj4sDKjHMys+t0GTFh2/Fmv07SPa1TIYSn7gjBLFA8VT9UywMB7X30HAOHzTx9VyvZ5z2zNdkD3wTQOqM4SgjnSChPGSdRH"
"rZUd+eU3BLTjAQt9u8fl2y/HdA2h/+4Zus+2aLZjGvRZnWshiouQhId+DBnb0zNUSI/MfWBedYl0BDz8/PGMbKdoygdgN4qQFsqIdJprz5KyT40353ckFU"
"L3VgfbL6fJPpHCwEk5Q2KTU6vxL8sGkX0vO5cFwylkStIMjqBOKH0taayMfFI0UlLxxsuLHLLeEJO8VHk2r62s+/m9+PUdFlNjMI2yZMmP7hHy3Um/T6YT"
"l6OxYnFRmhD5V2cXFE2VU3NrPeDjCsgVFkilSn6dHTfcfdPyUt6k9E9YbKpK1rqKE/r1A9xlGMN6q5oFG5/PW5clxll7fNwu4nudNUWQ86sf/yjYEPn5Qi"
"slLAeNYwio/ikXRRhigNQSwVwgy6y9ujl3VFefkIlHXmAnV02DpoooF1bZ+HsAjWfplkJUDDGN6UANLt94gf68z+7PWx36b/Vof8XjYQwt8lm7tj2EkHk/"
"pLlY9ViCZlTMbO4DxUIMz4TWCn6hZ7sIbEMdnK9KuRlxcceWqtvWc4LNzUK8EiBleWLu0TFxAFsHMgW4YtPKm2Ohauhpr0+uWZGran309uZ0hZan/DorY0"
"siudNRbFeE8NJzTXR7hbFDnI2B2padWvNJWek0pAur2p/lQwoHYGuDrUzXSfd/6gvmZShX2SJawRYdUkIwu/lraPC8XEbileZ4FfcMwGkPznifqfOQM2/n"
"D2Yi3RssWGVYlTxqb1am85dhUhLrEm+JnHtenr5jalGndvqvtTplkay9H6XFSQ+gLEidCQGff/5IPSr4raGyKI9s3l4WXutI4BQ8WSAtQBX+e9WDGmy9G0"
"J/tsPlmy8LhvtvbTA82qN5QWGrTLSaTcKop4ZhbIZW9BzOHhFLLclCF7LnpRrPzSOnD+Ze4h2CYKmiGkNmv/ftDQDgb369S8nZS6iyrJ/gnOclOVXR6hzV"
"F1R5XKyPSF1BHoagbMsSNEmAkQ0ElPowZ8jRQGSCt8AmadzU2MKnS6/ojTYaICjLjlhtv9QHPsQ8hVcpmHBsk+w3kkjPmZPkaUdArXiU5S3bf9hPE+WW8v"
"EcOxH/C/LihF+lju5ILsMuWBR9cyLcU1j4ZmCXJVjNiInTaxwv1CqOGh8xJMtRm5nUok55DEnv0QiMc94ao5zlCUyOShGuVRpAMpQDcypYlpNTS31tMRgA"
"PA/PySGPHTI5U9lvioZzeCv6c437ipU+xaunjKmSDmmsFjKoOQDJYLJgWYAnVb1UxXgAXwaCt1mrc/j6SwO8fPAcQzdYVwq436L/JqP92wEN5wlPsukwVn"
"pgGsPAEgyF/EYPdLzH8TcjnNnM4F6H5Fh6TzPeZ12lla2kTfKp3Iyil6LkMrVp+CJh0SsLsLP6hot3krqVzpr4ULgCVUcB67pwEkap41jphmKONDaoF0Fg"
"GUWUujF6WxHcLHt6POcUbHgM7cJQOeWDG0q9QKns2Kb2PKeoIuW7FvCyGezDE4k09ldsZzI8l+9GqWWeS0ev08wZO92aY443mdRVYXpIeHC8UKlA2H/3IC"
"9U8ipP+aiduiP3SQls08Mugp8plXSGdrgSOwOGYBmITaPBWTFkFm1ZIvL5NfVOcp7IKrXXZCIji9TXaDrP4hbVq+TW18Dv5GMrkRrEOhcJNKU3ScmjtGBK"
"si6pfUNfJeA773cgAn71ST8+i4uJHFlGL2G3ucLlxYtaI2H/foP2kwHN0+nTd4Z98Dxa5UqUfwWojmcFA9/7kx8AAH75r/8W3A8pbQr9pvezIi7IHlHnN4"
"9hiYyy1Uo+eaIKUOMtTvdJjoFCAVulbp/ktlfTXqqfW1gSoCoXbiYgCMfeKLAX7e+ERUk8Y12Yko+WhjkYIaldA+ECb4m0J62wUWYi6k9C50rVrdZecJaK"
"PglJ8iUiQUV/ydGkuO6aY08ourvwPD2Z2jSyJH0ActU7lb4GJX5zo3SefVZ4RLr2JpmnFCfeU4+mzuzV1ozrMas/wntzwECHliyA6g4iy1VlGgAt1L6K4w"
"hjoZKXu7BLGCRTZ+Emi1fqnuLEGvKbZyrPIkkxevOWoIpD0obtHMN+0LLyCrBsmJEs60duv3TqVHs6UX46XSiIkKQnqrxSAaLO/ZiegHG/JwFoY2iMwvF+"
"BNuHouJ+ce8ZhmZAlc4b7L5OOLvkbLCZgwG4D3LvmqzpUpsJLRoOyCgAtg3pGtHO9l/Mk8ks7pSRlzJgqIn1a6YJ1frSAmCnjS17W/VuFh5k8R4LpSoB0j"
"ApfQHIRTdSX3HqGxFk00wQy2YxekOCfOhLIx5rpa+rqb32GIRDMVb13n+1d1LuHbWIqgA66+jEW3CIyuMDSQC0BdTMczocQfRV+U4Or2YdKb3ywjstPu5b"
"wqi6lyoysWAJwjtNH5gY73S5C+RKZlixIyxbV0mM3sdpxRBgy2ysbs1SlEKxh0SLDpnfiJovZ6FFNdZvkJ2Jsperur0aU2JoCA+lDC3EQVVasg6mqZtF6A"
"M6dJQjs6XVFOsr9585NkJBWinoBTA1ZZcGGRdCKgpr2wY/+Uc/Bgj4q//rZ+j7QVa+kIA1y6zMSrtyirxtV54sYn/JfwtgrP4WbaMWG0WAzXX91ecjoMkt"
"LQyEEK/qFCAQrroX2J6/xCQxMHy1xfCwR/fM2f7CAA8UVv9KAI3pQhoFhgCYk3L/u7/8xYixEfBTGg3EzBiPM4z3SKRZRKK1BFC5b4uwpR/Gk3nqfIqdpJ"
"5N781rGrlLHaVWlzq8aFDNnLBcU+FUxOrUBLGMsICTkz1UK1uDl8lIgHUc1yy9RjENJkFdhmBdPynJLTLny1NjW+Ql63P5wXPrpcppxozzkm8Zdp3yTnWr"
"pEyNd2rT2NbMohzM118cZW9NRSoTF0UJleE2JLH/Xm4yJ2+y85AiLOqixlh+PEt4TOoo2MLLsWnM1wPkQ9EZy+qQycqXRXnLpOacF8meJMoo4cj7hqkpz1"
"yXbRiANinHSnPZWlCjAMrWh9w3PcQr/ZjpMO38gRwSFAkQ59V6oGnuQYCkDfeSKN+WIfsC5T7bdOKrLURgDLg8fwpuFoDQpsH+A8bmioq2Sat0k1KLDzSo"
"EvO4GjY9i2AblSyHsLCeP81AGcZxPP0rPZdpoJVeGZNz26psxlAGuclLQFKf2vJOS4uvC+dBzOMl8z3ZuRSqpKE5gkbpsUrW9WI9tYlDbifh+FT5YQVgFJ"
"tjWBvTMipQKH4pOMFvXEPCCUwiT5QMmFyqtKy0WDOmRQufUWgnGfoWui3P1Za6MLYFl9lAzQ/HOrNxD52TowJkK1nnDkCJN+tOekYZgcY5UnlDtEZOqViW"
"jNfSWMbyNc242wWIRg9AXJIMhbjKU3eaQj8Xr0lFnsVF4trzRAswUrhQenxwyiz2sFrxKCBidU9zl9Mpe3AKq4qAVGhDMi+STmuaRmROGIYBf/VnPwMADP"
"2gB4apuw5rebxVW9E3tiw/9r7wPKNHmfpDFSStt+q9o9uqeAf2N0awE6ceXTbPse0usYgYGN5usH+bsXnSIK6S5mHIHm/sSzHclgA3A16e84zAF8A13Yfw"
"VANoWKCMq4fVO1CdMLd2+EWmMmxau2Jok/NQOYq2IyVlKOotHiVtJY/NS02aB8z3f/ptMIBf/vtfBYNBjBYiVT+W+SVejH7jPPubuDGGj7/4xUaxuNyBkv"
"p/NiomAVV4mHFxISueQlnRa2TJnWkPo2M4NXaGIquLdf1YfOIvYbga0tp48PoKgsGaDYDYZdOVAdUMlgQlE9N/Y5fW2ndM0cWU03hUCsBrkvJKhwHmviup"
"Dlgg/SMb9r6CZ/MaJaGIBx6YFIrcLsrxP1yrhOkcBZgVva/t1SvSMqvU2/KqyhFXCkCplq68ztnb+0KyFvQLBse0Q1igQioX3/gpdGA2I6f5czusA8pxxS"
"zROOdnwJECqPmgKX9HgPXuyWthIJnDGWQ7joA3eqUDerxoH+uFInPUEXZfZXSXBBrCdyPjKl0JGtEjCqDIxvNMv0MaSmDJJt2ovdJK34FUHlbtl13IM5g4"
"yUb1Fa2Jy/GndLcXfIuX9TCKShlXtTrljd4o6Tp4/Vd4wfbcC/2Dy/pWDimIkQGdjRSOmepSVrR+lka88qAlsxmg8xy0AB0z3kgZA067B13NcSucsZSkTa"
"mBLhTLLGxPMnO54p7SF6a9KZpLuXyWmQhApdTyPpiG2jhgOubRqYbzpGGFRF4aj+ZA1LaM7F2GVWkxRcE6SkcBoe307glEDogaebBbhvkhrZ0CjGRdMg/q"
"kJDqYiRfVDbAVIxMK1pVTzKJ4sCuWUeljMoyiqBMuGuYkk3q4qFtc8eYKHj0FLT0GJA9P7GgiBqgDojlMxDldxCvyUkrKmbAVIV+A29N1+CSH2PbLvRGhc"
"iHNwj7twecPe0w9ChDuiwW2CUgzOBReJjhN3H0cCPACmXIPM6LUgAAG/IddFkalGNfcdq78L1qDc+11OGxWcOh+oozmkMVG+WVZLnxwPjlv/t1zDq3r52k"
"nADXMatsrBTRloQfFtTiy5IzWYp1Hzj3O86jR33flmS4kn0Zc3YmipOGhI5OHEy4h9KwyHUpa5LeTq5oBkrHVg9V0cuCysXPoidE41UGJgygZjCFzaioWm"
"yRaOh02bLxIGPQnZJsjqQFmlKL1ZpMDoO6E0QFZmczYiPanaic4kgCmIpKik5CUh4MuTpNdgsyd6pqnkR9JfiTL3dS/+VUnms4qC0t2dtllc6Gq8tl6wTv"
"WyjmwGpy7ot7VNzyjYsShl2tYuom5VQgP2Tolp2syy+1mFOTImA1IfyWQAzCO5XACbOAiDR4JqAldBcbUEPYXe2SN0vG+6Qm/3bnXUHj4Qu0xwt6jKOICL"
"t3GN1LBnqkOc1RHqP3mbaWFZ5ofC4905A0Ok5k74d8RtWQV54KoE4NJvIBEzCIVnSV0uJKh/8LQNUupM55LnMJYimzuFI2lMGGd2n8ms+N6bwzAMlPjuXQ"
"rpZCTkOadzV/G+vt76lMVUpZSM/KM0LKAw+Icp9IWiucqMSSJ3mak/IKnVFNIRpSrKaV9Ueph6IY4sLTYkEQaQOJs2y0Yc3Fz4Q4Ce0z79U2hUbLmLqrf6"
"ndCec6Gq18W7vEk55oVFpBWtnykJ8uy5oxV1oq5fL7KLose3YHFfWg1FBOXVQZIgRkPOh8eIGGTV+eQuGkSodgdDSbhBnGKkdX4gLEyWmZEr7UPLNMIY4y"
"0+0/F+I2ZSksdWTL5dtqX2aStqmHVPAgr0uOfxsTpm3IgGQSewBW+5xcr3Vz7wz/4L/8E4AIf/G//t/YbXfpfSoAU//Ni90y6D7nz7FrrnAsDfeA7Rs9zv"
"abIFepBFkAIAogrf2lgTJoRi/TtlYzpuUht+5PvjvOFP37X+xAQ9BNg0wP6DmzkK97cILjHTj9jJTikF9esf1OllHJWXkokiPnCyfpHbkX0klQODhR8Us9"
"NIKxiusUNkDs+GQcT6n4DVAaj8sa83oMZlBhsfc1t5HedJTALXnLsmztOiWNRCwWfYt+IPjQJkaQFec0Nufx/cgvZQOCretGcEoWto7Mk8WqZ9NfOUtS8t"
"kVLW2siqzYfE9UVQh632D1oAWhT1IjON5RhBF5tFXyVlRqaz1QVl7WIDC5CxwzacrasUpbWoD+UXqOrIyFolcGZy+0lB7pbE12jg9Z5CLnGcm0RzHQQubz"
"HqgpM7VvTR7FH70iWTOr2wrIc0pedSNgJTDVoJnnRZEBM4KdDNU2ekFSCuO28TeB2vGzZ7kcBzi961DfHa7wcniMaxER9u8wNi8Zbd8K0IzK2nqjZLzRCJ"
"YxLcav0AzIYd+GR48yvjuIfCkALSN9H3U0LMI+1ybyQ8BgYk5iXs7rr8Tlfa8vqndk9tV0pW5yDbJkHGuTVr2quiuJahVoDImOGeNiXctqqZrZPJWxoEHA"
"9V2UWhf6NDkB4V3hcXNcaCadkOTt5baTZ+eWZpKBVMqJynOM2eQh65t/FycMGUuGQpq0rYejBs6h2KSV0zyqap4k/xyN0LKPMohPu1LiyBkXHSHf8FRkCa"
"K2Z2hwItWDSDaPYD0rQluyq8wpNphMI4FRvE8yY82HqAmUN0ZlccXCpgLoU2V1vimPMg2rlFSWbeVdbxWRdW0VsWfdFzEJyXVF7rkOU0Crk/pg7rW2aoMC"
"tw1YeVtbJLCK56zCs6QXJTX5HhFhvxvwF//0L4GGsNv3oC5u9ynBMwN0kIkJ977YP0ZPO1yX+Jywe6tH+6SDXFiUPMzklWYApACeLAGRkQGVYhqMiiN6H+"
"FLbkkRDuO7zMBf/bofb7YkgJlyWDcsMB5BOcjGLn5BcQEVGaq7hgUsFqAqC0oPRVrmIlediTCKyQGshC2hw9mDCIpxlw/VYBEG1jLI+7hTXoXvIFfm6gic"
"9u4iW9KYCKnEMEnQQcapUXO1GZmjjhz7QXhSmTdV5/1GTuUpQRHcWeofEYYSQE8R+FMVdRqyc9Sct++QqoeWgQy9j+vMyDVkZNGdamTliQrGVK+qeLBq/5"
"F5x3gbJEO6SsgVs6wA0dpGflZluZCbjhIjca25SKXJfUxRHkaBVw90V8Cp08htNcr4FXMh8WYppxJUtQ4So0ywYUO5KN7JublhL1VT7TXrA4J9EFXmAcmP"
"AFizVLea8mDsCkR5JUGzifKEXpWbABICTDNIxjQaUM2iogCgIKSDFXJo2AI5lYBKI5hv+xd4yY9xKtq/OaC/GtDtR9s4eaYDo2kbYGAM+14sEIrgaQCPxO"
"/YrHEB0UAppAsm0MDjHDQTaEDwPikoGeEJx4aMniuit8vSTAxpBQAUxpV1eUirK9GXaqBagp/IMG4pkUUUQy8+JeOxSH6l94RC6SpKw9nMmypAlaOAxBoR"
"RE3vFBDrUhoHJJo8lS7qkWsYP+BNaRWt5q/EjJg3JfDy6hwTsSrPrhYq5Atkg89ueyg+Jl40hTJWysCtqEtRdtR3EdtKzOjY3PBgyGl3reCcIApHgdl3Iz"
"+YIeUtRjcciKvysrcqFG+an5Sdx1qgUvGW7Mdy1EHIMq2Sg7NBOfAk5ZiLlkERcfSXNZ0pP3dbRww+QPYh86UWkTfb+6KldJtXYFBhfGQg8xM9ISpfRraU"
"c3ulFkrGg2w7EvxYfs1vEn0j/Qtt6AJlBjg0ECBHChDTHGvKQ3uzRThY5ZuB167sjad1Pd9+jgE7V97HEG8I2zf26J6djVPswQttNoQf/8c/BBj42b/6/9"
"DvgtfIDG4CGMaQbeiQFDQhD5y90yEs9EieZujLzCPohrDuOCcaPvWW5lfDGGlGgOb4+Td72IPo98pogzeWbVgvX7A15EVedsirbRcwD90OrT3HuBo0qWZl"
"AUjDEcJD1byp+bkIQEqtiBEadZM8yScI7Ovf/QAA43e/+FjUtgS7wFLgV+oQzyuO7h25IWsu5ryjgRb4lJ67wCW5gCoZbwroMqPy1KVsywjAg/nGcuRTVN"
"92Cf0FG4tn8X0SeBM/oB70nPlAQJd7sAWDiufpXVvLRnmgAZKS7rVdOV9zeh4Ta0tRF0WKk9JRS/697ogQcfqiI8k8RN0kT+mXZ0REEBbhjiINUppsKBiL"
"W8G4ABXJo4eLZtSTEkypXDS8mi9EKHnaBQCaB7WAy8EFtaeWih6tYFMDPpdlFoWEPCPYFV4kCnAbgS+Da9M2o2cF5LnThvQ8agPXW7UgLMG3BGmAqMHl/i"
"ku+ye+sAQxc/43mxrY3+/R73ps9hukMGzTgJpxnyk1NHrRBgzzaUYC/JqwZSB6kYSwgCiCaW6iqLfUdNIQzkKNc6Rp0VEAb6E/yu/uSiGUwGp9CdslZB/R"
"377UJqOTS90TZnNPrciNvTx/Wi3xKMPFVXVA5U0grZTNmWbPMAIhiTKsvlRn3zplEIUyFGBr44FEmDPzkhs6hT6tjMKHRRWUR7CO5VHmmUNZCNHNHPYVnM"
"c6K0COw5x1NZJcJOACOTxNGaTZRCFUv8qyTNKkzH7Mr/M6V8Wt8AeyBNFUsLojlKAUq+gwQLK0VL6mwIyJ5OTvWBMFsDiHLdgyzHObd10SEUTtiUdsMiYj"
"59KwsL6o8rjcFIanxDa5acgDavJT2mfSO3UXP2mBGz5Yv8e6LGUsuPz4D5iAYdhBhR8HAjUN2s2ZAlXrZTYt8Kc/2gBE+LO/2ectMBEMG32dQFSBJ5kwsA"
"Oq7cgPiPH85acYsK8ILYiGGe++9RZ+9I1vYfP2m/j5/Qd4/OJ5ZQFXoJawvb/F+eX5qHT7Adwzfv6XfwMEfBwPgQiLjWIod8ieRQZGGsGwwfg89atsnHIT"
"wXpsyzG8ixF8o9c5ROUY7XqE9smmQV5FKvpb1KACJ1Tft85Tlpzqb5QZN6ASX7axNw/UY7+UoasSUHPPFoqZjIGabAhtNDgxrJgwg10MsyZfU45Pxke/HD"
"3R0ZGiBEisWFfhN7kQWAwobaAkAyuxFJ0jEu1AhSwEemmDvTDqAyDHvC2OBZ44vS9ATGECix4lo12iTZKYoxkS+sbk+dBRllD2AsfxwJznSO3XA2xFi4yL"
"NKrbpcKVZjSdMYdRTa7CUjHmkepyMDwXeBr5CQ9i45duvGy0SXEKPrI0Y2PIuc9kdgkAzXK2AG2HU5Yby5We+a7hKCfI2Vr1oLpy/uUYTSQ6pn1Xt4Hhg8"
"ry5IeLUyJlW5j2qAKo93AEsGHY46Pf/Qfs91eqEbvNOb7+vZ+ia891aDd4m3KrSwJNUBHmzV6oSNcYEC2AldIxgNQ2aMK/Zy8/x4vtYx0mMxRB9L/5r/5r"
"/MM/+AOgafDVzQX+h3/2f+DRi+eY6qbbzR67YY8LvgfuBwwBTJmHfHYucYgcBdCj6CUiD8D0+a4wtiJIRpaHqJisMZXDujRQnjfF+JcGho6u6LBe1otilA"
"u9IVd6aoNS8ityEp9ZKTSZ6eepe8ONjyD7PXkcFGNYrS6VHlQ0KIzzxyJsGF4sNdRYD/1llJwmvaEOu5ftKcq1QCkWUGWHMraXMHZI+XzJzokAExcHqfN6"
"deAhy0P2dfEkAl1qYnF4Qxy+gznOcWSJEdddRB5Vq1iAT51B1k83n2pTo/8yoGbvvdMv1ImqN1VTzrxQllYIVYFtCUDeIQPlT9EFSeY/wZQDPvV7MUOxaK"
"bIWlrGJLIz4OfGRAWIpjs53de//wHAwO9++XF+j5xsdMlG/p4sLIj6siV7k0we8b/WipcgSrE9ZB7ZyNAzC06lzJzjfn+F3e5KbEEQSsUJxyZLFIR/9Tf7"
"EdAFWEIAL0dPs4H2UKX3mUDWAGg3Auj4m3DeMr52/rWK/EcahgF/8vs/wR//5Cdo2hZEhD/43vfxJz/6Ef7NL/8WDTXVd0FAt23RPGnR7wjUNwFMaTy6sQ"
"/W/5Blw0P2/pIHKXVNlPmAMbwbix8IFLxSHboMHmuc+xIeaQrtNiFsjAC2YfWvty1FagDZp9QBQLFfkoAY1fVKcGL2FWX8423Ktyak6GSKW7kVI2O79Jfy"
"+MgeX8xbG7E5Zy68JykfxaupksK0uNhHdhrPWSqy1IDNRsVF4EsmkRRJMZ65WnK8GyMTag8oSUdU9i2/LoWR4YKpfr1cPyYAPRkLyHtrCeiYdbN5NbOs5Y"
"4TG0qXPE7kGnUrBBCtSdHfcsgqhDDkcVzZoitDkDHzUSZBvBI8Y8iFouIUmYg/pO5FXkjwK407aQdHGcAsXNCg4b0jtVQE2VLOsiHjNwRjPgWkqV85jVJF"
"uULmnTjXySqSIFkhlUtuf9LsygUCVhap3QUPJvqQQdQBG2uIRACMbZySiX5qVt3mLTEkWUNe4EblgqE0P9qoPCSgJhBtCU3XjgAagLTpGrRdi+H5Fs8//X"
"wyRNsPAy6/8XT8rugwKpRh3+PZhx/h4V//Am1TB1JmxltvvY83Lr4BUA/eD6CGMATjgDGAhyErBBm+JQLTEBYFIYdgxaKy1Dea0C9DKJ3isYFBjmlrglAe"
"2uccldEw7NHv91k5MNA0HZqmVb3Y9nEJqvogBOtayP7COZ34Kos6Zs4BTwUOsk/bkcASRCjVB8Xb8l2q5EaGszj2khtr0kQQizmx7Ni2EawPqtJkcZYOEr"
"G4E08rciOWUOFYebBCAnPZTpT5ZqJg0EluLPg7HrZ4ReEEO+1cWkLZ+Il6Fkjbeab6A7PdR+p5EN41ZZchqWlSmej3SKC49UFJ2nikBytGkM75Z2s5KUJR"
"La2bYn4RgMUgU3/MZ9tSLKb8HmhmQYR1COPiJeWWyrrqPV62G1i+s9XIUMlFnX4X5kKSjEjmIUEw86OAdgKf7EeDNIg68hJpczv5MQeyfaScDHFBNCtN0h"
"Yw2ZS2UrJZ8gsSL5X3qe6PIEliT2lerYsUzk3eaAzpRi+0a0YwTUDaot202Fx8BZ9++G9w+bIeomUG/vnDR/ijH/8Y//hP/xREhD//f/4f/PN/+S/x8PFj"
"UOVNBuP8/B6+8s4P0G46AARuemBPqZ4MYOhzb2QggeaYaxNW4Y5eq5pDC3YEBk6GUBpXxGluOnqYMW1crZsyEmPx8eMP8fCzX6l6vPvOt/HuO98xHmMJM7"
"pvAGohjNmrEQ3oPLWStTqJV/WebmMoWzA20s+6jFLsk51Qr+Py5J9s9YQuI+sHOGhQOWZPyCZuS5HD1AK83J+p5UK5HmrOVDaR5jGWk71CKt8XXCRdmU7C"
"EvxSbkP2+InOl5RNGv+cD5RKThr7qsockJ/B1DMaxnp1OhcnUfGS4/uoH2UnyM5l6U2o8kk6i1QmUUZcBikN5PDzdq9YSEtWQZ2I6wCHAAJbuFhxlrpF6m"
"yepxebyKCCXDBRrgjQ4Fs2hOK16olGcLLGT7FtiRwZ2FK84nPd1Lx/lLvhOykrKvtiuVAs1meCqdygqstRqqMAUTnHKVfkCu+VxD0yf0EEtJTmQvO/Nnmj"
"bdfg3a9/A9/9yR/ir//Nn1UXOBCAh48f47/97/57fHL5AnR+hv/xf/qf8ejJEzQT3mhDDb7z/d/Hu1/9YPTywMGwz+Mkfpdn4F6/PADcCC9NgmgT5zsR5l"
"SRvNikKUmE0OScJ+W1CYQIquL7In2P3e5SheSHoVdtnc9xFX1U9M10VyVh8TyhAeRcZASNlE4NsQyyqavFNxQLHtjnPkzJM+W0SIbF2M5hLqlXo6cnDc7s"
"hY45syhSG8zxtgLUaC2wWIAkwthSYgpQEqCSkEeBkJD7QgHShgcFrjkb6mnONb2SETn51bG9HMPDriZWC6KCvkn5J0NaAKR26jURqQ+jZ15zmDm/NjLUaQ"
"us9D5JZJ7ELZSRPiezdL3z4QvpDvSkuKhkFLhn+aXBmQGHJFvSExPzJCQ7DHQdYoMoABFyyABgwkTJCs8WVFIU4TotpCpWlpn8jezVxwEUdkmv24KsIwNZ"
"vhS2e3hCVDLStMzhZiryNv3OgCyJ/46/hKI17a9qaCIMmWe7aT82vmhLlzXhQZKQndSlCiQjeJCWvQTLCLTpH+U50aYZV+e2eoFR0zVoNsEr7Vp858d/H4"
"8++RCf/PqX1RAvEeHzhw/xH371d2jvX+Dzx48nQZSZ8d7738S3vv8jNNSGbTNZZvG4N+LxNB3iZlyAFI3XNKY5HWQR50Ipnl6ELJ/0ZQ6jpGK0KnqAMaSn"
"vhSmZFsahTnLCEbaa/IWaqmRlYzjsIwn6ZUyvJm3ZDjhSZLjQXS1jI3lOwnCNIBLHiMQKgyUSt/sqcx9PsNjAhKSjEQDpRL7knKWe1qjp6iCUWb/p1hlLO"
"dtC3UtG1kVLvSZAFQF2lYeQhyMfApU5jnnlfuU2asrUVPqVc7y0paE5Sv2lNilOF8LEiNzwhs1W0m0ZyCzzCo3g6gdJYYFquUlBJA1nMlLlTYOcJJNYHhW"
"9RFVTnxJi8aMdqee+VH26hjC4lShKZmHOJxAZMUiv3JRkgVRK0uhDT1gkRoqvZ/Tym/xJQMi1auKVApEcyoSZZIAUbgypVSmBr68MKgQkCqjFuqU1nCqR5"
"hTjf9UGyS5i/sShBXQln2bJKA2BAhQzf8I5w/u40d/8qe4uP9gctk9EaEhKsdRUU3GxcU9/PAnf4iL+/fQRM847CHNPOUxmdYMmP6WIgK6IYxczD9Qbgch"
"m8g/iITcoFZmVlpN9M/cB+S0d9FXZH80PKcts2ncyarkvmf7ZvkZNlkepXfV2CucCsG00+d1X5ZZmTFUdI4sz7L7T+g+la+8JwxSKRNZVtFCRh978G3Huz"
"BSXZ1s+acsMSn7VLriTfqIUi+QtnGAsPBQhmxh2q9Sr1Ce7b9NtIzGf+mY34Tr0RpIqYjzaRUs3xn/N4AhoxLpTbJlsLZ6CLC8REtWvpcrl7lkNTjzPSkj"
"lU5Y66p+EVQEXwzLV7g2/TveGy9UgEif7gEgWv2QecqBAd/qsTzo91lPsAsrSi6qSTJQ5WtvUyq6Wt2tfNN70ZghGE4h6ub0UxJtJ2USf3MMxOWFAEnhuB"
"hjunoB0Jr34rdI54O7AJCk6M09AyrUEIgaEAHvffNb+MEf/PE0QC4lInzvRz/Fex98Yyy+EaBO4q/3zyikgh1ts6QLkg8pt5l8MfYDEmAoQ+VMtbYjMe6z"
"cSrtqGKEGPxU0YegL+wXQey4H2UFEJUhUq2HBB82TFv0ZzZ5sCOrJNycLkwv6M+laaCyGJVkh1I/yCzkGbpSd9p/tTHkOuLmwupiVfdkwCXJZb1ieNZ6QG"
"KS5iPpHkfCtlzZD9TWKmQ9yqK1vfKkngZ4wiM1nVwYAuJHGcpVeo3sjQjGWYjamzDWXGEcxIHBThqrYMUd8cUBZSjKeYlaJ1G14zzgZCWNOFLISFiX2RKW"
"ow2ZN5eocp0tOyiFWIZMvY4rq+/OXRe/jbKJbCuL1WG1yFsotlB2OXhzgyhukvIPii52q3CqUdtuCgba7gxEjcubxspg4VaUuuLf1YKV5LmqIodszf/gD/"
"4IX/32dzC9GXyamBnvfe0b+P6Pf+p4BZYfWXGnWYTcC6BNXEsZsuiGFa+Z5Qy9NEBKVkXTo0E2RFRjRQj3UaTIp5jrFu+oKJJs1tDBU3+L7Z7yK6ce3CgS"
"2SbhSvPkzmJti6SX5NSIetOMQa2oXclI1UeyoHi2pJSRA5IQ4JsEaPWp+lvqfVVszZ6CabKQl6miqZhkyvGcY99TU14kuqQVDFAd8CICOgIpma4k0DYFON"
"S8VqlNUhjfFlzJO9dZCtjXUrmNTD6i0VkAZs5XdFwTZY1pRmXg2NMewKn3RMOF17NnyZV+zSZ/1swpA6MuawQgAfcY+isw925PZNFJpA0vDYHyUGhTfuro"
"eWFGzLbozELpFXquie2la+YxnsA6/TOGR+oPI7Vth4t7bxb5XNx/C00b19MJ3hiax8iN7XopDaUkmnfRvZQRximcyEOMgDCGgTEwxr/DgLN79/Ef/cN/jP"
"N794CjwJRxfn6BH/3BP8DZxcW4X3QQxwoGV37khbNnz3Dn9/LZxs6HC2oaWYtWGzyyiziAOhmSN+2vDJ+o+BqYjlZrv/QirDVNRS8g5T3GTHR9Iuj4tovl"
"RxmtbnrT9xT2C55JRIImwAlT5Zgx7IKY0k26TdS1/MCAOI7PsuGKhkq9M0YCKpZqaIqx/oNoBxPxM4Umfa3kFh+Eg0nSPRPnoIgNNco40IFt5uZS3iPoOj"
"JSaCRZbfIdKf+Uj1jVKtLZzdFZ2RklRuI5RLgn3RP8SJ4UP1EZi7ztiDAbVhUONjIvUwSH4Tklg+KeKNtmJl8U9Xz57EM8ffQL7HbPsdncx9tf/Xu4uPeB"
"a4NBeo+yLcsapl/kpjWWne0QrC3dtJ+4pi/d0j1ujLBjsURgHvDw81/jyeOPijyffP4hurMzfOUb3we1LVJ/SCtL6xzYbm9llhZSijHIA6t/GBg8DBgGAv"
"UMbhhDP+7r7BsCbfd475vfxvf//h/iZ3/+Z/NCcqT23b/3+3jv/W9gv+sxhJOMhp7H04yGcKrRwOMK2z78DbyNutAs3nHCAxnWxK/4lRexstPyFrv2uHF/"
"TDcMPXjYgwegoXaMGJh+RU0bCkm7XSu559OV8oEGjrVTRGrKHdjspMnZkTrJR/WWIDsWaasejNiXqUtz7oqhPx5X7B0gwcircFkXnUJz8bNmipGcj/00mk"
"pYRrhSIWb/aDoJypFBXmcc3zOj3bHlXQmySWH8pDi03ZdTWiqdB5tvauZa7ytl1/nCyt/A0y/GJKLzSbAr8vEuJShU3EU/F5G2XLxCJh9mE34tLERyc/ZK"
"Ltknk75a81y2Z9mQHERlHUWiJH8C4cWzD/Hph3+O/f4SRMDVi8/x3h9+F+//+Dv45M9+XS6AqzSPtvsrAqdaBs5gsYLi6X6RtgbomxUm8qjJypfx8LNf4+"
"FnvwLHo3qkFIcBn3/4S4AIX/nWDwSPwmQPgErRmIpfmEgjU/9Nq/zVZwPDmIogakBz6Ae1wCd6AfsB6Bj43k//CJ/8+u/w+e8+XDxnysx4972v4bs//An6"
"3YB+v0e/H9Dvewz7AKj7AX3P4czdIXjCnLzWZAhHpRTrEXUyp5YtvqhC+QQSZOUUDId+D+579Pst9rsr9Lsr7LaX6HeX6HdX2O+uxs+7NR12V/HM4Zz/eE"
"rVyxFoGwO2hcoQC0aSLWCiS0pfcPKOJTQYa7w85IHkeBEaIM6Xe98etdfFQffWbo79k61IQr+3QDfRV+KQkat7C6DUog+2SwEslA5eEPfjNr9oSMT/8YB0"
"bBZEeD62Q6p4kAfLgrNEfDA1d4WFIPz2OcmEcT7jcVsGPIYot3unZCb9GRKhDVWQsOiSQhbposucvEUZRgmpE4DUOJUdN5Sder3oyCJUWAKr98U5Sm1YCK"
"1w4XLC7PUWfkolb/Pcsyus7CSeKR2tl30P3OPp418EEA2yoAaf/r8/w/f+yT/GWz98H09+/vG4alTxkwvOErGuqTBEJseobA/9ppK4SiPCZMJqrnkbyaAW"
"/DGPp/LwsEc/7LDdPcfjh791QTS/0+PRx7/G+ZsPcHbvAZp2g7brgtfTpgVCShISQONYZ6P90gqEUTHHj2BzP4we0n4AI518JxSOPgOlJ+D8/gP86E/+Ef"
"7if/un2G0vUXQYhzZn5/jhT/8IZxf30e/26PsBQwDRft9j2PXo9+FeAFLuo/zs1hhAqyHkbst5NKcQ8dBjGHoM+x2G3Rb73Rb73SX228sEnMN+i6HfY9jv"
"MfR9+KrMELzhsS332wH9fijq++jRb/D82cfYtBu07QZte4a2O0fXnaPtztC2GzTtGdq2C4u3RrDl0HesrxT7rHXKtOGcF1CmoRj7Nlt9VfopXAwjnUK9kf"
"qRVvtFrsIStgc76EiiKFwdFiPO541Gk4fgCQ+1Ds99hMHch+mCAcOwBw979bfv9xi4D31jP2bTtGhi+zTj36Zp8zVi27XIayLkXLuOOuQtOBZMJR6T5j16"
"wpYEltq+ohSxa5xFDZUtzs6CiOuApIczA9wYLeX5lrUCqPK7fqvw5txYtvRCVQ+s502T2YgbEkAt8EiBWi/MysOiuO7sOgxO4KHHfvfCBAsIzz76FH/7v/"
"+f+OaP/0lhPWvXVA46Z1QV/JTeObkTK7X6K3FlBSbLUZEOxsDjAO37Hfr9Fv3+clTU+8vxut+CucfAA4Z+DnQIQ7/Do0//Bu3ZZhzc3QZtdzb+3Zyj3ZyN"
"CrrbhGcbNF0LajsQN5CTW+k4RBkCZQbHE316gGkYv7lJFE406tCejXtI5dm71IzKv+97vPfN7+Kbf+/38ct/95fzOMqMb37n9/De+9/E0I8HKzRNM37LqR"
"nzH9oWza5H3xD6bY++B8Ah1NuPc0PxE2mUwrTBWOEBPPTg/X6U9278t98FoNxvA1gGoBx6cN+HEHIAy5j/EI+ACP03NPh+x+j31pCL1WNsd1fY7y7RxJ4s"
"9EYTvNSm7UbDqD1LYNu25xl8201W5k3walPz+WHUxGNo12hmMUXP3OnfCJ6f9LCKYSW/wiI8RKRMCxm5QyoWkTxMmYAdbyvXOaEHYZxXHIbkQQ4cjCMBjk"
"O/xzDsxnt9PwJl7B/j5H9iij1m+8KF01dEAJoQrYl/2/C7CW0XoxItgCZsp2pyhEdEmXL4Pco6mlbCiDCqTts81q8VYjWGlKmINo9z29Y90dxYZWea8tTK"
"xUqWeWCcfLSdgE2+SGGFYlNvQZGHBt7kdpFWKHPR+zTPRCLf8MSeFevJobYXzMlXi0DnRU2LbnMfVy8fKjClhvDwP3yEsxe/yuAtjBnbljqC47SRbFP5QV"
"2PCsXBAJkF4aHTx07N3GMYBgz9Dv1+VM4jUEawvBpDhMESVmZn6lM0wZRp3WHAsN8BzR59v8Vu9zJtEUHYb9m0LagdAbRtOzSbEVzbzVkC3wi2TdcloG2o"
"RbNp0Z13aM86tF07fmklGByjp9hjR8jbUcR+0wis3/zBT/DZb3+Npw8/rYZ4mRlvvPkuvv6NH+Llk8sxhBu9zJ7D8X05dEsgNE2D5rzBZtONMt/22G932F"
"9eYX91hf32Cv1+bIcMlFv0+y1434dQbfRmh/BllzwPnMCSc++PK5okKHG8x4xhP8y0WPys3mBamJPH0/dXiEXlbkxKEbfNJoNtF/+do2260attNk74OBtL"
"KsIrul/hdLqhP/kuO/fiWBHKHnlRn+caCKUDaXTLKFMOsYYxFoFx2GPod+F3/BvAkzOosgxXqPrXDGgq2TP3TS9OfRkYKvO39v0MnJTAtCkAuJFATE3Q/Y"
"2IbjWlm+DYHoUOZi1jy29nMywrYMRjNarrwcXOQSbzgkPUM6KSn2SAeBv8y8aVhgGLDhIHjbRodIiPfH7CCErjbIm8VKjCS25GViEe/W7Tdnjz3R/g6uVn"
"6PdX6X63uY833v4+eGjESjNrJ2Yw0/nrkLqR4DyAxkvR5tK7GXiHvt8GsBw9jX5/hb7fhoHdh/AsJ/mWBcx5wTMUVq/mo2FHA2G8NwLCgB7EAzCM83sUwb"
"YdvdLx5KIRbNuuw9n9+7h4802cnd/DgBZXlwBeNsAwDl5q2uABx3N3o1c6Hheott4wcP/Nt/H9n/4x/urP/lk44q+ktu3wvd/7Ce49eDPPvYbQHw88hnR3"
"Pfr9HsNuP4Z991v0+x2432HgPYA+APlY513/ElfPn2N3eTWGgVUYVs6phvETf0rQHlKjJ1mrJcLCg7/Odp9SZzDs8EqgMOzR47L04oAQZmxATZfBttuMHm"
"13HsLHmxA+7oKijvOhZGy4uLfUuI2p32atktPGpBPRn2TTsxpPCBGDCI7c79GHCE4Gyv0YVVBepPS2uFque38qUmfw1tP05Ys88cwjTv1mGfBCAy8s8Iaw"
"criPhAn5eW6/nHX+vm3U1yMa5cVGtjOmW16l43Fhnpc1pALKs2jLfHzPF1AThQLAU98y3mKc50lhqWEAs4jjh/j9eJxUIyyYEEJoWiCFEnLsPgp2FF5TMQ"
"6gBsZYKw4AbepJxr+OizZYZ1fIiEYFdPXiE2wvH4KoAxCBlHDvjQ9wdu9t6IFiLUUjY3kvea9k0jtGgGnTcVBL7/JqBMwAln2/TfNq4yvGvJeZHwCWh+jj"
"CJgRTOMKxRTWRFhxyMI8ZYyLLMIXUngYAAI2985x8dYDUEO4fP4ITz//eAQgGo00attwPGCH9uwMZxcX2Ny7h83FBQbu0Awd9vs2fCIt9LFm7GdvvPEB3v"
"v69/Hxr3/u1QLvfeVbuH/+Hl48ep48xORZ9Nnr6Hc77C4vsX35ErurK/S7XfiazPhODHu1bYvufIN7b72B8zfu4/LJc2yfvxSrjjmFbJk5r/iN13HRUgz1"
"Gc8z3RvkveXtdn0it1sx9+j7Hui32Ie2ln1SeTkhhNw0GxFCHudoI+CO83yNPggidqLkwgpeOIcbx3nHHFLP3mJoywCQvZiX5MECpKyEqX9NLicUcS1S7j"
"f1KTtAvR5pARwDQDxb2gCvnKeWhi0F4zmAbwpBR0BuGuEsNtkjZcNYsPXy01QIEhhpccgwQ0hXhEbLSifLztbPkxUPYdwOCijHJfW9scCs9VUMpUpj6BBC"
"EiQ1eaK8HQG3aVpQ06UwEppo4dAI0Cn83CjQGFevsVt60RmExTAMW3z+u3+LqxefG0uC8fzxr7DbPsVXv/dHON98FWZjY7aY2QFGe0ERaHJPGGU6DvJ+Hz"
"3McRVm9DYHIX8t9sJ1xRRNz6xMpZzKlENIMrDVhI9VYwCjCUA5XhOa/PUTDGBuAAba8xYPvvIO0DR4+egp9ttdMCxymJbDAo+m26A7b9F0wH7/EttHz4Ky"
"HF3uGEZuQhi57Tbj726Dt979Gh598htcvnyu+N9sLvDg4k08/uS3Ify9x7Dfoe/7tFKWY5iVQki3a3F+7wzDpsX2xRX2+z6D2jCg3+6wff4SANCdbXD+4D"
"7Ozs/w/LNH6Pf7EkTDezmP8TlF7zQCcDJGWANsnIy9M+RHXUZPO4eQo4OtvdomzcFmoA1TAG1cFDWCMJhEaFWAo+tFDmlhT+rn2vqersstUaGjeeL5nSHP"
"cctTR2nttfioAzv4MXaDCF6EruY1inW3AkTzSliGWHYOIC8s0h6kL9LYEW0lWYDkIJT3HhjiZHefrN9yteYhHexUIQRhvcTQQJO9jAi2+m+bPWE0YBVOIB"
"WGUh54DO04rPPQo+fneP8//Ro+/Od/jTN6H5uzN5Snu989B5jQbR4gfhA7x6iD7HkYFxbsdykUO4ZhrwJY5sHuzluqPoGj6eQqIQBB3j46gJumCqbgcA0A"
"NKBpWzx4923sLq9w+fT5yKGY6+Txg56gpsH5g3O0Z904B7nbp4WUcX4UDWGgfbB4KX15Jv9u8ODNd7G9fCkEQrjX3cfl00d4Kby7CHDKCxzk37HvdJsOm/"
"MNGiJcPXsxznmyBsbtdo/tsxe4eOMBHrz7Np5+9BmG6L1azzSVNajyJC/jM6QtNXEB0vVCu6pRT91LKuSvERjHyjB+UWd/WXi1AKFpKCyUAfRCnRr/FV11"
"kgFxWnkV2Dkdz70RHq4nA17Ic0X4nFcEd9MZSHCcKo/dO17xeYI5Tmz3OezKg/Bq5KRKrPSrtMSmQgiDnhbpjUzUKJRnnYqJcrFCrWlaNE03ersx7AzGk8"
"9+ju3VkyovbbfB2YMLPPns5xhe/DXe++Yf4+LB+6CG8OLJh/j0t38BAHjvG3+MiwdfxdBvhXcZAXMn5i17PeBnQ7GVMP1dIDHHl+0vTh+l5gCe8bANoiEY"
"k+OZsJt7D9Dv9njx2ZPxIPrwObBxnjXMtxLhLIDVi8+fjBnFj36Hs2XHz5PlA/PzMXaxb4zXZ909nF88SOyf0xk2Q4f95TbPQcZOZ+YkU/hUgO32aoctXu"
"Lemw/Qdi22292YRgBlTP/i8yd446vvYHPvAvtHT0ez2c6XhsMmSAK6CufGMRHvD2GVcIgEvGp9emj3Qd0tCD3G6e5DWIfVV3KluUzvPC3CoS8wxfp3ed8R"
"TKMGEA0p89y56C2sO4I+wUJbYMmbHAYBlHIJtaaKM10hxqtT2rWyZzxeBtiN29v3CcCA3eVD7K4eT3JCbfBoB+DyxWf45Dd/jve+8cfoNvfx5PNfYHs5vv"
"/w47/C+b2vhdV6zqpYeeITgLlQrJbFHaQQcqT48WnEsG2wJ9O3Ncc9jfmbqE1a7JE8ryHse4xfLWjC4pMhAGU4XYj7fvQGA5DK89hYHM7AZsGbHGf3L95C"
"125AaHCBC/C+V8MlzzfmuUoPSHP9ofaUkp27FECZvc0wnTIw0orOgZX3qcK8EUQHmPvInu3JPNLbUeN2BvJw7DNvGKfoduiGC7qjQ/+2+O5SZmIVUvZApC"
"fKCVUzUMZQTwTHDJIx9KfnKjUdBH13yLk5BWn4nfJ2e+wuP8N++2w+v6YBNRt0m3fQtM+wu3qMT37zr3F+/2vo91t0ZxchdYv9/tLk4FjUr1DmJzeNIpgA"
"AkyH0eOMYVy5EKQJ86MB9K6ePMfZ/Qvce+sBXj55PuYVvdImg+ru+SWapsHFG/exv9phHxb5sPI+M6jmrUc6pA8ALbfgPYH7Hs0eYMqGl9wsn/7GMWlWzl"
"JD2Jxt0J1v0F9tsXt+mUK0CkCHMdR1780HaABcPXmeQ7rsh28TwA653BwGjsY0kseb47yvx4DmiXvTML4ALWk+yWtDdz6se1zZc2/F511aT5LmysRjZgw8"
"hLmzfPwTI3gxxaKeSMsGyRSTVL344tB8/Rm7q0ezIJreaceFUW13H5uLr2B7+Rn63QtsLx+i27yFt7/3HQDAi9/tbqF212+02qz0UXkJJR/nP9OCohjGpe"
"yNjmfQZoDst4ynv/sMD776Dt58/x1cPn2B/dUWwzCM3mjTjO8MAy4fP0V3doazBxfozh6AOR7dtw9H9A15AUNcfxCDAOm0qvH63/+Lvxx/9H1y6kKFkpDS"
"6TUhz7zQKOxppQbc73H1+Dn2223yEJMhPIxfOjm7OMP5m/fB/YCnv/sM/XYPQHiaav5Tg7BanavmbJHmSnOo+Sb62e0r6fkSK0hZC2DdWBWun/HrjvOnFr"
"DNqWPei2XCYu4SIUSDY0Ix15sjWwKipU17F8O7x7EUX9lfPcX+6ol+Rm2YzyvbpOnaUcf1A7rNG+Bhh93VI/TbZ2joHO3mfmiaHW5eAZ1unvQkrTqEuXki"
"Mf85rqbmFNrNIDsGZkaAYQ5gerXD0w8/w9kb93Dx9hugt9/A/vIK28st+t1+PGQghGh3/SV2l1douhbd2SZsgzkHmtEdjgcbpMMU1MrWLL6rF+OCo3QcZJ"
"RJXH4fFjA1YfsMhfwxDOh3PXbPX2K/3WHY9wno0mENRGg3G5xdnKG7OAf3Ay4fPcP22cvEnwZQqBBu4YVWFj5FkJfbZU47Vm9OzR+nfudnVE9Y2K3Q9Vh7"
"1ZWaKP9EMu/2u2c5dPuqydkGc6BY7hZdA0T7/Uvsrh4Vte3O3sB+9zwtzZdCaTebPI9HBGrH73Ey99htH4GHtwAA++1TNO24krBpztB0FyUDr0oADp3CRO"
"IhzPnHac8QdkQTvxUbFwCFOdC0QAiI+025GRcWXT55hu2zF+juneP8jfu4//ab4xdd9nvstzv02/F82aEf0PcD+qsdQC/DKUbjHlPq2vS7CVtn0rdTg5fq"
"xXnU2akhpMrDgH63G8+03fep7AR0gZqmCccVdiO4dx14YOxfXuH5Jw+xf3kVVunCB1ADmHaPaPZynTTKnT413SwCHT8neheqktc/sDBu5RTCSVg7/MHdoU"
"rFDuG847tUUZ5v2mk/5xV6pfEwg2s4Ynm7bI/d5UO1lwkAqNmg3TxAv78Ew558w2g2m7D4Y5yr7rdPQx6EYX+Z8ttdfhY8F0bb3cdZ+0H2ZO4oXatl4xwd"
"gqJvBGDx+GULEvOcPOgFQtRQAIv8NYuBB2yfvcT2+SWatkF7tsHm3jm6izOcvX2R5Dv0IqwbwXW3B7b77H2K/YFpHjXcd9b/ZWAyi1dIeKjdZjwvuGnDaU"
"rBg417R3fPL/Hi5RX67Q5DLyIc6uCEaXAsnlkv1K4iztU8oX49rf6asuMPZ/sI3ha/IvtAkLP9m7ao5XtjRcbOL/dCkvmbtzxS7o9qUaqRjiucuzAnejt4"
"0F0/i9NVJrXPyfDwJoBVzk3JPhX3ZcoetbxsmXK/fYyhvyrStN09NO3ZePhDX9at3XTp+5NEDTbn76Dp7mF/9XgMB2MMDW8u3lMe6c2B6Gnlfz0wDQqeaJ"
"y6kKtp5foXxri6N3qoTV6hS8R528qQFwsNw4Bhv8fuxcsxg4bQtATqWrSbNh1c320a0MUmnGLUJFDmEOq0858s/5O+joH8m8T+U7O8flw9vEe/63H1YjzZ"
"aNjtMezjynk44AkBkmJ1ec27tHOht+qFnj5fdq6PXw90xHRYlDVgwM8s3jTPNKguZ23RHG8y5vKiOAW44V6+lkfDxjcnAPjGaaFMjCFwaOvdMJAeWe8JWR"
"8ejPAKgNA5LC7iIdteB40dGCr9WIzoXPEYQXHAQmkB1vnt9y/9xUVEaDf3MB5V1bl1azad8i6a7gJNd4EhnGk7ruBs0Z29OZ62AsD/as4p6VXOXQsuhEIn"
"IIFqAsuhyR4qUTh6M5xxOj4Q1Yj9A+GEqviMIb3J8a9WNvHsXkoH5YcjBZs23I9nwObDt5MCiuG5CHDD+I1RdaB8vJYh1xTaQ56jdPacJjkNzjO1eMgBUO"
"ulWi/0VESZ1xvJ2vaba+XIus3ELoZ8dnFcxBl1zxDeNDrmlZDoNwZ9p7mSERXhzUpgVYAMoSNJAK48COMQHXJ7/SPSLQHpvId28DpfFfOvASGgVhWLkyiE"
"+a8t/7lyPY6ZTSrxrNZ5KC52ib/HTrPfPilCugDQNBs07TmAEQw9FpuuGxeVmABV251jf6XUP+TVzdLp4njk3jkg7zjXR6wOQuDxUKJkVLCUFAk5JSNcHF"
"ouPcKw0Aj2GkjbW5hIrPWy4TEyl1kJURFey99vzGkA92O70tMVQ0Z5QR4IOmDJNl08rB4m7WxLHtBuZPhOr5+6/1KFM6l3pDFtvUaG2slgn8c2qOWt2vN1"
"JumsWB1d0ZNKBKZveyCbzrmVK99J52zHzBzpoE7xSCQpqLsZ6256oGivbvwdDw3X33wzHiEMMHoWkynlON6X3Fv4PFmYmSV5BoZsIh526HcvnTwZ1JyFRU"
"Q8ArDnkbYthu0+bOkQ95uz8ZgyqyRvhTSonyqneGdJNRg8HvQdDwhBUHjSmBCRKrXHMx3dBwWaTHkAswBNlgOdkM9KNqBaAKkTqSgwsairHfUVoJTv2r9p"
"302+H50+iiAsgDgHa1iVkedtl7Uk83xfYCBFBfLNU3u60gjXoKi8RkSvUaZxBbqgy9Ncgi8gLa2njPzlW+LPRL6eB2uMUBeAY3oS5ZDLMacyNTfdYX3SUW"
"VsOlLyzhhQzwzoMft53pqCX17Hmy0my2HoLwEMNgGiV7HfvRifDHs3xEVtg93VFYZhKz5kTONh+k2blIPwy4OFf/c9U7dTMwNo5nPlAbvdc2CoHNUmQFMf"
"25c9yzhP6gJl/A1SgGlBdAQG41EWOvUQheNcshmHBvBY/M7AAONNchqycr9qHYxHvvNCFe09aG9BGI6L2lxqUc9783LRBrgNp+bw+JB+F6HUU+khXnxzpa"
"NIDwKvm0xKmxyjRs3tai+4DD+PfzuwPqZOhz6lFyhYkuGNxO0UuzOh0QW3v+jEwx7gXcWApbD/MC46GIp0TOM+0v3lC+y3L8YPS8uJfyB8g7MH91cYaEgd"
"JS9WgV7UUpZyZO0OA1Gl1CLwp5CZOJEHALr7SEcVVWmU2WTfit5HAMe8elZ4p/J3AFV91J8BTGHxJku2uJfFTG0rQoO2DUrtEFca875X90tQgALMsbpinK"
"dQrZCDlL/9zSb/gld5S/amKL8GaJe0G2Poe/UBh8y7BUgLijoEWwivZHS2Kit9EYnL3ylCIW+T+0Yc012fjoo7RWiUDry/UiQedjbmG5+k7yIy9xh2LzAq"
"BzOfhvGj3/3VFjpcFakZPaZhQL+/BIl9qOXkv5jHFQumvHm6hbVzfkqlN8DOL+WzmG2439R6+xTz/YuxuD8nTy3KPnih4nxcDajiGjntKB7pwUrRyZBufn"
"7+zpvYPn0B3u7rshUARmcbnL31AFefPk6fT0ut7oV4DXiquK/yQCW4mnet/OeEKd7LrA9AP99u4XtEsGE2bcQv1VNfxnDqSsfTkj6Sx0onPoVxAlpDFkcR"
"M3jYgd3GC59hYiB+LSd+to2Vkhq3W/RXAZCL74426Hd7pC+/q3ez4q224IJFUzK0l+qlgNLb2zZZ6pzgrvHu0qbhKnjaQ+btHCvDAm3+rabIGejuX4AHRn"
"95tbhK/eUVhnvnaO+dYfv4eW4/442WgCnDtbYf2DnPU89JpoIPSn99FlbdtNLNUbd2sFdNhPGYxvq3pTL4xTR+uK9pW+xeblVYPpZB1ODZhx9h3D5zgYPb"
"vdjb6OVgPFU+1Gu4oxS9+xjikYCZqk2VvzncK8EzGU0BnLt7Z9g+fqbDtAv4unr4BBdfeQtEhGG3E/zmnqKPHax4o166lVZaaRF1a5jj1dN43J8X1kXwbs"
"L5rDF6kIAV+R0CqGnR7y7HOT6TFxGh349zhe2mUtb1axIrZCtx2yK9QeIk9+StSuBxwTWkV27okOTS3T8fTxx68RLJAZ7mIP++6rF79gKbN+7h8tOtThHC"
"4spLTbZNbUVG/PFFarOVVrpZultHBH4picd9o5U5sXjIQ1praxYGyXQjkIYvdth2DeHgcHGSYNkhdfziUcUtV8f+1UA1ExGhu3+O/dPn4H48zvFQae2fvs"
"D5V95Ce95h/+LKBCzk9pFDwrRfxDZbaaWboS9RaHdyyeY13r+O/CjMHw7TaWI5IV30NsvVYyK8Z0K7eh/jKXhfaZKEl8ryG7+G2vvnwMDYP7+C2teq8sFk"
"92UGtk+eYfPGfexfXIL7UywcXOnLRdfRb9fVra8/dfSlCOEYBaVOIhLzeNWFiWJzb7p/mlWAzMO4LaVCFLejhAUg40l1YeGPeI2aBoQGw76Pm1pMQXm/BT"
"GZeq10u5Qbru1a7J48w7CP85u5P+oN/+aLHebYyf7FFdrNBpv7F9g9fXGy/rnSl4GEfjy5bpRpvrh0Qycb3Rb5ljez9/zIepqFF2XWcitDMTNZ+S3zn1s1"
"TWnFbjqswQsDUzvOvO3CthZn75zaCfU6N/udpkqfBEybjL+3j59UtpaU+XrJ5K2rh49ATQNGH6Zgp/YEf/GV20pATQfW+uPhWU/pRsDTgVl9fXH65A2Gdg"
"9ZMjH9LB8S4b33qhFB75GsrLvVvymuUSH3XF2/FLOYJf4Og2FUoBw8m3Kxj5w15XGzzB2Q3aumY8JZjsVe9Mvlcl0GogvzGnrwYA9nmKu3XhBVB97rym2l"
"eTomRJp7Ylr5cI3+eHoqy/a7vAOqaSbqEMC9Du4cTx0v3lS/lCpbHmrWz+xS+6lnYd/ia0E6TKLsuKk2IIRt6cJzVWdHhl/t+P2Bft+nY+t08aTe4y91aN"
"f00SnFwzyTh0d3Xa6mrmo4Vnivhoq9RVR3vf53jRydyYf3x7rR9Lq0h98vOdWjRrYPGmejKpfT0RGh3QmP8NYtodfbAs6n3k7UgweA+/FfBL+46EgcPUcE"
"7F5eYtht9bpPFW+ZmfP4wlClj95aRON1Fm6F9yk9IbdhJbKnTphnXzqa0ps3qTNf5764tB4T+mwmzHyqPkrnD77GXiOX4+aL0iB3iRhDH470qxA1LZruPA"
"Vi1Rmw8j0idOfn6Lfb8TuUQAaOsJ9w2I3HQTbthXn/dVJsWgHx1POV7jA5qwle2wVSIr50J6efVjqe7H788h5A6JbO0a10E0Qg6sKBDLUkDajrxtW78cxX"
"wA3vDsMwpgUyyKQj3wZgP7qw1FJ2zuIzY07RbPgu3D+avG0elblxL/1KrzlxeTVzni9Ner1l+uvyJBgTlytIfrnI9NPKVGR3K1/QWsknwvjFDzTgfq8OHh"
"dJ0HTNuL0lnm3biL/Ih8mnA98HHoE0/mUGDzR+M4YY1DYJbDGYL3rUvuzBqM9DwFNy+uVZz9Hph6+TT7LSTdGhxpS3br7ekyaBce2TKy2k7na+RblSSYTx"
"rPfx5CJq27zqchjU7hpqmhH82gZN2+Z/XYO2acO3RzEeMzf0GPYDhr5P/9APKS8wgCa6sBh5GPEWFA8qH4B5pcWVK5pMNyWOlVa6Pp2wX659cqWF1L1qBr"
"7UpGKphKbbgLGRt4LHChGG5fw9zoHBxEDwZNO9+Dx5mdBhM7s4jmX+p6zUSivdFVr75Uo3R91rfR7Da002ZEXAQOks3RjGZWag78ewb08YZGg3fcZM5ChA"
"lAcJqDzuNWXkEDKzSj+C6hd9Re9KK6200mlp9UhfJUXgorCZ2h5IHxYXcbhO378sDkSXeTqrWoNHSk2I5Pf5A9rJy43pVlpppVdMcwNxjTnfNVqB9MZpAT"
"o5QBbBM5E85aN22qDewZxzUvu9i0KKMvwC1sG70krLiev31J9jVwGXK5j94Xs7BxJ82elLuNjokNDl1MpSXvoWjnb1eNGtk4rGp6mD4+Y2Nsfb68Bd6TWi"
"uROt2FwvuDoxg/U7i4qd+1yJF/GiiaRf7vF9B7a/zJ2YwjOpDjhecFFdry+QL16XWuJVL5DCwlNEys35E3mutJJL5RGl8wYu+7frNwt6fXroQj236Fxc7/"
"b8+E1bk06+R/32KYR2faH682YHbIvghenuFL1eDfhaUdVk9jbn1zI5LBRd4vHavnebvA3w5X11b2Gk6Hq09ptZmm2Hir++ZKy7t2bC1jQRQTsxdcOwv34u"
"K610a1Q7R7iSes6ipqnAtXiyAvIM1QCwfKaiTLcCgiu9njQ3t8Wzr08cvqp+zn/1yKQwydbFRit9CUmDMXv37RtToeviZwWQ7ywY16ZAnDO4q6+tILjS60"
"SH6wCVwiRbgXSllY4invjJs69k8lZmO8/UK7Vl29MLZHx8XAFwpZWuSyuQrrTSK6USkGet4xX7VlrpTlF3Z6JLK6200korrfQaUvOqGVhppZVWWmml15lW"
"IF1ppZVWWmmla9AKpCuttNJKK610DeruzhL8lVZaaaWVVnr96A4cEbjSSiuttNJKry9161r6lVZaaaWVVjqe1jnSlVZaaaWVVroGrUC60korrbTSSteg9U"
"CGlVZaaaWVVroGrat2V1pppZVWWukatIZ2V1pppZVWWukatALpSiuttNJKK12D1u0vK6200korrXQN6lYYXWmllVZaaaXjaV1stNJKK6200krXoPWIwJVW"
"WmmllVa6Bq2LjVZaaaWVVlrpGrQeyLDSSiuttNJK16B11e5KK6200korXYP+f+6/f3kmbeWkAAAAAElFTkSuQmCC"
)
_RIFT_WARM_B64 = (
"iVBORw0KGgoAAAANSUhEUgAAAdIAAACWCAYAAABq3gn4AAAAIGNIUk0AAHomAACAhAAA+gAAAIDoAAB1MAAA6mAAADqYAAAXcJy6UTwAAAAGYktHRAD/AP"
"8A/6C9p5MAAAAJcEhZcwAAD2AAAA9gAXp4RY0AAAAHdElNRQfqBhsRGxGtMZVVAACAAElEQVR42u29W68kSY4mRnpEXirr1pfpmR6MRruCFoIEAQtBgKAn"
"Afr/T4IetIIAaWexc+mdvlV1VWVl5olw6sGN5EcazS9x4mRmdaejKk+4uxmNRjPjR9Iuzq8+/xuhT9ena/U60kV4kIfh9727HD+OJheP5Fj6zevPYZTdUu"
"/BJYdf7GVO0v09L0ndmNfTlvx9uv4crzPzpwb+dG1dt/QRvvHdfXiMd3K8TNZ8j6nnE1f5J3zx4Rf9JY8l8Fiu+UDaT9ef9XXmTw3+6crXyKncSv/E7Lx/"
"Cp/Gxsd8HW2d3c5ulXB3YUcHzx4eeE+iT9cHvM5M04fm4dP1sV1Hx+eNYdBtp+8GT3Lr+oSNP73rTnix2wvmHd3y9tKOMyxbCT5dH/o606fQ7k/u+rNqMV"
"579D5qequW5ke8XePmfXsZP4He9AFYXJ39fN/8PKK8Tz7r+7k+eaQf+fXRqDnuf9qyDhutd/YgD5HarzL2A/Vj1dBxWXw07b153X9Rz5a0+YY8Nxd2hI8V"
"EUj34wmvYs3dXWX26RpenzzSj/y69+LRx1yjdbfC+u7D9aV7zvW7OfCBx8ZHPTQbc3fUykfWwD666PckW+5+3In/A/WS9Hp9jfud5nj/wq5Pi41+YtddW2"
"tl28fWeovDay8+dF3fU7mP5fknF9jdmk98Qj4fw/uI57VNWh3EPLLiaz6M3EWo2Rw8Ap9MYwl/Atl8fQrtPuH12eefERHRjz/8eDONJwWTlfHAG/e7CxiU"
"MaZ3ZIvBkYplO/w2yzuzc0s48p7pP4aLN558dGpXKg6543XkTa55mVVZW++qJNOO7MevbYb39b+fYi992uv8SSj3uWqLmZtdx67HV+IqvEXwsdfKFOa9ih"
"vv5nzKjfLru0jL56U23BtIR7t974zrLSrxpzI25eAUxNrMHfeP9E1z4aRy1454xjzOfM8tNUxxYdKeo0r2sv6ouWG+s68p+ecjD0j5CV5n5k8e6fGr7yRV"
"B3/z+s3yTmM4nBIXt0O4uUfP54PW9FpZfJTErQtvHrOAaTvvPgB+fF3WLZiPZMOuXod14D0miIoAJIIQM/3sF18REdE3f/iWBlh6exVvrMDWsHxsSHrUPR"
"aoioCVgbtkqrNTGMbZXu7U6uBAczotWDJfr6Q+9V8KnH462eima2xjP1bFHs7Pux7dSDINrpsqGdG3D6DdUMHdKT9M3/7Jr9W92wS4HHg6otogg5mmaVHO"
"E08DH7gKvNc9bnvRzbGaZWN4r31UJknDrfO2g/dcSavnSwrx7DEind4gcgMg+uu/+xsiIfrNP/9rA9OqRZDDoxMlH+91fv+bon4a16pHuPPkuN7Sk60sjw"
"6A3qM1N4OVB/o7uyAezeWtdXvV5qpf75ir/jQaHn9trXo+DmALknz7+z9ZAft84HvNCULFsH5cKwUOSFhFRZTQAPhv9LYf6wXL7rzVDPNEzBPovXHbd6a6"
"1HR/SteZPubQ7tNOqwHpg5bQofUwOKhg+B9ehOMJVtNsVOVoAOI+wCwda9szi5XNH4NRfWoZzlVX+e7erbpJ7g9pYd9ztm+LvlROymreo3YpJtwFopzz3F"
"bLimYNizvm5S29E9rTI/ea4pvxgTX3txo77O+lXDjosDhfhX7zj/9KRETzdQHW3SdVDwyOj8U//eLLz4mI6Pvvfhim+ai3v5yfn4mI6PJwuRvNNTtpr+7b"
"XhS0dIpRmKUsK8SC1oYXdjjp06yOZyYOQbGx7Sg6sKSfh7k1/uxwduth8Ftqp2bszeu3RLSEBJ/q4gNPP87rMbwmREikdody76g5ayeHd0DVMd49174lRF"
"uz9mPzkYrVxtuMBRUztn1oi3KOLFVyma/SQuYw3oNiWluUNhjNsmaovB+o1XVEa1j5Uc6RChGdz2f6H/79f0dERP/h//x/6HK5PIFaKmCORypcQq51MN23"
"CpA75bPDrJFeYx1rQu50XjW4beDwvZeSbEmFbkw5XrS1tR9wXbke4eRxA5s33zzdp7lk99snnAFemUkYte2Id96gEWltS35bMjx+CYOM0/shcLLXzrFopY"
"yVomv6G7GgHZEtKZ7lFbxHx31Jc8zFJr17XD80T3QNKz/K0C7LsrjgNJ2tAoti3zvncWwV5BbVnGpX8+1YBHS8G0hrTB+VqExGoLhCLtAYe4qPHQ7bebdD"
"2scs0hUHaWcdbhmktyvkx9J/2mu93KfxC3gVDPZzt8740Y07Wl8ca8yDsCmCzaATbk9sbNSOI0/+u2eqkyevkCxAMadKM51pQVO/sFDgqfun67plrM/imw"
"8dBuZf/fzff2gehtf52WNDuyN7M8+3DTZ47pkL3RiHt07870m/ZqWvZRh3v8cAgeyq1+3+zPZCrccU+JQQ9fHFfG6/PpSyCPsTqyWoa7zdMF0sO43KdX4f"
"n6bnq2ej9tCziR0T32u1cqVCNs+gOFD40BDZ8MTf9/VRn7VrAMqV1bJ/9mI0f9X7mFw22KHowhp4WicWKF1S3pFfKd1AwfupBMUtcJtC6mE8et2+GD/lx4"
"PIdhxAdqc8XuYawduXfRydI/uQ11Pyc9smqMd7x+Nd4HortDWbuvUw+2tlKDTnVe9yBcP3xGXQJej8ws6Y9tRHVy5U9ozIqK2a1mJ9DEsQZVTCNiN1/3z/"
"3urHfbIRrw2f496TRx32ejdc9IUeWXeHmqwT42R8VbMVP1b6LLyVZ/Qk1a2n0QNr2SKDMPZ6WGb92oaimnp8sr2P4Pbev9ucuAPVD3c95cwwJ5U/pLeF5k"
"V4M2QOKCI4+gbF7DOEdu3a2FrNrDqBmX75N78gEqbf/evv69ObWgZJxHkwDvbMbKP3yv2LVS9/WJ8yJDwaqxzabyCetGNohAsS6hPSyMj9ug/MfpSLjfZc"
"fPgdgsLA3pTYQNhhrf1Wwb09zxYWmJ6PnnljIt5J6NB8ZGceq5SYnr94TkRE796+20Xf3/fDSHgo/YLiSOmNIXrvbO5den1n3d/pep/xqTsyL8ObtcJrHy"
"t4VLxfJKW3mQZQPQ0i4xGa949W+bnMkof/kGdmoomWfZgTj1crRF+5Mj6yLxoL2u38Qd8+fIB+YWPjymOg7H8HvEhqrDXTOcgmf30j5fdY3c6Y8VaV//qX"
"//NHM0d6ZExv94GBN7WyFwoDHBVYVRZb93zgnd1UpwOh0X3+6FpQq1ZoL16+oP/xf/rviYjo//o//m96++ZtkNa+MOiW77hjIX4akXuNiZ3s3S39EZp7Z+"
"E+mgF6h2vPittjdd87ZylUHT6zT7YyeJoiI2teHG9QhEhTBVyb847DWMzOtempgLVzeo4Aq+x9Lr0+GM4Hr8pnVE9QHANr4jHe6pk+wNdf9oPDekxnGMZb"
"8Xo2AZj7nJVlV2XOyr0EN0ldu0y/b2aKC21U0dgTji0lK0ITMZ1gH9Vygsm6JHdHC0wPgQZZmcMZEd+zPWc/IO5eL/no69aR9/EC63EpTyvvjtZ9bYsLN2"
"9vjUIApGFcVBI0eXrh/Hw8V1cZoEZ3ZIxLTY8rGlaYf0dmLXQqnT5Y8XZHUczBTEqFW+E52iIw8bo1RUZSmwn54wbuqcpwXWkia3eyc7Tx3/zyf3nv43II"
"GKvAUN+7hw6OvZkt22FYfZlBezyNypCnot17V5vL+Df0z6YMNt4dD4NHQbx88YKEiN6+fXuTgVLKaLfn3gdj9qQOZe24PgZP9ad47VttIDvT53z35ueYES"
"IrL1bpZCzOc4ZdeDNmFAP8wkvbqMDaBNI+A8XLLb3VzTA7r8zvohffC0y6tGOmo7eqsmFiJvrbv/s1ERH9yz/+JtseJTlMIvhE1vPh9Z7nSGsBroUHSxAZ"
"xP54kIj7vhr40blAfRazF+q7429sa+ZZoD3SLi3SQUuuhZVvAlx74fCvc6MnnoZGwXaZCIQjFGXKs0DOyoaVWoWHH+E512m5DbFH2p5/Tii8B1GoUvCjqM"
"vIB9iKje54xRvvZW0kxxd7Qpalp9gRBg0i2v/1eZz/IwLdEEQp/biUmA/zVgBZmatjb3VD5p0CQ12cgdo9kqX2/n4t/F95j8uKYCae9D7OjYUVxa0h8ngO"
"JaOel2zKJ33z61/9r+/VI10JTKx4Xn2L8/DVut/elVHFF6jv8OV9NneA3xj+HK4lcyaSk7bLw+Pt9DzSV4M8fUBqpfz0YJeRcPB5/V521T3HlHbhF4/75b"
"qSfdwwegps/fLrL4iI6Ltvv9+d5161qLrdYxXNYW9zR4Gy8xk+36pLSbNbfVTl8aWpe2R1iAcpTc2+7NJMzBGFtRjchhy0FNkpezVCCndVRlDbANRCu5tG"
"V67PHulHOZ3ljl9/2RLvCEjGIc9edbFsA3AHYNDzx2A9hYeVSuCqlSsemNfe9k9kx7xs9U7W01ZO9Z7WnlZp7fEXBp5lmWfHQqMiZ2bjUb248BCOhsh3Ty"
"M8hrUDlxDR1HJPKwB3r/Iy7SPy27ugiFcyVMbfPnW4j59MP0/ZyOCdPRs51GEMw4piHqf136jnuPd8U0UX8OolhX5wzf96HIirh0iHGdL6yzgVt/DGSQCL"
"F1nMDTOqogT+Qs3W5o6/cRzEn+i2rD1jReHojqHd6B6PNi5seySFoAP5qaAB4YAEvmMvj7uRoWLnPCywsx8EpeyYrsvDh0G1V7Sj3WkuSXnXFhyNZ1OYKl"
"UA70aR2WJVwSAK35W2VddNMmPyh6+jIfH7XnU8ILbaPuj5/k+LJzrxGuX3U6tcw0elK9RCB6JS9flIfUQ/RU3hWS0noT5s2o+HAb+8boCE+btOX2kaNkJx"
"l2mlRDBjhI9q8U6ci+zrVGqKAaBHwAawsmghJ0EiFQxRSw/QnZFRzDJz3baZz+VxbX6ODKQzPeLinXdrinJp27orZYXYH+dXKZwBgJfuWb+ApfIlsQHGSl"
"rqxxQHTVXLvbZgZWVGXDviCY/r2PM1opGe8b60/nw847q77EEb7KG19W532h1E9pfzmAD5OMVNYVU+QGNzEcyGx75rEc2Bd7z+XoqoRkV3y88YT+7EzOOe"
"3n+NaWNJRwCfUD4vC27sNtWv47X4QHrWIOrBVp7yqP5rXiDyZDErrsE4RJ4wFJyU1ZBGqgtKtzJ+NFVnZwSPOkpGr8PbX4bR+4QBm14G46KeuvG5Iqz8cv"
"R7V8OdIdQ69lhHfGNHHXtYnKxQKeljlUfeqHcgDdnkL0CMvMnoTe/37OKcLspyi8ZwqT6t1S9yXE8D5JGyp16PAORtId1E4yiIP9WChW059O1wiBfezne0"
"bmEhyWa68bsx0K3Qk76n6tsR0OHQzxpnZIBL0hvBA02/3XMbvKcEDpVDGoDMx5kDvwSiuhATHcRuaVMSwsB5tbSV1469rwhsub4s0ncyHgi731qjxBI8tn"
"RllAHoOg9C570aZZeC5j2KQ+p0KYTiFc2V7UGEu/xROL74pxLvhpLhHWmKB2sfke4Bhvv07PMeZeOHMrwLbnlNteEwUFaIZaU3nZ5J/YK7Lo90PMLQySVr"
"jI054a06lI82+v+9vNlb0t8z92PL2Lufbph/a3JqGgGiDOjwjtQUtaBAeFQG6akGoeDJGMnsVrQ0nXUpQYlj6kr5F+tqdhldEeycbgdMOI4CsKawMPvzGE"
"Hr/TIpFExlZKShnEC7b4ORp6rpdUGRk0mIi9N+nZzYw8IDb0kkyz5D+PJ2GNrtFe66A997e9EzEQZwgKSR/86+Ch6YpYfFOQ7OPGxELpq0BzqcZ41pe0rj"
"+UV7XnjBVZgGLR/P66lHPhaXd1XK/rV6u0cMoj2Gx8iQ6g41G7v1Nf3k4uZ6rain4VXWj/el3UuzY2uQcDVf7bA/+VXFCG7On5qk9Djazdoc8N5Qa8W2Kl"
"junlNEoCFN9D/8TVzHUWnBVi/uaWZPtB/Ni14LZmgn2JQveVEVPzIYzAz0ow3rAy0fQOQakQm34UTNhzOjUUYjwGex6tcyZe0PMaO3Um1Bm9wLVz8Gsdvj"
"wr0WELLq+vO+lZX1m9geQsQTNEbcj5kB0F9xpJG5l1hWfUCzn96R+Y7NxiUFDWv2wwC/jrIWaIoduQNPqXhOssHnRSyh9LpzmsLW8Xype5Vf1Cno07gPbK"
"WrgHyPOh7h7C4+dpyFvMkHyvkGj5UPJVwbYaO8vao+fB7qHa69RQ43BhQ2T+ylnICu12pH5k5lkEiCzqmS9AMyHg/YVDAD7HQh1AF/hYER5gKlcgPI+o7l"
"SZUr83T3qqngSVK9mQ4LJzpgZuh3o7swda9ZmfoWLT1VvZX4bhzK9bQ5D+UyMK81RhRATh/L5UXuUnz9ZUvRZevfujxP9N/8u39DTET/8f/9h8UlLr4dFA"
"Exv5zCM2202H25FAoD1cxb7+30Ftzys5lAySjpc8aWScPe30vf2C6vvh3HwIT1TqHd3KnEu23nlW94XHsBtMzD23mq9LvK4RU6O/ncc3V1scJ7A20UqTxy"
"RcPrcM7+0Qay3YK1fdxCNtLXHGavbi9FB5sJno3XlXehVqnfdQo7NaxQrUkieRxpcQRLiqRlL6kPGRY8JhTL41wZF9P/LQgbq03l9hHCturfVf1SkizZ2g"
"IONmi8CHqrEkuzItjp5nFVeaDmpPVquuBd9bV0dLcM9YXdCdIPQMz44JVVu8OB2SsV/W3fmlNrSZAJr9ryrEKr5TlXr7ifR1wqv2b3FHSo2lqS6rQBOCmg"
"MU47AM+qCE5DzWuknn321l1mE+bh/nfFv5WJUfRhnft5hrUQ27pnurl+s/O4NwFeVt5VMjgC4pv9qZDHCE1K2exAwD3ERGhrS/gtYL8d8txO6r0nGZqqSE"
"uPb/k3G+6odPv6dAd99h5fCaygbLmnhB5i9mb6kRHzx5pBTTnVDqIgWRrKV+gp/Q8r1xe1olZxftDp6LwtlE8eOy0DhqWZDL5DzRnaFturm4c1WdRtWhqs"
"XPCa3nlcWB2agVE1HBRNcoLtlFxtKIv/9m/+N1mjt/WcwUtiNDGgsTZ5hubu04/Bk1PeMd8FeK6cJtRZLQBO3SCWOp39LQEq8j+qc+wY0SFdA/9aFlznG7"
"ZF5GHYdhXQWYhLQDvBhMeGvNfachUMOPc4NLSkptnJrbCcaefFfb+r0SVb5XvBdOWSTOH+sd440mSUwAUhg3SjbIU3NaIwqmunDWQvjcGzhtxDnmS0lK6i"
"3VdOvdnKJN/6Ckz3WwZpM/8JHCoNKjt+j3gtZcWDdLInLYSw7V8e0+GKzzRNAE0RylypW3VEoKbfnCON5GGuUs2Bpgh0ctjTZRsFagmmBEuiG/9x5RYUHn"
"ve7hShpAI5lw8W2YrnyRv31phoSICF1fPisoy7VfMZwX3de9m0XEU5lbfGhez6ttE9XesAwjvKDK05cGFLsKwdlj49ymDNMAgWhnr363z0EqreDuJygxwj"
"WapcNlIQjr/tNLHcx8Dpet4d7jYxhD1HiRMIFmSHgJfcihiObf/ynnzZb8x1qGGm4teVM3fP4ph1TV6lJaLuI9Y5HNwBFtoPKVzqvxf9i/ejYEr3W6A3c2"
"w591a5O/4v0DJPfIk8Vuf4rnmLy3vcsmnUBpKJC70WNTR5haxDpHZmN25sGq4lF4+lm/48jxVcDsGpoo2dDnW/q6w8SLRgCTkrbyc3a7+/EWgMlD7DjLnl"
"l2q4pZyd5ymh30ecS2DY8dJ131DDkcebr/ozvgc8uFC9HOt3g2fVq+W86i6qllK9Z+9MBnx2nXzE+6i+NbjUHieP0w2uNeqbWMp70qD3dpCLYLmvqdgbL1"
"m93c5eNUJRt73eYvcug1iXqTYmAuCkBx2IggIwzSOZbwDFQVWlo69ghJGbvi5VKDnUqwxXxs1KWGb2PiO9Go66sCn6UE2bCREtX6rD8LtHGHpQ57aTY9B2"
"lSHAMaS7/JlaKDmGjNEwV8B2wBXXfUy2Fsc/u0YkMqVT8siM3tiWy6EWNkdaO/aoZMeWf+dZWegSRYH5oyLiDszXysrw7pXUhyG/RDr9Rv++LqO02bOuDQ"
"H8NQhZM1UlUA/yOX303DiV0VctDxteB6cBoPdeXAFIFd3O+ChodxaFhDy1cVFfnYfM/U4/EEudf4+jtfacKfVuLbBPrQEV5qawjy69tYEttHlMz476d694"
"X/7+8HDuMtRhYdTMOzxo9IIEKYPLULR4SROVMffpJKdB7dadWNSbzpleL5EkvwTuPcDEU3IRXDQdjvPOWKBIr/Y8YxtMrKAiXZmShObdN59WZKZC1xCWB7"
"cuRtc3MqsM4EIuWfFI0blpW3O6uWnglXkCes3zhHpYcnZW9HzgMxds9J8V680eTv9GJaQeLARNBAHU440IDPqgAqb8rAREBTvp88UOk4SMw2FV+TcLpjMa"
"ekOkK38lZBq91SIMDaO9lw3k6foPhuL7fC6LwnDRQZHfrdYjyYL7POX78G7sngWwH4T1+/usvKkM6Y99JjTQ1pPkum1RD32Dxziac7/48jMiInr7/Y9Lze"
"b1GgwJ4rCWAyRS/pXgenufkSoLbPy1E1NYnXtU5W0rLdNmyHL+bgA0ywNvcy9WyvS6SjaAfKYpURnXos5zrT51VAFh78QO5mpDOikPpMj5VL/+7d/9moiF"
"/vkf/wsJ7BGNIVDQq+jZg1cqTS+re+V8iem3GNKOHndY3QvhcMamEuRNV9wCVTU60dNF3iGdElIsEYI+3GMwEROdsyqKijfbKp4z9WXfu4NLu6BROBAuFi"
"E1SeicKdYp8JX9IdD8ON/addTgKcSdY8HLrQRlz8EQ6PKi98mdnHIbRDApPF3q81cATSbz/CyXldu4qgMAfgJBLvKOPdcafHqPcaRCc5mF2il1tpTvGKiM"
"gtI1AI6vZDMeydql1e8nympa72enlv7ETWHwfKDkggkZl11WTNOvVFj20EHHYkCgAz6GskfBBlPELVGxpLoPtWL+/JCpmscUabRZkgwTqHF6jt60DNLJYO"
"RPUDbyk8rHuji/qMeznwVeWJPh4pFOTec23jVfAK6efzTI1esMYXImYmnfN5YY02Dxlb8B+FGnKd60sC31TUxMU2ht9PRVSqrJojS4ld1GXjMiMC1+xYaI"
"iP+rX//vYbemeiA86KhxTk2lmb44EkDUue4WIGUF1CTDoaSkUNFtDJ5nBpnKTQC+wGTLezpD6UMwWAk/M9KQMv/a7/JdaVRorL93gOrTnNbp94t3CqOj9O"
"Z65jrAXQGbah64ViN9p6xlk42bHkJrfiS+l55+Jb/RqjUePoxuxoKHgwFX0PIQm24IPxgSzjWej+XP3lYSNyVfYCSJntaedyugL5BAxi07fC4b6UvP0N5F"
"q0RWaNqoEBnSrMPTCOZSvE+h22SM9DxxKLR7305VWls97ACT+eld/gBGOKdZCEuol5eDakzYpeV4E+lIoIk0iKiY446rfUUcRo0Sw2fU2AAMnN2gbMLxxZ"
"YmAKp5BAi2mHLkyThD3fwp0Msbhxjy9mBblVF7Q50HyDlPBZqw+tYyZdAcAUn/kblSJh0fKBNsmUSFRzxjjt6AyHnCfQEWNYhJolmV3bdLT6/cEOBtZDKo"
"5QeCaL8Kt4pH+cAb38KY5LlnGosV3AWQYj2ZF09DulSr5RITySw0BNIN3u31se9WjMkXcVNZyXwYRBvpISiqFuWazhCoBl5xnnervcwMJMX8pEQKqIAFy1"
"Jl3Sl4SWVo0tifBLxv9XbXARV4MG818ihQTPzsWIqi6FGGkr8XA56dtYMbO8KuhwSW8WKo1nhRlxLKwuYWqK/odImWK5HnJdQs1l84ldGZFxpJZSbBedSW"
"8xwYCSKK/y7KW7TFoncII7K34NHSBs/V2hxL9TJdWYJrAL6zec6QY3gwPXiuEWQ41K8Gu9htqvI4pQplM77X3jHBs1xuf88rNDHLL/7q58RE9Iff/TFwU4"
"Myr9KsNmVwOimj8nDrD9FRRyvIt6xvIdWRV9s9d0Ti/lG7xugR+vCaN4p6jKGnd+WuIDYtioF5WrRH8eH6MlezpoTmoUe8hcjmzQ7SHfVztw6EcOWvCpy3"
"Uheen6udXHYZPUAQKQBWqnTBIFC1y0Xe2ptDfcqgmPtzZtMcIVRguXetE4wADYUCUCx/8TQeWBUrrSzJ6eGvMV1tLYntmwEVe6wwTnblHpQGqmD+VhKc9q"
"ryy/x6RGayh2KOAReGJS/VEuU4Ip5zOi22s6iuZljtCzVmNnnqi3P5nc5OlSVY7bRT3jeaqsFcPIuwHaHUDAAvJnigCbRGC2AEuiX3eRn+6b0p5EzFOBXg"
"2XWTZCD0tTMaA4/Kht0IOALv/v7Ufk08URU6r7y/GjzVUouHsZWrd7lXh2v7O6OBVL3nju/YBkWmVGa5E7Qqj8ckQxrTFAPFEOQ5sDCobke9pqn1j71hWl"
"WyurRy95XV3AB5Vp/UV+mhVhafAgPIti+j1a9Cx9EpTioK6ciUZXQepsBvILC6JzP8Bi+QTG935Xm5bQ6PpfOWu/IY8orP/VXdhYm6RTUGrMhDstvQ+7QT"
"fRKQKT2ry9gOM5BEAESRMMN7BVV1StXhCUiuVGP5hPiiXitWEv+A5YLuEc6SMvRRX/ujZFzHoKFy9l4TbYvF4IWhZvOgGC9QYGDoVqhcPFwb7bkUonNKDU"
"DFGjPzFtLlhsEiSBuDQj4rr1SiETz991QqRG+GWBZ1tLwO460vEXRH7ypw1N9//P03xLwsWx9tE1rfl5vlUJgtK/UMfOrzAZhzelEfmgH3O+Yho8QTqAbj"
"JgJ9Bdw1AmZDsuCluaZroFnXL47ArUuPpZ63PocyuMaOb3xw2Csd1ja+kdwhqvyqVDvvs+9x4TxXLmgpvRW+f/aLr4iI6Js//KkM4wZ9XhgAvo0FR5aU5U"
"ZQZbOhFHzLfaSqcs1gYBIIQ4ZtKsBz8EStrzdedc+pzteKn92LvCjYxdYUM3S2VpwjkGY7Z+HXP2lmcgAes/DCWBFy9EFDQMdUaLA42P1MYCbcR6syxAhI"
"J9dGSLgdWs/wSkHGgEvNGJSkNUSf196zHmecuw6FtAyNGoE6ApUp9g4c1wBUTEgRSFK+QEuBcQ3oGu1CYWaaPl/cyyjQzMq8qGd8hnRrGmj5lfwXMsl18P"
"fLiKr0X5ZpD6BVeLYPr8cyQ8AIwA/va36Nal2RzFFX+WjdtseRna5g+znF+hfk6+fcVgrOdabKsBAimjT2NbxwBFbWeV2fcFWeWnabiDqwiILgmmyhgENk"
"VcPAyYP1dKgs1j455iygHDDJqSnOE34bFZgRzM0JGAKt9DkxTCt9CqPBQEcX+qQyFxrciXbx5DiV0fIi3SRyG0kwhZZB3DxQBPBU52peuAoMBKNEsBuJGS"
"KdXNnrq/O6C/Cz0XD5Ol75lhtvvwWkxRhRzDGPGFo7dDlOxgS7fIl4Ce2islPCHAoiE7IWE5V29HCYGfL3vbn3kJx56HtGk10yCSCRLbH4eg82ewDUPYp1"
"AHXvoUqD5cV8leKPq521s5gBAjLmQTmVceF0Ek/DfNjdVuSUFpFFeazlq94VUMYEB2gwPONAqMtZGCG1vEd5MPLSKqRmuLa1v/S6B/MVSE9rvSNGJewXE/"
"E0kdCOrSwGurKEH0aVK54LEdEM86rA+9D75OJd0eBrod0MTFLRyTQAPbIHu8vbzHxLjGv5++Xf7779jkSIJtzHyLkMzsUaf5KfV+Bl/EcQ4O49ysr5ySDe"
"GTkKqvpakvwovVc+cXyxp8NgZix/4dpAF9I66Gkde/0iRGn+sf+GDnrEBnDqgStQiu8PVQ68jXUbCzhnvMgktwsrdoDMnR+J7YeeeePDTzZCxQgbc3QHTa"
"W0scqmXMFa6tO18GYr0POkMFzwlibj3rnijOsUw3UO1lvzjDa0OHrPmXdd1ZxBOtSOY5mZx14aKCceeJRoeKws0AFw56oc5KtTB1OXts8n8JvL9BV4U6Ax"
"kEEpD6A5smyob9e9X6WJv+Fu0raI/QkZ3FMmFwDXmyqRBjORTDB/6GKPZTDZ6sExaNbAyHl+MciSBxR419TtGpBVC5fX+MweWhJHxDe3bwrO4UFhEJjSTa"
"G7zFcVkMD7eGxdI9nGjYEZpfoAPxF4G6h0q11jGR6ClQgAnNJgXvHxjEftVKFXPQUID7nxVa1LzdWAUIAKowbCq5n/sOAnC0PrQX2bhDS8HBgT4lQS20g/"
"5yncj38P+XI31RG/u+qHCyl4Guutzc4Th/xhf2Ww1JOnli15iwywdwRMGUEL8obIFAJFv9hHs0UAjdJFbw6BL4OsegYMT/oZKi4ANP0L4GnVgr4RpWBVi3"
"LL72lQ90QvRxNCOw68eyyDujwu5Ay+XOXcCZwlOHdlprRB4RaHzVfglW5GvFX30TCROg1KAfZQx07pKfM88BIa94Sv/uoLYiL64fffW51YveF2nZ6fiIjo"
"+u7qPILHtLZStnslBHNC8LD8jU8lzVFLmZrTapbK0yrz6TOJz3AglVwm8Cs9YiBYfWNFN/9TZL29SwDaN2cnbKyLKXcon6B5BcqPWALbO4CnYMSKe2q68A"
"WFYiBEcLgBMBhAh3F1LgcPUGslcL6AiATDCAEveNI4BjtrKKBmrGN+jz2ChX79678mIqLf/MtvAXg8jxo2+cxdla8FnJR3lHM6KklP5POv9EA1IOk5xlp8"
"wlX/YlgLLQl1eZeDPbCHScodwRElV+oAIxXTBMAtwEfv47aYsikKcEgBFgPYxAOkVw/CQpFhUBUgFZ2asUdmXb9OG0PGWM4IOPFrM0zQnBQPo+9Y7/njTL"
"duj9H85XDfJ2dVOT5Jq+MNRionyl3/WKlbdR9ki10XFQRLTUPEvNKu/7c/U/pbGQd/9e9+RUREv/0PvwEgXf7OYEiNQNAuaQowl5EXX0D6NblUvrDA2Mse"
"ACoobhqtw6EOEEHXgHK12nJuFlbRJ2ACeuGBGIhAszmdbIxlgOHEDyjvDLpkCrlpWe6J5vNyLYypL8kDraEPCgKyGwaLN+xn0XZ9ltnnBgX0Pg4+kLvxYl"
"tJJBoCVYcJoJoMA5A33jfWwn5NRaZg6LIfkB/kSX35/luVe3CPDVCthYoQhPe1VAbrEYFh1YGHWXvFExU3Aq1H0NmKtDTsHDGlzom/O4XsssGBVipApkC7"
"LCOl+frrL4mJ6E/f/sle2kKr1sBxmw3IhrzVHYS4LG+1bkGy0hkCmJ8ptgGG1HMezzcV9MR5y8oC70pQliJP+nejnXIEwf7yoN2MZ+9XPWBGI6imscXXyn"
"u8AQNqVFflswvxwu2Pf1w80UnnZ5iWLzy1aVJhIf5caHr2jOgZ0aTTpxMRzWL5xsFRB47hwfYj9poCMYACFdHHB5br5Wcvl3q9fpNtZt92oA+559pHF+QL"
"7RP3LCJFbi5a9lC70cLo6EEoMhDOADkAVuUneHYRdMz75DhKEJTVABHgzQ5Z19phFKI8RN73pOpKVfVUJdGrPVWGUGUEVQQ59/aULxWZA10w6yC/zlFz8o"
"BdUC5dM2i0Uk3QQkT/+pvfenN1HcENNjcAOEZafX+Ne+Pi+tv26RqgSghduwZySD2bZYHKjXP3Y/itAOmVrLYu9HshJdCKlkV8hp0t7ofMytPL6E8pAgXL"
"yFMPbguAChgqxXxpAJXsMXJsw1C3GqgCQLEM+MvHVER+sCsiIA89R3J5duAHNGtAFqpyxbxhKEQZhzrvAc36XW7TOn8N6JG3HvhpQK+jzXWenNi2O2UDqx"
"OcW9g8TRYtfnjxQHSaiV8y0VcT8Z9a27FHjjpIkVSAKmchW1AR05W3llf7UBHF7K5J56smUJQLwhSjPoPcouwR7LtoIIHJ2XkJ3icCuJLpYL9n6suA7ReW"
"suWR7HlqXvG8yhuW7fORTYIBPBFUQS4MpwmZpxpPbtO9wwJ0rSZTA0ADZNcqwvjOz4/VomxpTF5am/qDeYGYd2Kna05I3KuMddZDD3SO0winIjG6QOQGAV"
"4LOLPJvA0Scj0ChgfKCjsi4LnPrsQv1zB4sbkvnF1xKij1XRe6aug4DqJR3WevpQ6RrqRpjHJlQUP+PB/bKUxQBJzfc/NEue277AAUGpAl8OXgzakOyBeU"
"D+EiBOVSLqydECXqniAVeXL0oOMnyGIMgH3d00lOA/AJ6pH7dhgfvqA/eiOIiOjVq8+Iiej16zeDMpV+0TfgCXeZCn46YM7Q0bugQyMi1ZeTNdWDN9CciH"
"gmmk8zzS8fyKZGvp6If7wSX5c0s3A6nCmhA16zwHm+VLlqMejWgSxARIGimvzNmzdE5J/fsu46CH9HDwlbry/HTwXrvU5s5Xh4AowgALSuCuKppetYHLEE"
"FHncAoFVbfk4FLA86rw634tp3IKHa14jRBYgMtsA0D3cLqzNMJ8pDd7Y5ZBPO1KPEY2gdJRRHA4G2MCLciqhRUNL6kImlWnGbwTs0K4gjHgCNxru3odtZT"
"AnUCSfqsTwcFhgRZV36ixgP/Y5UjudKE6odqOKtZAI51mZOgCk7MnbcNHG7NwNPqDH3p7lFRQbzuWBJLQcBEfMztKNBQXPuDcUAYOCdG0LUOK59owRspKB"
"kT5lhgZNZzxAPlyMFNIkxR/LwvT4VyjOqbrF1wMwl/SDbLvnPbjlOUQgHeQfaUF/rICXa1mlBx03XAgNZWDpeuJLX55iXygvbmFeZro8a94oCbEITS+Y6E"
"um6buWblalheNQBz/QlGb1J+AMFw5JYcL5Iw1hdYwG6I2oFvKs4HtXdvL8UFH14dXYhzTcWYV1cTUpSsz45KjNnAeJhIgNtIIkYIrHVuoG/iGUyxBRSu2E"
"AK3U/WSdtGo0sAZRu4R7IDqfUzTgUGH2nqDXp8lH8yQPcTEO9DAHaCfS1cMCxoXjBoK3WhjKSlh1HLtGDPmKpF6AfJNFd8WEKgnA3aDwz+FBv9WIQgiLc9"
"c5mYjOFqYlIt3D5qrD2Yzg4Z6od+p8kMGclG5WfAmMYEBFZYQHOQlsU+mVaAmI5KCM/AUAbYIJIA99TFdTcjIiAn1LjyoY54gzQGI5BSBy7Wnn4xYrMKlW"
"84Y6FbLp28UT1d8ZzR5o1RcKEPXqGbNVnYmIfnz9oy/qKumM66dy8Ksy6vyfCtu6LSe5MXTsDCoZ5MVY19671Wvi5o2+uNBERJMsB8lNRMRfTsRvZ+IL0T"
"x5lEUCTdCNqkQmAP3KG0XE4p69qPDzg9gXMnnMlr45EeQpdY5Fxk0r/vyrZQXzH/507VlJdQpV4uQciI/g/KmOhebIg+49p+V9EpRkMYp7RtITRsMhbhFx"
"vRLCqUyEK21xwY97exS8XpcFN2CUtu0wKqdcFvKv+UXIQrN9PSTMQQdFOrSkfItQMGTY6eHieOtLrHtIQzzFWyRORBPplIiWZzaj+eKhPwk0jM8xezqLjI"
"h92JuDVRVAVJUAURM8fuYMbApVFBIVlsbKg4IRf4YGAHb0CKDIR+ysxjMoRH+e6tTaFMsMPjr2KbC8wuIisFRCCLvoCMaLeAN0YV/C4QyWWabFCeSS4hwe"
"AFHqeUmeXeS9BtxMI7YRAtU4zJzbKwL5Vr4MeCF99XHyUB6X7zrecl+oQDKAf8UPF390vrSXQeCFia7P3hFPs/UbFllOZX1GRF8Q8bfUvknae6PaGiREMu"
"OUANUoJzGPStkABZSYFrW6p3TwLoCmDNkANmF0DAy6EoiFwsIc1CFatgFGfq6/kWah/7N9oSFYSt7SSA4yegHvO1A1Haw8sZeNW7GKvcW2sEjguALW75/o"
"4evJ2lc8EHW1xEPC3Nwa8X6Sx5CdIGQtx+7QCPqmCG4UeGflvb0DuEuN2VpTyLzM3FZh/jR4xq2UtmJXvVc1FDBG4mKJ6xOEaDmQwbfmshWqA58tlDGHKr"
"pyAIAQgY4LtNIB9ayeLwBmPDLOGbeFOKn3uk5jazwXCwwyhsEkCIrwHgYqAr9yFpU0Rw+0a7BoFIS9s5xVRAYq6Z6HsGbuINyXR4FewUPgt94T2vNUA1gF"
"XmgMlcBYAk+tHMNH3vE5hrJyGxQGQixD61PQQKFmHuEAka4szjQW2l++WiYxv/sRPHRo0ApM59OF5vMDtKuAoUVEr5jox5n4XdcqJndVERIGDlFcZQOIov"
"ft91d/8zUREX37m29Be4F6Avetc1AzsOHvyntMjyrPUe+/+W7RG/GcfoneLoS7jc0CkLTM/sg3f67tE73V3nwxcGRX4hghD5kNxIsVw5rPqiIhpNoZFQYc"
"bZ9nC+ELVJiNZlv52wrUub/Fu01CCkpRggCUHrGvZF1W4yavH+yJ8JUcHD/isrCj/0g6mTCwZIBq3VUCX97XEMaxw2GYVhc7wZiMxD1PMubMI28vzpJ6t4"
"GXAslopFipKOREK4ADB0ZckfeHN9jobcIDMtBQ9cELpjvQwAJLW+uuaSy96G81DCK/Tltg0zDWC9K0Ohjv3KddLNfsvVvVujpRoh8WpWVlH/LlfbMVMOUB"
"kI2aJKsV3oiIpmnZaCazdB5dVZcIfkT2gQQwjirQy/2mMyasL2WMxFFayAMNsFTh1XAvtIGQHzWH+0Q5NSxyLiR0Pb+zFeTEYmFdW1d0YqLPmabrKFRGi+"
"cxI2hL+ENNxlpmOKuNPWoy6VacpqWy7CoHt3ouxcMKaAMgpjbGd0gqHniANH1urjqwYXhof3qO5Too9WoRQVVfhcUzTEHmjAwMeBDjHdAU5iCjjnPzwRch"
"AS3x/mfzl7jFBgc0lBMUcAMyxor6hKsVpPX1epMbRdmoETVs1X1zYec1TlV7eZBQpwBAdgam6AwE5tudnke85Devn2O7qrdKIgbq+vackhmIxppkG4yCNF"
"EEKpJ+c3kYLpavX/UrpJu2vQ0BjLEBrTzsDOKAEkYw7GHCPiKWxRRnVhhoEeE36KBLO0jCfGwGMARKsrrDe6weVWVQoNSHiSXcRxrRUELZOSAURhXUpQT9"
"0J5EfGL667//JTER/et//l2aMgC5l/Vsv6eIECWQBlnUZVBFG2lBA9nzDI6djJ1IFwaGfyYS+uHtwlfYJ9oWHsU6LTK8TheS84Uwars4GqIf3Fquz5j4rR"
"C/05GeEHUm/6wkhsvANfTtI/pOx7vQ97/91hQdBzoRQHABSAnqCYDimGLLG+C+Ar3QhnFbfg2urivMO4e0RGxfz8leq+YJ7HLUgJVnbVpU4tjqPVbUBCA4"
"4M3TKaBpR2leJ8qsOInH5M4UPKmuiaD74KIbm4MVbR/2hW3N6w0nB0H5LgcHKNsCBh/0Nv4VadWRIfLTph2bM9uhbbV4XNFMrAvDJHinQ1lAHfAW+yS2oz"
"WaLByfsVkVZDgtmNGOoWnCfSOIYIEhwWEao70CoqiEk9KL4EWJN5cAQ0o1EMziFwLvo/fmAoBCZ++8awgnceAxAVJQnGMAjUoD+E4dKSj8lD+WnIBFF1xl"
"8z+VsX6IhMsNn00c5eaefsqb65GuVSAMf12r9CBL/bskN06Cr/paTNsbMfEeJFIwbnW2Vbw6OoTm04OH8k12KbRLtLinr5imK8VYIBHRLPFRRJf2m8Fb6b"
"1zC1sBoKNngzRxrySmzZEuyb/VE+E+JpY9U8SYuFkP5AqgmMG18lolj8EpRTelF61IVqQIGuplxa/E4Mp9BFXb8mKg54tZhDQBgqplCjJDOAlNPQDUEkHa"
"OA6fG0NFixXX/ZqtADE96waTSVYiMxhG1tWvOFZ0lfREsdtBRBrkCJUjNwQ0E4vQzH6QxWAnZejiVns1FJiCd4pN7229lHvOi4p0FRNCqQFhIFYfL4fdPK"
"cjsk/ZJqWHJ+Zws8idL1RKnP5XhAuT8cgTEcVVu4nfCqSgLnYQgeA7SWWkVaqa1+qQjBDrAwlEoSNH8AGwYgoggzLp5IoLl0Dh9/qdgY63Q6QH897JIMCQ"
"ssxCv/vH31n9O+9zAJDEdR3KZzx+VuYvQJNTgdjPSkMF77miHZniATOxPby8Cz+QTBfipOkmkt4jJSJ6TkTPhaYHKEDIj7wLCAD32p0HX6tegFGCdgnmVt"
"pfUX4vlGK/ifcEikkCfVeWcbuOsSnRlwu8BTmjlxLnIl0Rx/NpkUc75LwR//pXXxMR0Te//bY/OF7LRtoCWo8zfdjz2d4LPLc7RkB1WbuT4P3IyyTovOC5"
"jgA1e9YGqOllB6hkg2mZx8XPXSug+lyn4OJSUrD1z6E4j41vdSAEYp222hjbFa0F75Y2NSF+uIXXRvopUBMJ9BXdf2uWT5QvN750OJy1YGaJ+x4pDobaOw"
"EvM4REexAN4OfdIAgYaQQQSSBq9NnBqdXd3gZAi22f3qHJkABUKHjnHT2gG2gLlIHeKgIo8nMDgHbyD0wWpzFloNL5gFB3kGWy2BEs0RPv+wlbKCXLPfI+"
"AMRhOt9etZqnUMAIgJwaMc6HRiMiAjCne3KZlEBdMZgOzmhhU2GhK7+jZU7UL/WIdBuMj/L28DNa5kqbNl7O0k2FJ9DysCz7Pa46ZSgYNI7P8jhiGNjoPb"
"aBeb2wMtbK63E+8srhuXlKadHfyHP1Fa2xD0j6rWKJ5+PCSlZVmmmRYOjb5ArWRMEUwdLQssEC6B4Ewc5LJT8LGL1UgYiae9soiOhNhIVPAoCKQkkDVoEC"
"Y95hzlC7Abu+yYBKWIbAzhAE1OadOu1iz22rE3r8S3nNO/amivOqzfBQ+jPWnxSo0dHzYw6FvBHDuqtmrDqYLu/PvnimwmgwtTqljSCau37s1pjLW6wHUU"
"o0slKMICqh845ANOnEoLy78DQCH1i/HeDAbwS4GljFRKhAYEZF+FC6l7MHQENdrWL9Wbi9B8q9HKqtI1zIx9rJ28qBH1soysxJSuovRRslRcvwT0U3g3sY"
"xCga7A8o09xPysVFXKSLf7N1xanRON1g217oHQlf4IQtla7YsvzugAOhZTvMC6HpXVN7EBNDcGL4HVHNFbQDhyOiANjawpC2NUAt9rjXTvOlvqRZ4WEXab"
"N2F7xt4MqhhVXBR7UVJAzKFshjk4CHgeBQgeq3v/2T50MZA+AFUIKO4Qcq2OiEI/qiwavbSbBOYy9VzEMVQVgm14uBH5CJSctRoosahI6rLlkcfOhlC4Ee"
"gXN3tYwFzFvfgQ7C0OfcKPBOgjJSqSGYmhFpMgKbBfrFYpCygffSxs2vlQymKk3v1NjX2cDU5XmOoyF2SKG5XDTUN0jgnHy6OHqnsYnA48FlsOB5hMlsLL"
"8hpysIVPjkjQ9ggAlc5RcnJRUz27jPM+bjQNPrg2zonK93OV5LD//qNpt8YX7P2wIloMhDVrSsoNN1oBQAtEinVlpmPuQv2hnBcC0tlh/AED3kQuaJWADK"
"AHrutXEiEoA2P4P76bwcDCDz3OfNQJmZJR/s3BaPXOmth24TmMbQbt+X5YUQX4RodvkhCLmCZ0CP0C0ooK06uKJzVRLKtDlRieFPgrQGRFB+iAj3xRnfHX"
"PsXCLgeT37Y9/wcqUb9ynmr75ghKYDVWAaj6qzOgkeMyfxxByM5LTntkI0SI1aVNABlyxZBNQwCdzuw2tsL6IOUNVWwo1CeR4yUnIE8bNsXaZRK7YxLA5I"
"JsGwFaulwahO2LJCDr6IFcED5dAfXZZdL4jPvOHS6xQ9CR3Rs6JdoZnP3ALILjLv/WqFMrmF7Q2KBxzEoAmG4hgaMoARKjizuvW5gyha00LNIiSx0EL2yt"
"B2xRMBRytFAXMBULEeCbgARH0xDQCOyUn3pDLpBP06gEJdYGUxhbKRP+c3Aii8t/pzl7an6fJDxVXyEcBW7wugy+CZgBz7hNU/lCcpH75LwKl8c6oLOx89"
"qIIM03OGymP+6TzRr/7Nr4iZ6Lf/6bc1mGYABoFkWVzoHRFf4dPqIM/WL3ULTDm5cyKSZzNN7ybnV5sQtUI8iSCGXU2LojkNXhZgMBaftyr84sslMP3H72"
"ZflCQcaJYqrnC2EVztuYEV5CX1jHHkCtgMsBq1acBgX+i7YBGkvlV5qjghF4hFVYwwoPs5FwBRayABamv7uH2TreIMNDvvK98TyAu7DoPcBQAVjgNEvUZJ"
"tmF+0+Kd3rEFxqGYHhQzIkQcsiLgejtjfwgCDoDpvKP+EdjXiltUcOGs7amV2BuCt258ag9KvIHBdPY5DGdpUbZZmUqwbAxEkyeawUePoQoKhFEMreOAAl"
"TBgDFqShBX0HKmCxYFKnjnFYUAIcYMslyAnnbpJhA0IDB0qCc7MTBRg6g3agxHS+AHu1L2rnGbUZhXlfzMQ0MI2jGNC7syOohTPuRXqxPkCcMA0kV5DkB2"
"YPRkwWBbIU+xTTjyVQBoP+cZ8xCUM7HQ1BTONBHNahFzlCdaBHluXms+05VmetvmRTt/zNuYmkeqlnS2bl4ITVfxvYQ2jAuEan+tNCHUHotXAPfi3+HqYo"
"P+1RKVjSdRI7xb+Yp/+yCY3SLroKMjCAKw+mpXfIbppJu9QmWIfOUTeMIgAI/MXRMtE71UpAcVAt4Y5CP6IgCqg5Ho65Yuj1fBIqC93Z5xhkzTw6EKPv9H"
"ju9C4YCCBTi9gsp3mItOC3x0rYn1i/Zu1ufiIBt0TfBEvba++BOMotxFg9WnK4QdTH3PqUZVohWJhpytMG5vVF7LazfVzj2IElV7IaNnjN4i5nM5IF4y5N"
"OHkzGLwMPGyxqI4olIS+WycnfeO4WsIRxO+Yr8kff0t3UuB2mG7TpeMJMvIHFFqlaa1sJpVRd3NL1RlSf0Qqu5ZVwwMfR2bwFQlA8PgDGXk/vLIC2CJxoz"
"JdBykpE1okTwhE5qHjM0OGOebjERE81Cv//PvzPlOKU9oVTxAf0KH87zW4onhvWgw9KiL6HCCXRPRPJc6PRON6ymFZ8hYASeJoEnAunySlwM0yYD3sJvxE"
"Tf/LAot0n3aLKHJDsdj2AN9e3cXiwuOsmGa/ZM6mfxwH5foGL8I1/KG7xzIbjSJtBB/nkyt2D1TFolaICa5IzNGY/Ji4fYa9MtIOUE8mKb0DVCeQzVUKPL"
"9b2H6cHzZErtBvWjeKys7QcNnVe9aTDy2jVB9yx5bmGT6HIh1jDN5OivC6jsuEPr66A5QueNYKqHLJD2mcSPALN2Vm+jxSTLqt0QREhbRfR1AMPOevZ8mg"
"2Pd3Prvg00VV6EiygUbmboXE2JTtDZDZwjSjs9BwsDnAACcOCEeYqorPuVsKXCZzixhojintfGOspNy0YZ4bMC3Njk7RXnXEbMamI0AAWrNdPO8rN2q3gx"
"+cZ33QrjlG6YHgZMXL0cDRpo8LFxg2VXAGay4iA3C89X6TuAhTLmmQTbv4uVSyoLO8ryZ5YrzfwQv4SW6ieipxrN/fYXaFsiInomRFemSb1EaEczOec5IJ"
"GBT1AcHMKi2o+1NCxT5wO9JeE3e3vqxngEaAeNSN/44l7LgM1ZAmv+wo0+i6BaeP4mb++j6LGjl2q0GZz5sOdTPFzZ+pJt2Sg8RqJK76Mr3H8Vxlf5quds"
"a1OjhZEEEWygNF9uskor7o2UevUE4M3KqkQaWa4KpsTJENO9pa1/CMi2KSOUG0OfE2rft28ydzCNOGR93bxv6DwtnylL6FSINbjASTsxGjBCQudgvQET9h"
"w7a/LgnGEn6v2AAbD8BSzIAjBM4VzoNVxompAGFTJx7EzGXXrU0qEnxz31VkTz5KIujMmsUxgsW/818EihFSWWvVAEEVzpnAHUfyKA5VrHOleNh4OTi2TO"
"D/gNISLBkKcAUFDmedFSBtnSMMj8KMh1aVKYG0BykXMEWzPsAFQRbLOxQhjBSOW4GDJQe6WyPK7zG2K6dmMpJVsWGxGNw79kCYmezcSXs0cl2t+vfvklER"
"H96Xd/IplbmM6MPL8PIJvDXQqMDUFcpeIFSkw8lBrAzh7kASQhHXojOW+09cBLgYjeKqgiGBdCNwMDGwJwp4vSsWsz1kVYBIYCKmrBfIkJLAvHHIR8OeVD"
"UPzy562d//idL7ISjzqw0XIrR2jMQ7hNQvX2AKRFLdoJWA+nb7/beNE5TPVotX10zHTtKI4VEvgDBMJGbr8jxrUJEw1RC9s2nnB0ZjDcWn+HODLaIufosY"
"AjnQ8dgIqG59BBoU8FhR7mEE395jNUzb4zqfWLfZim8AAWNCmN1kBxTi4DYV5w465jWKBEyDfUW5+Bd1yuyrXQIJyk1DLjYfRZ/j73W3uh/jMvznHliHyi"
"Mo+ARD6ny+i7U8rvMuLiXQDQol7WNiFfjBSsgWcsq+8blkcBFO+J4mIrAETrK9iJrL/GvXCRZgbpxGcAYhxTS3lXeSAR9UajcsJLvYwuvItpUCmfhHieaZ"
"IpKCM9cnBiJpkEVjqiREeQWL2l4F3aaTXtmSpY8wBVGaU6QBzM7v0XgjInzZBBFRAU+lRenJQKabQH5skoTwJUL8fnBn3rRJMLeEyqnP1ggQg6dk9uzNjY"
"lMI71XQYXVHQUn4ElvtomFVcx6M9gIZIdN5RmGlFrspd0D0SN+ZoiTM6X1CujhWdhxWQK7RFiLBI14zWSx0wvV3QcDQwhc+qkfhWLm5GJNLxdpeuP2kk0g"
"5kCCCZQAU7Z/7bnUDCCJZJoQGIhgwU3xPkC2+T0VEDHIJoOvigdQ4fzLG35FW+lr/ovKjQIyBwW9RjPSQYIzkK2MufSzmvgSgCWtcOGcjIebBP3jHyn+Wu"
"9YQ53WDEoCEW6xQMJzy+EUyFCoCzETBeveugKDT7mCayUqZpckOGKc2RLrL4668XSPvtd3MHlN1CIuxLnHgPId0MxGyg+/DwhlinMFKo0eomRM+en+mrly"
"9pevaMvj+f6OFy7cEU7yciOs80XSejISL0/R++c9nntgXFge2duUKVG3EFlmLACwPJppFRuSkBpqT8RMvKTMKhBNorxN90nqok/RJCgy7f8j6JFmyNVUCN"
"NHylMG75yOm1zmj0hKMOm1dp7dMBGRIU+u6b7yEaAbUxIGl5M5iiR00QrQAAMmm0+UGGs/O0JD83F+dfF4ITLZsiEeRzP8byc9c2MCXoO0IdHcEymOA0LD"
"yCUVunoQVL0S/jX+/X4ImqrhDRQ+tRCTf7AUBUV126Hik8PkJwa4KaiLAZerB2YMVUGgploL7oJvQ4Y4fvAaPRRYUnqEjj0ERPugdMfBbrkI0G97Q1XAXw"
"yDicXLYjEI2KOHausCCskENJ2+SA0YBovMRyCnnnFc1pUtUBlEAORRsnufb8R2Ol6kNqhYvM9Ob1H0lkDrSnaaJXX/6cpukUgMzKYFwtDsZTSINACUZPAs"
"v+AHzYr6reAjNdr29pvsLcaFYaTWE8f3am//bv/2v65c9+RsRML6cT/cO//hd6uF5i+nxNQkQzneRkykE3wbOt8Fj3MFXdIIv5oIS0m8Z4kkQBMaj6q66P"
"LxSKwCpN6AiSDoBBxYOCA2UdtC2PART7objXy0DfjCiJ6fPRdMBsAxoI95ICEdCVBBKSyDQLxIwRSt4pSpVB61ij6nNq87l+YpKFMjFSY3yJLZpSGSEoLa"
"zFM4VzBczrDGDa2ST+G0DSRe5t7ltn2ObZezCVNJ7cO7YJCdbQLmAOef/Rb7c65CJ9+NVenGNIF2u/dglUuhJH+gkWDqXUYV5U04TtMQ62+sTp+mjOwjRg"
"g84fzXm0b41MCaKeRCJ9aAROtCoQjTT3g6jyqym++sWXRCT0/R+/T2VGEM1eqMpMB2cJwDYOEohyz1sHjAiiYHh07ZaaogPQUFaUeWhvXsKWswiJzCTzJS"
"hz4jMxqz3n4BfLYPpd80RzON+BV0Cm+jwifphLVzlMCqLt+bT8/Wx6EfpcF6cSoZ9/9RX94qsvbXz8/NVn9N0XX9A3r3/oxlK+eGbiByaZ2+pCWf7aGapM"
"Ht0R6RRZ9s58BCLgEcV/k0JsXksEWZwHZcjLMR2ApQHyAFS9b3BSwgoQjTr0aeMS6qL5EDAyoFrzsOs0SeTUI3SvDg8tgI6NYV29T15RtLkVTDFkSyZVlO"
"YClK3V0jF46o0aGDarIK6u9m1gQTY6/wtbWqw6Rhdau7CyEEQrQyvINEREMayQTBzU8YneUg22s6WtH5EuIIpznWZ6hPJ7bEM6xERnDOv0fawxi3ugTHCg"
"iLu9k2o1JatDLbkAwHFgcddtYdDjhUvKWRs/gaNJNJcVW1OPf0DliosN8GsxSg0XXoWFCdaoUWHHNVNsIY8Aoh3YIlkOFmNQbo0H9JI8HBoNjzUQdblgi0"
"Cd0r3nhy1L4X3yVvUvb9xDP+iXvXsezJ/rq/WZsI8UHqQZWxn4zZv09orh3vg7Aq+m5wam/nu+XGl++87yZJtVgUTeXYjmthpehGieaX7zhi4/fE/c9ZB4"
"PTu/oNPppYUFDUSFSWZIiCfLGGCBkQyDBZWf8altgvtFheDoNXaly6rUifSgc1WKs8xki4tE5TcZj1pgADsCHVQcsh7nPtkXPhUxvFp5U5gn7FQQw1BBke"
"bObvRtpBrw+N5GB3AB71R1hI2GtNBrUc24BQZXVrfylDDoRlHwBDDF75lC9/DqqnGAXp2mBsBxHhw3gojMWPFtNhlczbAYgKnOY9ooz+WoJx1aph9tAtLq"
"GNX8RR06fkXnSLXducoyg3LHeR9vGNZGhwbO1dDEeVGO9zkBBeXxa/2CCCpnn8uE+b4sNkaPUFHG6Rjf2r1VIWLPwEUq4vU0fsEUioAC+ZhL3oCVjqcoX0"
"7yJfr+j9+FFuJUVgdQAvIFNTwK5fZpwNsKnjZZW+Nvp5e8bGyCrmwwWOy5D5a4YE0rpQYJ08RMM3OQIcP/EUSxzbB8AGtGuRah3NanugVImk8BdEJQZTqd"
"ntPrN9/QfL0E+Wjf1usPDw/08y8/p1/+/BdETPTNt9/Q7//wO3r38LAKo9N0opdfvlo+yk2LbESEZG73E8Hh4mT7oX0hULHNpY1DBQ8DS1V2rTn8N4cj+L"
"IH4qFHorcPr+nt2x8snRDR82ev6PnzL6wD2kIe2Otn5RCB9wWACv0Yh3QEVOgs4I2GpoA+q/s4GRIYIKCzG1QohnL9tB0dr/rpMF2cZSBigmOLvJnjIOA7"
"MneHGhiPDIewd3tOg6WRPMoYRibTHxwMGUvtFpS1ruf3orhZ+wsgp8MUKEZGtDEEOxb2I9b+wKkebowS0gdw9q2Zce5+SbswqxHMJRTvvThOenjnOI8GZb"
"8tBPJyes7dj+4uzidGC0nf9yCHP6L1xxTTB/ZQa0tMiLT9UAV24ZEqVHFla/xngEhzbFgfe5ZBNPPPqb41mBE5IJdAhCAS8jifHUAmoCVto9AOyTDQf7vy"
"Yyi7l0PsJiWIQytWXqsDaGwLO3Uoy8WANAFk7j+aBtuyDXrsSqH9OIK7hgQcRLmBqD+bmOj88iXJV1/TD9/+oYgY+c3l8kD/8R/+gd5dL8Snif7pn/6Zrp"
"cLnbrVRrHvv/r8S3r+4sXyXVIFTdtbuigFmV2JqORzG9g7oSJE62NYHQPHAAy7UXBrcD7N9/sJkVxT64sZZ8lJiB5Q2EeZATUkhLytpmoUqoeHIWOsaAJU"
"r1IygoDZsHcVtJ2DmAMbkYJF7LtWTgDTBlM2vwdySUCJbeonLvncqhoNwaNWMG2WAQKcTYlon2h841oa/ypKi34o8BhJnZ/0aEbs+xlMlY5LUrp+IaoC4I"
"nrBAdIsnRab69ftJ5UvgzL2907RW/cd9We3ZtA9tB2zOFAbMDmMZpKk9AZNByBIMFGF2iZEsOu10QCx3WZoodO582BKyZbRQW3y8BhDZS8kW7SWQL9MIOj"
"fHZKEPmPLx3MeuNEFXW/7xO9QGxk67beAUtPNK92ZW8DBBuVZaqLthF6pPYugBUCm6tYrFuXj2MZ3p+ozO97gxWvwDBiqGNRlrV3907bmzug9PIdTK2PAn"
"CacaiAyk7PDC8WmgLAEr364ku6vHtD73583fUF5OPy8I5++PE18Wmiy+XBzuOtoFRI6PmLz+jVZ69IT6oxHGhnu9q9vcvAiceyUa9iVAkBgAZl1TxGP/+2"
"Y9IaUd9PIEf1XpjhuEF32ODAdOnokaXlHlDB+/KN9a2+pnCLhSXagSrvVOsNcrJTnnKVmWxaRaAjhxWxeEBFt+iHbIzrkXUTM83ggdv55DIAU+IEuMqze7"
"xE5JGEvMCpi/NSaGNvEtVbYGEpB9CmDpKZ16xaNWKC5pvrBIFJffM02yP0eAk8Tgw/26nBnNqTodmhkweZAwyf+16elCZqP73tTLH4M1Pwn9gzcUtCQUsp"
"mYHr4aCgsPKaaKCVahGUvVqjIWSXFi852PeeCXLMHMsJSAOdocubjRNAQnVMI6jgiHYFH7zeDLwoC9ZBmtJ3MqvbpAJRNHA4p7M+Xnl3kuoG3HJiLZTZpw"
"lcQtoJAS28wzCsAzEuHiMAWAwFm8w50VEaarABcOL/ExNNpxN99fXP6Nt3b2m+XvvuAnU9td+n2BRd+mk60ZdffkXn82lZZATAieHM5b6NP4vG5ZCgAot7"
"S64E83woKkI2D5aI7OzWGA5LdbS6+Biw/gD5vSEARAPKkzEVPNQURnStqqAC87YmI+6Z9Gxh/tgXEi0JfH44sQeaOQwtq46vQnWnwCDDMtj8J4NnKkhuxT"
"M1MFXgdbSxegNPuEgnNpEuZnLTQA9sUJDOVgfO30qs+vqFstYHcHoIM56l2/oy68EJhV4EUNQ+7G00YspfeLg7xBiW7S/9qi+wRjn2WURjnIhFayunXSw/"
"8jCKChNWEaEDn1rNO4K2qbkFrXwdbOCGG2A2Jei1AvAFzS4iy8Z1jjVCWUp63XtxRDbacHCldow/4sEUETQwqYRBoQxkEA3g1Ij4/tvUSwL9HAaOytqBCI"
"AHowQZ2EBBx7BzVFNTEginMkJdglEkbhgkkcd8y2BzAHZkzQAbwt3BEPE6xvnhtEeYAEQbzYkIwFzsjOkXn72kz7/6mn745g9FH0IjpNEpwdbb8/PPv6AX"
"z5+3+U+x0KkIfHGD3ZMiavfF4iLft4jzQ+DNQn4D1uw5Gni714PvwT4m/cSnKX6O7SJwWpCv3gURtN++6rZpNPOYmtclFMORDBUxjU2hAkET2OESoM9QB6"
"StJbrg0loLgZx00ZG3cwZT7BcCN2zuMDvAqMfGIAfsT6029m1Xyw9gHdoV6g6GlApFfbEerPu+SaHefu9n1moO5xqRJcuNoN8CQx416RBNgL7fZcOBiCge"
"7g8un4XTXblYO3HnkQKwgfXnCsfn2roLlJiCDeNowKoEsPCeZGdWktMK5BU4zPJ0izjzxCDKoKDQgYUXCKJF7VoW8FhBEeOn01AYqownHcwdf31ZUXYReI"
"NSJwfRDNIIohoK6uckyahgNCKmSYt39BkCiAFMTtcfVmHvOqCOIJrTII3YD72e0zTRPF9DummaGsChR+4Gl7ehGiWx35mdFd6ND87QPjSR1wP/n0CeExF9"
"8dVXdH37I7378ccy2kHcQp8KwFwkIKHnL17S5198YSE480RNG7hWYHJvFQ/qFtCaCJhdaCx4pzVIZsVvBNNcoKfBiBCstDa0JQjbtiMY4PzBeqFPtK681Z"
"JHTWTgFRdJuRsUzP222tb6vcS6wjkFQT/hStxuwU545kQR/Lyc5X0E0ziHB3HwDlCXfs/piYDAo6HQh3ihw4vzKTpOzGBBW6d5gLAVxzSl9SUF9Lgi2GhA"
"/9WM5lFjX7M6Y59HgHZjxbgJuzI45bUa9MYJGKYLkGrrB4ZUqNo4AoxGH9Uqjb0Te1aHBggg8TcySVUpSWl7+mjB6byYzZdkYcMSuVKHwUIGDvJITQzSNX"
"2RlGhpHobNS6BdDPSg6Sq7RZWeXImnU5p/hYGRwscIFvF5YeQA/wiOzkMCUSg/GBiDNsJfGUTHxkEEcQXS0/kZybuH0HdO52c08dQBEBogYRV5AmevlxuQ"
"fsYIbJky3t04Urz2eXl91qYUhGg6TfTlz35G3zy8I2kh3iwfBfNJ+0Sqy8QTffHFl3SapuVQ+rDQRqM/HtLFYIwf6SdBSS5zoKhwsjcFzAVth7/TvBOmhb"
"8afheJMte2VjDrNarggIvlg3eM0zXgQMB7rwuCqSY20AGgE92+Q9wOuQCxKxCY98uBTVuoA5CRwZQ4VnMEptgejp0ako8Ajn0qGz16YdSw62jFIwSS0Kes"
"GcDzxv7S2jbuZyWXW9Wp0JBDvxeTaZ+xz5yRc8HoxQJ9m9dXnoBciFiQCTTvw2AmOkeUj0yFxUPRrCEvR9PowqNIij2JM14BBEwm57oGGpyyUEoXbE8EEZ"
"xjlYBdAcewfKwryNXBY2k0taWcB44yFc59MNgX/fIkNGKyPBdal7d/ordv/kjz9R2dTs/os89/SednvmVARyaGGZEYW7kw95fKwbTZW82A50oneZSFbHMZ"
"AUS799KDnMqBl2Hy8O4Hujz86FVv7y/v3tC704lefPY58eSDiy0dlk82WMNcKbZ9mIqAPgB91Lxd4gicOncq3g9ppsWb/PJL+uHbb6KMFGha/SeoF16vPv"
"+cnj9/QWQg2jwmad4pzh2ZtxpXZGaMi30wKdhsFELaboVGQi6m5fAM95Z88Z9PsSx5+/lMN9zsdCambmuIb+NBLy/qBP3VAUryTCudZ22g+0DBQnCwaH0A"
"wDTwMwAs4yWdPpVtiDCOWiVy29jYBll4Pmh3PVkKeoTZLyBfSvIK6g17QeoiDKliXwvHxRB+kg37FNJBY0WtpYyjJERBrOzPQlqMy0N/tMV2YibJoBtwEO"
"q56i1hb+WIiHZQpm6VGZfpcTvy8oi7AoYF9q9h7PojmKXNH/vDPEgrvc4KPyt5NRqi3JIhslqHsC7QLeV205FIXgoT08O77+j1d/9EMj8QEdP18iN9/rc/"
"o89/+TW9/qdvG0sIkFKQLHb6dmCFrxgsifgWFaEDrM8PuXexDaK1UZT4Ai/74d0P9O7ND4TeMirod2++J2ahF6++IPQOnWUFOk5GQ9xO5PLH3z7o3CiDle"
"nsBlOenqZZlu0xxPT5F1/Q5c2P9PD2rS9isr6y5MUD7nVwP3v2nF599jmxNBAVWc5g00lI+OsKQRUQboMQc3BM0QU94Q1ZzoXiUXTUVKYIzfNMMs80zzPN"
"85Vkvi6/ZTbP7DrrXkAf0CIzzfOF7FAGagtroH+Fo+3Ai4jA2J5Z3YR8rquuq6/sbPnFPbwwLQc6gEEw0ohYGLOBqX3mCzxbPDihs09KgyWW6h5UAjmTFF"
"YsNmUca2pQA3ihRaICzAAF40iMj+WUscXHaDyYblgzUAYVtv6OWguURPKmvI9n7wHpZWCP/bdnJyODD3itzhlBnlMi6RhIh3wpoKcwRXbMMeaPVhJaHbls"
"DjTcnAjgaysusx2jgya2ud0E8F3odCtvQZBoZGebnXuSQ0u/AoUsy8Biwb/QTG/f/IHm+YEstEhMr3/7O/r5v/17evmLz+nt739weoylZLc+e6NatwkAsT"
"cS/F38rZ1zyownFMkeX77c2Iq2q9mG85VmmWmWCz28fU0liAJIXt68pvOzM51OZ1q+Os0h7Nwvamrgbt0R0mbPWbz/4QlfeXE0zdzOwW00AIlO04m++Opr"
"+u4Pv7dvhtr8Oi2LpeyYQ+uLE33xxRdLSFdPW0BXQYE1ACq54rFO7Yoth/t0cZKCgmfUQ76FRMFSFCwbUM7zcnTjLOYZq47X/azXWWj27wRY2O7h4TVdL2"
"8Wg4knovY/88n+2nPtf/b5qxiS1npYc1jIORolqK9MP7PKxOdEEUwlJAQC5nMljyqNo1JRt4QMh/3rPwro8JA6zZLfK4+wqsn2FYPctQTt1DaemZY9ybqI"
"rbX5YgxpOy8Gkgg8t4ZpKwMmbS9tT15+Eyxtty+y9BoBQSssaeJYVZw7RU2NZvwg0EAC6SrcCp63ubmLAM8dCqTGjp5Cv8Ciywc9Z9nPxiGMObIS8BWP3g"
"V0qayEnJfjcwSGoMyrCkOajhm3YzLojeRo9wy4glrc0oyaeckgcqX52p9u8+77H+iP/99/oq/+6t+aNHxpPxFD+GJYLxdIKBO7H4dn/tzeZwxdEQeIxG4W"
"yc5EbZCSXJuyvpDIdXkuMy2uVzKcg0hhD6TM9PD2e7pME028DOppmpb50/abebLFSdOkCjotVFJesUxYRo+rcx3QmSY9mGHSOUGdB1crR+j5y5f08tUrev"
"PDd0F2+n9etfvy5Ut6/uw5kcwBMGzYT+xYOpMrQfLFSKpjM+YLC9GsmaWBpHuXpjARJC35HLpE2LoCQK/bNhy8G3C0PHPzsk2JBfzX9pmIm2LmaSKmBWhp"
"OtlzAq/IFC1gX3BoVBY6vsGxUeWpp/voKBCOBAJgSqRDIOuunORz+YCAUEHztIJWi/jbgzcslBPh4L0TYftlcNS2vppRNMvV+9DskGYGEmiC4TNiW0mubW"
"OatIHrEh2a4H4i4lNIQ2pUWN8CHcMoC7BwckfvNjszcArKWvunGhYS+wQxFycbmdcVTKxYCE+JIf3jlpi7zq6IsFpOu1K1/pyBUuQndrmuCh1orgFU9mx7"
"TzdyGEE7QnN9LX0m1Z9jTo69vMR35olOp2c0XxL9ienNb7+j88O3wRPkQrzR81JZwV8rK3uSyZbRPEG82TCIv33D+twU75VIros3I1ciHbxybeCvh8PGqQ"
"HfqoJGAZfFW5cWoZmEpnlephSZiaf2QbMGcMvhCcvzCmD1EHqaJmIQxJLeQVPlspQtzXsTmhVAFXTbPU9ELz97Rde3b+h6uZgxoCuAEUhPpzO9ePHZskBJ"
"PUPrN7EPub5a9tb56UZL6JUAFBdF2kKy6m1KAZaqIQX7M8SCzIvD/ZsGRaQeLRpQethCt6KS0BvUJ3PD5JnmNGT80IZmzjSFrMpYvdrg2bYOFebtGL1w5N"
"zl6oDpUSWJoqjz5GtFPQEeW59aygAQlTz82ty4CM0zgGTzHmWe6aqgKG4cobFlTdzqg5/DG7HvreU+27CaokDsW28gnuKyxw+8V8Db/mf1dtWwQk8X83QK"
"CbtvcWpSaOOqVsuzM5CMPbsTwdYrKKC5oKhosii58ygrGEqIYn+gumvoBXREsJmIzPJReYYVprwmAWuOfKpHSG+GQwzb9jynstYdUmKe6MVnP6frw2uaAU"
"2n6Rk9f/lz8qPHVskUfLgV5n3BK8gyIJJBFcmKNKUni2cpV5KrAyXNzcOk2XgO7WD9vAfRSpZedjbYuPsVuq0tBPIAjsxMNF+JJya5LmVeFWSZiKeJTqcT"
"nc5n4hYulpnoOhPpp7N0/UD4OyGIqte6yPd8OtOrL76k13/6hsi+rxrna5mZPvvsFZ3Va7ZwnXt6ouHT4DFKk3VWhk2ZXjU85yFQtbwrhRK9n2RYIogSAO"
"9an2zE8oECuRWDt9Zk1x+PB8BP1xAOlOauqFJeQFqBtYWPJwVcCC3rV59Jw/LOB0L+8h6M/1G9zbPxRUH27UzGSAHUGsKrs8zLWKIFIK8AkjN4lQqW1j8y"
"SJqHPT40o9Lfo/psPhu0ZO/pFZkhokLNoLP1QtpTJVG2UHHzZNXbJQBfmIvX92ECRw1js5zInCJtJ1hshN02174CLel6+mJ9zk03WcmQMc8dRg+2KlOgLF"
"UoZuWm9NZZmqW9eD2Lwl5CTvMi8olpIh047CE+do8D3y9V8J2pWKXs2YHJZS9xg3Av46QygG5404D58u41XS4/toHvr549/4JOz16GbnjoKjPkdc/kHiim"
"at4LtUG9hIMuJM3LdNmjZ40eLoadR7wX0JgFtO4Mo3aKlZCKB1CEiLqynEw0PTsv31e8PtD1wcOQZGDLixfZQsen00Q8nWieeZmmFRSkb1c4n57R8xcv6O"
"Htj867+N7KZ8+e03k6E12vKvzGl3S/l1CsWGiODFw8VErEi1FwPtNEQtfLleaLn30bFhypVzQv94IJwBWLACAa/VLbwNOAigijn51euTe1yUKyysg9BtIE"
"r6lpQAtzzxeobzTdzOMhAFsD2tNiRLVwY7XBPzzowMJlu4Cje5EaZp01WgPzkhZWJ88TvDmQaYxUZOXihqlIz1UnNzp+VXXd03b7aOJGooq/NDdP2q89kJ"
"6XDNjqcd31zVMzunyagGnyTePt3RmKHEKa4X0b7aJWWUrrQmfIqe/qCncA1JY6hsVmCkit87MIzdS8mebVLHNn+lt59oHtClxIrss3beLzmG4iVYiwqZ+X"
"ObYlfDe1D0a397D5X5W613bC2i514XXPsYqUisz0+vt/oevD687Tfff2W5rnt/TqZ39L5+lV7Hhpo9dmKLp0WYVmmpe5s2aY6LylzBfzdnSFqCpPlK/SHK"
"8G7/vSKp873puFr70vWnJkk6zBAGLv67ADfWKm8/MzMTPND5clNGreJdl6AFUWS58gIrnS9XKlq7yLfY29j/ncKdP5/Jyu797S3L68pON14mVh0vXhbQmg"
"sLOk9bFmfE7LKlIN2Zoy1/Do3BaHaBj7OdP13ZVMPYOWtm0b2EHVA42RUHumZdUBv3G79y+1rErvDFFqFwj0gCEmq36OtpXHoDHCYigHW/VohcjCrBpOdY"
"C8mhepOm6ZgpRYNjsPYSvIQUfR3omedPQ+r1sh+T4leF+pLjBKZDYEKUPOLVa0qBSuPNLiHrYbDPwoy8cE1nx6l935vjrNRmwx+w4gSZf5t3CgLqAB/pjU"
"G4612CtKG0a6Pw0WcyxGtbjyVJNBFSKhN8JtUDFNzXI175cnX4CCVg50arOckTPd5lBJToRmeqDP//4L+v4//YFO9IrO5+ehoZZFSkyn6fmw7kLSwq+60O"
"dCszTvcr5aZzMZsnUp6Cf3GSx1H7nxUq8zfPeLKGyH0C0WuAKloQAz0+nZM5qvS3i6VZdSixHx4rEys3mCLOLtbCcrEQnPrQ/N5qAqKE/nZyQP70IVJj6R"
"XC90bewv4SaJddQ/tlIWIiq8eMPXqwKwK2Wbr6YL8flEp2cnkncXWB+0hJ9naFkxjZ54cBcg9Kz4/l6N+n6uyosSM2CEiK4k9NB7OD46mm4zLRdCquDQt9"
"8YWpQ7VfX9yevj5SGZYENVNdI8mMEjPOd+9WtfKEIsr6Tbw4d2vlmXE4YVmgtAagjWvBtyBTMOBY9rcA9lvEoD59bAKPYDMdK+QGbweNHzndzr1ZWI+o6E"
"3r7+PV0vb8dNPE10en6mt69/T/LwB/r8q1/T+fkrImJ6ePc9/fjdvxAT06svlucKjhaKbXMswWgBvq0uHQPZf8Sw9m3yP5JvqwfbwSG6YEK0JrFx4xdAoH"
"8xEZ9OJDLT9eHSon1T3GKhRlybu7w+XMBTJfJtF8oVMqdhZF+cw639nf9lnm6+qiHnnmI8Ao2MjnlTRMsWBiLi0xKyms3ylgCWIkTzuyudni/l0/VivPr8"
"qYOozbmhR5/bALff7GjL8cvapw1PCwL3UN27Q5EwFBZZzanq3GeAfbw3cS83vfoA3uj7uWTXu/vUXZv7XGohHOw6n1AJ3Ud5S8rk4YiZ7EsAAh7mrItL1K"
"NM+UkBE7xGCG2MgZw3H+1rhO3FOruK4ZXiYX7GDy2AbRbmIbmSvF5e03x5G0AMo5IK0Pr88vCafvjun+nVF7+m6fSM3v34zQLCzPTj69/R+d0rcrCM+zC9"
"7IN1BpW2JnrcshJzp2PTVkotIyKpg4Q0uQ81FPQ2SGFsZpqkz++OFm518OPbHJggCKQ4qfVTb5IgwJDsECKiiZcwsn6eayYA0CwNHLPktwio+mK2+TgME2"
"JoGBYoQZ3c003FKWnJNMjmqILXFYC+UHwQtpQReHZtVvezeHEWU1fkHrzHftXF2ta8mxHWgyFSZn9iJ27NSOhjiQdpHyzvZpqPJvY4ps6mSLpZfwhBtX8d"
"YmYi0U/4zERtvvLaVoi5gtaVh8uFf4Uk7Y3bQL1doKiU10CMXIFuyJfL++jaDyElgVymXVUpfpJK0850fXhNcn1LRCvzm6JzbCeaTi+J+S1dH97S6+/+mU"
"7PPie5XolP59akywHvzLJPrIGnVCOUZ/0qyGMkB0ubtJTO8+GW6kAL0js9PAtleWO+5tzmNZiW04Wa2+hHrME0hoWCma6XK03niabziebLhezrGc149PD5"
"dTGKTqcWaRH3VnVfIVPbTuDCscPSYeXm4jky8VXtndmHJ1O5WjyDpuKNbgOZZ6H5Oo8BlIim05mIeFl0JMt8XQBRgXIQLBEQMuBqHoveJDMIDJTR6Kq8CR"
"n8rfpv9Tu+qCeb1unJ+F36EUx0ayKJKoWIwoEJFe3DOl9Wb9clh3I56Pru5HNPssfaErLSTntpr1XxHDuVBrbUkwSPkhQ0de6ykZE5hf6Wf/FUy/qK+7RG"
"c5lCtfeilVgLOx91SruBO1D+R2gG/sLXJXLDuEdlp94I0fX6I83XtyaxVT9RVxhOz2h69ormy2u6Xh+I+A2dphf08uuviJjo+r16GkxhGTd4TcHBg+WVCJ"
"joTMUQJ1OnHdL7CIY1KuCTCsSFMg+1V+qo4RvoWdhCrgv9fJpJ3A0ns9DDmwc6vzjT6cXzBUyvc+J4EcpVLm17zETTyU81V8BhW5wDERjFYvgSBRPR9998"
"38pPfVHTS9eLSPdd2onwtADYfLnSrBvpEfCUe55oOi1fQH14+64ZAeBlE5mXqXnRqzVPHFeRir8TR+DgjWJY2D0z72DKA3XpsYHjn+jhWe/ovML+xBqvZ/"
"6N2kqqdEXflTAyiit6F91ldMoIDo/pVkztvWTXo8NFBuNj4F1kA2W1XF6vezRaeBdN2binQX85S5uP9BNJYJuIeOgPQ5Uc/h3jdKVo16qAq3pLUISBlew7"
"wsHCJfUiFIjpy4wDmpVXu8cN3vlYr+v1TTcnyvb1xkKBTpMpsun0nEiuNF/f0Hx9S0wn4umz1vmuj5y3rDb0pAOvKa6u7DxJoGnv2xMFucWL00LHXmm98B"
"a8UnZF7KFVPHKteWIQdiXSE2m8R87zTA8/vqPT+USnZ2fiMy0rLa+6GlbcQZXrEgWY2E9P0sMayOkH1YEIoAByvVIccSAv9TSJCGLzAGBCcr36nlIoLoDn"
"NC1HuIlvf5kbQwigwQul7J0SZRCNC4vjfGoG0bgylhNYIohyerwRJk5As+WNyjAnlMnxPfa7yhs1IxoeV6Dd70CRqkJlHTKwV7JY88BkIw3voFd/x2t8ba"
"fvNfYa8XHIeIUbHr8+Yjycr5cfvFqqdYjCIB8DDal/sYIJ62gR3mqlKqBaodgpaA3JFXTMYq+ACOu0ulIuhg6zwWB/C+CgjTx6zfOFrpc3REm2fHpOMr/z"
"hTFQxrIghtrxXbQsx2+afb6+IZEXxEQ0X9/QcuQWNSV/dh5YlSXH772ueaWWTi1k9Eqpq8OaV2pgimHsjj5b3fFzUXkjPJ5Og4t8/Jg354pJ2hwkEbZEiJ"
"s0AV0uV7per8v+0POyp9ScWzw6j4T4KkTXK818tbIVUCc7AakZJROZ3LqTI0PPAbBUcJr1yEQ/1SbhNKk1zHpUYot8zLPQ/PahO5ABARllGEO4+iyGcuOc"
"ag92CqJamxGIygBElWAFoj2YFN6oGgOU01IqP73nPjybxSwrz/BGyiiMA7rRwakLwXT74Cr0m5A/josyp/Su0vocp3S/stGwxfUWYMZXA7Ctyh4RG4TWx/"
"l6pX4OgNAp/lhCVPbBNNsV7+wBkO0YOlPOmzSGXKzwWjNSpcmgS+j5gETcU9orhwYAKbyLefyzbDNdLq/JzlF1FKVpek7X+UJ+dB601jQRnmBzvb5rc3m0"
"7PVsR6tdLz/SrNt1pjPx88/L4xC76qQPH4cIQgGsynqQKQAeLrwhonCIh7QtIxlMNRJhHvEeMBUphje6s8ApTHmGE4VE4jc1mZfTZK7zsl1lmohP7QzfMx"
"zk4XFNUsAh3UwfOQltbc9WIvnlpYaPHiaiYA2tJrOGeRX0C2AGbyjPe/qzuKDIAK2hAHqhynIHoubBcijL+0APomueaAQ+0VEV02UQ7Wi39wi8oVWs9ETT"
"20YwFXq7yof1cwrvCiKDZndjSVK7if3VNJJkrXXJ5/qSA6eOPfvKDsongau4bEYgGPiupl5yGmzTkk79njbyaF2TeUG0k8bo71lvuvEKCmUnThLSGv2lVX"
"oSPmDbf+6GArCNgF6ImkfJZbn+zAda73U3pS4xDQxNAyk83quq75asKNG+Xt404IvvpumZfcRbzxiFaT6i0xTmsE7nlyTzuc2xLqd6TDzRdPqsHfK97HFd"
"TmVJc6X6HUUAqtypdEN3BEkN8TZQIj3DVAK/a2AqNm87BtPQEqmRHUyJ7IsS2c3QL8sE73RJzxw9Ff3MWlAVEE8WpmWP7dVD5nocoB9Yr3/1yzpp+01TRs"
"MV4wlgvY8OojbSvv3ZTjbC4wID4LU6mQwCqOpffN7Pr3bh3xGAghLeCuW6/LkHm00Q9d62F0Ql50/KPXqKIA+UVZap9r9AZ3wF7x4AMQCkLdbyyICBNsob"
"5URUyKsyFHqZkn75h2mZ5hCXhWtCNqVsRpHyxXE+Ocszl4/lUvlci46SHHrJxRqC8uJkxNXEhk/Pq0HiKkfhyd01vJuS1jn7p+5Np3BiybNrCVXH2nn048"
"fLYikBh0WaMRW7gB06rv6LhupskQccYq6yyouOGnNMS0h3vryl0C6LZUDT6VnLMfVtIYtXpB7GEio80zSdSWSZ99K80+mFKXTks1t4pN9RRDAtQry996mf"
"MlKP4jYwVeHYXCY0vUJOGGAIpo1VnbeVlMz8M+w3rUICPFjdJX9cPH4QAE6S7J+Rl6Fn7uJv6y+s7YFpsE9DJQDQFeCsTwNqSEqPWZdsahTGsRTfeT8MSh"
"rBViIvKO8IhOhFrQGo9y/CMhnKq+ibSUhQjhPNIFqC8AEQldS3kHdsA5TPcj5uD5L+W6AuybBJPFRH3WHekq/clYJ88r04LQzfG19JNtC28JqotbWDq40i"
"f89K2xVbqGPHc79KJrIvxbt190ZWDB/V3xVaWmhXFQx6AWuepYINQUOGBR89i3UlAES8+uPKchpstsK4vXNRKfBhc8UhsYTsonhVb6Gytd8cZ/48tOgrlP"
"GIOE3jX/tQsHUFOdl5l8uz6+XNsvCL/GsSTIsnKXxaOG9en4jPpYkQyTS1Dyc3j6h5jMRnEnq7eLHcvoBi4AjLhgAwPRqhB7p3WAXvyajkT0/FYxrj3DJb"
"n4k0lnfuGeKmH5V5Xmik9ELbYbrc3mZESwmCyz30J0jTf73FEzktDjTVGPMi4jwopx+M/xbhIoyoFKOEyisoSinB05SnxDQImMGjkR6MKy8ov58hX5c3eQ"
"dWoji9QDsDJNAMqjTwksJ7MgItk1aRDsKodg/Q0+oY86GsOBkCaUEYV/xIlEEld5xXRnor9RunkZVyEHSkq0toAziYoo4CpHwGxEyoRczgZtfMec8IrmZe"
"0jP17UmpRK+7bKSr6JwlS5ISWK687y/p86e3TEkBt0ICfAooTxYASO8BrjzhncvSQN5BEMERm8aWd2B2SMtFU2Va2IQOGV4OfCQaQhbMRFfgUehKcn0Hdf"
"U5ZKLT8iFknkmE6SqYZpHZTBNdLm2zvfIlRMInmoXp2hZmX2U56tB4AkNG9xSv1tdxzdokyBiNIyFimgsjRKA8yBfa1sEOy0AeJOereCGiCQ63Y+r7TOwj"
"3lnsJKeQxg0jLZeRb694AF3C/CaTOEoioEp8tudCzRme6U916YrXQam5VVyFD/GdeywZnFAxYnqiWacNOgDkANisJz8peGRgT3VY0sQynQ8uQasDSTAMHB"
"TnWH5YBCUFzSy7zDOn+whEZkwE0Mzecgx7ax3CPWUaFSC6vs48tWa1Dw4EMMmyrOqr7y0tl3ygDz5sS8lpob/ob+XX6DMcJgSaXMEVlMoSIYkRDagtwcbQ"
"rn/FfaQoITCDERg9XKdj3Smwzj0hEKrCVsUiynJkNdodljwooc4bDCDk++88MiCEnPT5e3smezIBJMvnCTgC/1AfqPuSJ865MZF9hSLS8RXE/kHvq3Usht"
"iqMNPD5UKX+RrPdqXlE2BXWUD2Ku3jBUTLlzzsu6VcyKe9F3gPIEl2D2kQ0FRWOp9MRTkAmF5nly2z+qs9oObvMhJVgCpE86X13QhO8bNt7HUxEPXGzZ6r"
"03KB4BGK3QldJmMJDDMIK4PmIRDFNmk3NUiSayaKSoqC4vJMlQKNv+PClWi9c0en+46o0YsgyABqkaWoTHtQNGhKi6nI54pJ00sEhVA/v8nfznR+aq+tAk"
"oPhUoJJKTDuTIcLA+ne6CT5FcZHDkPt/bo8oC1Gr17jnUiKXgdGRO44Cn/7d/1cs1GUu8pxjB6X3+7V1uROQC9/WUd0djPXBF4v2Y6y7ys8GT1CrVjoDIC"
"MzSAFvWeC/7uHG4DuGiHs/ev/hKqtYkQusvDpDXAVkVIoIG2hBsSHubtAc89EzQ8cOFLrm+4p+UrEKyyN54gHKxnEHeKeGFgnpguD+/o4fKwLG5pL5e53u"
"UjQpMIXeZL+0xcA5FZvS4Nz/LAK238SHye7MEeLCF6YMCcaSNNFj91SGWroxnqr6uO42lA5P0WjZd2EAEaRmT8svEXvUgBb91lZHU0L9XL9zrEyEOoa7AE"
"4NfEhFEXfBv6bPo8YVhIhW0i3ZPuRx3Szd5MpCWS0o0UaFG29RQ+oYmRFLUDVCwYtjMhEInf69x8fh94aLpjVckm+eAHyWPdBiDSASBDaupBVO/Bm+rkaN"
"6SrJQn3fPKCMrPJD9jpAZ8maucwLHoA6V89V02OLq8vZHXyyPnc9mO+qJkOatcC/n1kQnor8nrPV+vb4m7ZLU3iOCRAUlVvmcuLjnwDoAcF6IEkBIFpHgO"
"TQj7hb9xZW38GLA+6+eHvUroldbeW21ERGVYeWzSvlhffT+F24HlCyD4F0ECYAnRPE30cGlfHGmbIhlGwmJ1znS9Xmie2QEA6r5gtn/Jhky+C+NsEYfoNX"
"rbSRmi1UIY70kBUlsrBH4KOUIxCsbzXLcHF3lBqFZXX5nTAz/8o8cp9l4olAX9N6SDdwyMhO7ORKfzeTmWb57hXQWSLc/END070fzugSKW9iBKSVF0Sew+"
"ejm+gMhuuysCZk+/Byoh3b6VFdzqs8RjTRcVbQojglHbPo+7Ur60/ziUge83PR5NFRbkRDo254mKWQqZFrzEMuvFRTXv9bNAs3XoEWBHWjzwRrGdNkK6TI"
"kGJ3kVIdXwW9eN9HyU9cP3ZZ2qtoJ0ylP7e/bdbMiSEuG0cIR80zp0zE71p8EWwU86mvZXKISNERS1hC6fgamXhqG0ABRNkLgoCr2oDhBloJTFnHyg0YMn"
"eqpEA+9dRda+CekhUuCFoeFmDzsG4GnW6uVybfOnWDbZO24h3sWzbF0WPMZwrzVB+RrQthTqxYbVpmHYpHaJwyTbVZ289UFuC7v3OXTjGAyIDFaVp9y1HS"
"tvbjZ2i4cCUIL3jKFi5Q8rhLKAZ9NpmQO/Plzt2cjmtOt6pRMzCU00w1daqkuyVqCoKExRlRp2pLxqRYfPe+WDpfFQiUUeVxZ6ILgSh3ydpyeyCgwV35WX"
"XgFCfy9FPRJdzYMgkmh4Hh6X2xkQa8Aef/RyqFdGL39RvtLR70c8rnxZC/9WgJsjBv1WHuMJ3xYrv3M7I2jX8hzJUrp0ep3t/E946Pf1qETmHNiSd8cU6B"
"4BU/NckBcAQaKk+CTex+brvUtNF8v3NWD5lOChx8kDPliB1t1oTycB4Iho+SLO7N+4pFTGpKAnc/Nap5imlTHzRPO7C10FQpLWRkyv//R98yTPFGa0JNVF"
"gFfi6FG2XqzzemGRltFgMx6i/HJaCfeU5QXlmfzF00UPk9xLFeBDy3brsM6LfQtGkYeXE+jDvtLswYbDExG8C2TUvKfzmeTtQ3F+7zqgXt480PTiGV3npR"
"95DSkN36j4wpWAKgNgvsoQcP49AOFKMZWewirAjZRqLn9txWkhm8AzJ4DogSPLWrDEsPAqy0vAQwZ6Qe7q8bqrWstIimcDAEk/erB1hV55h1kSwznRwMe6"
"3OLiHaZxm1Xz/Sn9AOjW+KSNdxVoVu/OHu7sATVeXD5JQ9DfSPiT2XBgpDqUisybAuUM3vA3AHPcqN8BolA3R4n/VumJChCXPm2kEUO1IbwHNJcNKX5YPB"
"oSBMChFrV6gSoLXVU6M9PD9boAqaRQp/BydCALTRN1QBiNALjHdKG+4K2ZPNgBMhkL6PmrleUeb0uP4J46T4wySGyX1E6eB4A689OljV5vyIfdtwBWa2HO"
"MtMr7jlNUEt8Pi3H9L270p4rHl4/L1uopil4s16y81teBQhVCWQl7dCrTInXlNFyGwvYVHgdP+DF8Eq6pOQjP5zyjQAsgygqcQ51y4C6KQcDzczLNq0M7F"
"ttEQCP8zPnIRsKdfsNQHXUfmQmeU+j43/tVHcFby6fV/1hTU7lQraKLqQ9y4A902FCZr5XQIeOu3o+5p0mMO0AkzGfvyOiAFppEXG5qMebHAFEghAzsPTe"
"rQRa1ZYYoj5PTqdKO4CHKuoMLkIk4mFd8wAZeYjhi1SaeWkzT3SZZ7oSwcIelHWSl4JXBfCJx1D35LmFcC4+y7QgTFDLEuhA50EQjWCufznSCnzJrrRhAZ"
"RACo689gYGvBBom2wAwE0wGJjpNE308O7S5kaLEcZOv7r47YWmF89oZoZD7j39GCQH72XlHT7L20zwveSaZoVeh4KrcvuVqFzWLSv5dbqLYug9kA6Wraoj"
"fktQSs86/rp3wJbdbwNvBQj6PLTLgFfji2kYRQhh3zUeWoruAIVEV4CWtWeZXsp6dEaG0uKep7C+pgJT3pJvMrYG78+24i85nBwySvc+XAFhY8Gjb1Tau/"
"wMiyxAWCVbgaNifg8YAoKEkFwFGuSAimuLzWMJ5cLq5cJbtXumQknrNdvikmAHqQ5tS7NZxL9t2QDQmqXRn2n5buYsCRQ4NkD0pAEAcKWp/jXw8gp1c7jR"
"kunBF7DAPUouZdKt1M35sSjoc50xU7SBjhKuaOQuDp61JwP5hAESZUY8F+1cf7J8Ok/gjUpkJKNAeI50ZuK3DzSdz3R9uAxW8W5fuTh9OqJWeQD4IEPlLu"
"CsgKN7N1p4UgNYVSaOtXIla3qH+UfGQclTps1VeVJ6gWvp8PlqeV2dkxzSFpflZzTmUOZr5Y/miLt0zTvK85u9XEfh3Ew/G1qJHxg3a0ZM1a67jBiyIwLJ"
"lJwCSAA1cq8nqwIEOPQul9RsefMxckEs7KoQdXIHpqlW5UlK5agHC517CS5YIxA2bvXgoO5tECzv0tm6QvEe/wr5+bGokq1DRbY93+KN+vJ+Q9fYqELE00"
"QzMV0u7WQjK6QZErIoPgU+/DyZGxaSnrmcSUWnwAIoGQFROi8OwbwDNyrSpvclKEP7oecZ6aV6JBmr/EN6jrQjPene9/Cd6FCVzomfiOjy9qF5kvmSOCB6"
"f9p/zlc60UQzn9rCI67pbVwR4DAaspYuvunSVoAZ3gUTssuT85VewlpZA3BZU6Cdt9qI18ZAmg9NvKDdI3MuC8Fx9M3U/O+AfymaZAD8o9Xb8YCNCKJRVt"
"WK6F42Y7AvliN2bcddP8tlhD7TtUsBrivvvC3GkZSynQk90vYkglM/fMIh4oPK6YrWzioqxvYCGNIlCeCJHh0BaDWU6sK7YfO/PneRZ2DGs16zp6bKc6EL"
"YJ8UaQxf9mHjRZGHj4O1M/Glc/iD4gY+9cPIk3WE6HnOQnS9zrZqF6MBTNLmSMnPjZUCAqDO8RnIR/CZjNR76N45DFzSDfn6sG7nIboV1pWfwbXkLeezud"
"o6D3a0qm+FdLr9iEbX0naXt+/24FvIM7oeru3r323PbFZug1rVJQ2QstMNK0quyjhMY00ru+jJgKY+6YGYC6CpFy3hzyo0G1pcskckkS9tg7TdBmnnGo+B"
"ZZSv0LWBX8wj/maDTrXntZfPOI3fs1ETWJzSHcsjFMOvHY+QXkZRCcijtKR4V9Ul1bvjrZJN4+lcWZBrl37aSAsZDUkFVC0se62Y31LBwA/zlTPZBuHKe3"
"ERw2+hYrFLnYdBujyiB+k4vehWwFJ1aAF4duDpivjRdVW59q2MtmI3yA3qMk1MVxK6XK90pWq+T7QBfP40168COeSpMGpqYKl+K4gXaZJlxlDYKl3ddNd5"
"a7EtiVdoBD4iUtZGRn/lACePE6w98nfbOFdfa27cgANZGcSy8XAN3LZobbG6DbhVen1ZeCgB4PYoynG9hmHfgi5sUy54ogGdjeeB5+BP9WUJ5RQ1fej6kt"
"OK9+xIKwF/wSzSksTQSBbVKMordNdlknntwXck6036cI+1P8+mMPrRlE/ywUJ5ONp1ZWkG0/hbq+qdVGIIECiaJ5I9U0iLujgDTTVvqs8zIHVno1JenRk9"
"JqTV8c1VPvV6oBulCd+s7IMV6cIMdSae6EpEl1n8E2uc6SzXBI2Ap+z0c5W5S4OXWAEN8FfJIT8PeZh68IYHYdsLpgr5cA8xlLQB9vXQq/Fl77MA3rIzT+"
"L3aa8VRFx5vO4Tr4FelrgM81TPa1BHfeJmZ01jvMWn9n56GkPaNoQLD6vjQlkfyEMGssmACMz0/ObwOKTLdeQI9thexmseSgnsgtFQ1UnTcMpXGTbmRUpJ"
"axj+ze3FgDgrdTPKHPv2SO6hvpD+3O0hg0sBSKj3YOJKuuwpRre7CgGHMCGmbXHOnM892uTF4vsCMC0UXIS5cNFNELHUnlBc/OIcxjBxpL/IcRz61HBtGW"
"okDcnOtBwzD8AqlACY6fLugS6Xi58ZIzG8W4XEzYjJ4VkQoL/RD427MLc9UYry5Dj8grdLab9l8ipLoOvAV/OOhlkRrq1s4NA3Yi8Z2ZA5f50bnqxg0q1O"
"6ZFLNl9I8Up20qqVOfbFXbwU7EiRIPMlO/OXYIP8d/kG4DX0KpMahvJ6YEzv9nhJJbCPPa9x/TOY4/M6pLpOG+tQf0gbvdXMz0i+fdkDD7d6VgElIX/Spa"
"1BtE0ToKH/8y9/JZ0CxM6VLP7ITh0+zb+re304DBNWK0iTdvLnnMkWeWmov4KHJYPnFb1R3bo8PMgjJO183ZzW6PBE03Qm687wbdNQJDNN5xPJRVd/JnkS"
"kbTTb3g6dzLxnwWvo9DkwIOu2yK1UleHuOinKjd0bWyv1dBp4VVz3R9Lo2rM8KonOgLC9wGQ975k67ls5FkFzeGnzMflC5rQI88Z06Z3FWhVMYpOeY88QK"
"CFwNR5VTX9/e9S+cnjr324gQy639njG+2fHBkGOd163bJTVT0f8T/+XfNWmtTZgeTEe3H4+2ITKIhCRiI6+x5Go0+L0gMuBiFCoF4WimWBo014CABOLkfP"
"NOd1VVt7pyl/oouC5SI9SREKbV5f2NMamoZAv/a8USpH2yGmnmiW2WiF9MrTNPkHsrVeqVwiostMxNOpPYVOJUIkHvKdMNZqnpeYIOKRdlwADPCXKjgG0u"
"yBIveS6p41WH+kXvAYC+84mC8ZANPg5z5zYU5QaNTKeOr4GlxcEjh+jUhsAdPuS9ZprYHlHj4CLIw8kWGtC1Ab8NsBTNBZceFQplxB4Rig1oHO3mUvdVBX"
"if8UabkoR1LepKKHoDT+SHZ34tNAvrVHy4U8JOKU0DBCsccbL2W8ll9SPbjvR6E+mFcXjaVOc64t+r575PBsYb/VgFbQCTagbJSTQgMhfxGuVQU6GsRr74"
"ayEBf2+LX0JCojQdwoWE4kmmj5jM+VSAoaQkTT8k09ZvYtObwIIJ8fqwwpeFqhMvsZmsbMQgct5yXJHEEDv6FKyVgLEQsulfu6pxdlvxrJkF62Ff26jA3e"
"ZItmhuns7o95KSnJ8M3mo0ddsvvhxpuVNKaIeJhyv8cKz4YeHg/zEaSJuk9qRTk4ZGLEW30a0wjgiq1B8GPTCOhAUHanXTUSBsC3RR+fjQ2CIhTMKe+GEV"
"aVgaC8XvdiPjTTXul/Pd1aJmdJ5jkecu6sZA+rngOtYoAjMK3eoWUQD2uQGpSAP0xrHqY+7zzKmC+8EwAL9JLQg+rCdzh8omc6Eo2diLF73roAAA5wSURB"
"VNTCt0TTAnbzHOgx6/+8fGZrmoimiab2e5rY5mBFD22Y5+Wv0psXatcgMEVV8gqzUG7YSkVx6mkMaLQJpDzCBh56c1zcDFfQVgQkDqfO29zlTRZ1K6yyo7"
"hXGaRbj576OlrkGJvXQanMn27WPDYvRsY0pabV06y5zIp2C+zytiG5maYkmjxMn8F8i88uPydaPbkNwJFudJUgyfV8aCnHkYz3AHiuT3u5Vu9Kdp0MeVCe"
"EJ3xIAElHkN9XBCLx/Bla25TnYSK9iG6/LwDxkTG7jm+w4pWIFrls/eS08GBCp334ecVa2GdspYCUODZApQnC81avSamubmSLMu2mmmWxUMVIZnjIQKzCO"
"mHi2WW5cv2ja2r1gt7Sv3ZiSCDQdMNQa0Ew9xYtA4iPKDEG/dd2/AovYzzVOHgok67QPQ9OJlPee1xYB/jtW6B7aj8tTI7hVnaJ+tcl0C023Pe4Ke4qaSw"
"DqD9qT/2kzPdHXyk+m2ltfJbgeP01elEK2UMeDjC+670daUOGA+xduf8kqVvsK1bLogzj9PkFa6VZ9o9B+fJv1AeuxnSjWALoeEUVrC50Mwj5s/zdzJ655"
"nRLuswhDENtZXK7lnqd0CXj3kTzfp9ylmIefkUGj3gh6axLgI8ioOlLEaAMC0AS2LPvYdL5H8LTNA44BH0LQkD2Hbh6FEZfU/vwL2S/1r6lngNzPqPa9cF"
"7gLEom8dvu6FvI/0bvdml/GP3fRkJcEm6Mp22m1aMlB9gx664YHvAfcyrWx50lUeqENZXo4zPTLkmfMXPlhQMwfq0oPrCJ2q2FlaDT2YC93DU5kGnYIvP/"
"9F5RHT6NmeNLvzbs2JHaB1C+3yfkUx9+nXPSafR+P1ejHQwzlQAm8zPAePrVTo0mkV/8C3mh8JPDPP3Zzghrzx3U4vbNtrXCljFCDmtTybRDfTHMI0viHP"
"R35tYON6ntH7HbTkYN69x8xUnvJu7/ioZzy4yyC37v2N6HlMbEtu2IZ7AMOfCeThjfSr7tjQS93kmaiUxWoZK3TLdDfQPldenF68guAoy8orrejlC8/gxS"
"IG01xjjzFNKOv8ZD7YXjtAEWUMc6QjhboGJhVNK0ukzI+VsQMQ0MsrgLIEqeBmb3TzLhwZPVr/UfHMq2BThejtOe9LvxUeXR+++pjXgX5U2I3e48rZJOv5"
"DpbzPq9bndej5+XLxottoDoQot2hIFefHwAel8UAUAqvcckjJcUiNpOerwEXD2jsAyh/vI/Ocs8b72lLNcGzupa1Vt1nTKw9H/K7cn9GMByGIsmVYQajnn"
"B/zq6GY0vgStcQgAcTw/ZgzyRWeyYrJAHPuqKl7J7rrC73Vb4EUYmpESiV9A/Mw+2KKECMgouGxvB6T4OrP6kePATM0YreITSWofV1ZF6TbZduD9ptGQI7"
"rw8JrI+J+h7Nuxdst0CWiPrw4UrG8WESXOqUoQdZlCEySIe/DnqwazKQwTjZCyLdO6FORy+l1BrjCK/53R6Pb/OZ4VYfny2i2MemCrh4vhpiZ+IvXv28k9"
"Vjwre309keXfub9BhfIy8nztvyAeDsb/bKYj2EOgCpHfUuQUHWweJwm3MBqGv5BvOqHP/ZLjekGbjMefQyHwauLW/5htc/yUtueHkEbEtA5DHhzdDxlrIN"
"tPYEKhO5FQYOe0PF/OJWPbfCt2O/tX62maacWxx7qzXdIj0P0nflVZNZ66HkPfW5Sb7tOi9yjkfEBXcaK9HpKI3du8+W9vp3MvJOEptgVAlUzFXYt/KkFw"
"MzqfSBFYuvuKtdzC91dn9eWjJ9/TqP3+Ra7X4sLqll1j0LIVHuemYIZw9lzyvv0mvZBo6Aj4VpGr3FlfdrRsWgrYdp9vD8BGn/XC7ZfLDn1ToCVx5fdzfo"
"M4eUan5XotpG3s2w9PYMblVuZRCslZNBbpxehnpgd5h0EGYY5y/asSo/KF0F33W02DO0K9ms9ZxhaF2ng169/LILTI08nQwYsYg1tXujh/jIkOUm/fJ5r3"
"mPlrlZ3g0eJdF4Lq4MFR/hmW/Mt/s9r/J/pJwyzQ2LlTzt+gqjYa++ETF/ikB7OOy7B/RGT3d4mKs0d8y73VK/EZAfobMrfLmT/5Es1+dr9/M89gb31feI"
"V7vp8T+izOHzZqisecRLsvURa6HdCKSrNIfX7cp/z92OcvYsgNnxbjjrdzD0OXq3BgD3C6fzbmC5CbRG72+Q0V7Z9I+3qe4qlx8HbLt67bAtPjZI3QEQsi"
"/99tsd+XYQ2E4i6+nuDbqbnujj3pfgJLe1w23h29tp2/NBIbs9363nO+dfx+9kMx+eR2y/PgMgfZzXsZJml8czpr4bsHaUs87DI5/ztpqPwYdttTqaw7yb"
"55jDv90l6/nTm80+cgC8bkp3J6/3prrdRvxglq3Ut0DYtvJYLXejyGOzVzfUZieA3cLBsTnQfeHNanLoKIjteV8D7960rqO2QU128/yYRUqr7x5R/vL8qC"
"GZpPTZy6+GFO4BrGvpbgXd+v3aXYqI80qeYiL2nqC8FS4+RjMp+AHfcojmyvPNEOj6dRsw3mZg3ZznkZ71p6u+blmgtPP1DeXtMRyOl3+kjreGKHmUpjMm"
"9pkvu57fGhbe87xVagTkPfCvzYNuRCB21u+WtuGXn33lWznXFp0c1CR3AYrDSm1tZe2ocpBHyoz3BVIaA9+hvDvrvZrvDgbD0TQ5w729U0u7O8P7Dbb+FA"
"H51jDtbfR3lvYBvdBbaR9S0DeHnfcvttoT8j6cd0+egZ61V8OFSzx4Pi71MXU88o5famgXPcG13DdsG0jkD73blW6HQn4akOBVUHwM+DxmQc3xcnkfnUes"
"uXn0nPtOAk/ipd7p2rPY6mO9jh60cHM5T5XnoFd193S7gfE+3rLseHgLSO6hfxTQ78rTI9p5a7Z5beGRAynRStxgPB84fD9kc+884vq1V9k+ZqHNY/OPgX"
"/fnFtcmL0T7NrDe3uQ+3jFVLKZ6UCNDvNb5n8EgZ8wBn4U12Nx+LFAfnRJzmZx3Yef5WbP5tb0e7esHKU/mvu8P/075r/Bg99XTr+epbr4RVq1+yQexh29"
"ib3+MO94+T5CmY+eZ74Dr5vByxtWr94ThG/Ke5cdKXfoS5+uQ9c+BXenBUmP9EIfk/dm4NyozF0WH+1ePPXh0uwJr+8r4449TsbpztWynD2KY286Igqbat"
"fyhJjzI9JgujKNwIR1CVSV+NeORRzzNJyc3ksDwyWDhVirkXja7iQsxwb+cgbzfhB6kqjgQUt57/z7zqJuJfVnf92nrflmT+h98LhXNd/iRe9ZNX0TSN1g"
"VNwXMLc04Qjoj8+g79lncGjOfEfi81N4oMN8Oxjau5XhccoLzIC0aIFX8khbxbK2OEl2eJBb5a0ZpbyVho6B7EBnlbSM5s7VGPtBtAh1x+bZTWlAbbdiOS"
"KHG0n9xV73ldP+FZrLxVX2gsY2xScH4gOG7T1XHd9Ec2vhENEKEq2twK3f7HIMdtSDh+0/preW/tx/27y+y5l3e6MH820tu14VUgFiexppN/A0ANmzzH33"
"rOYTuDNHogrK754IAPK8m/567UPKPUvu13hdobZ63VqXm4nd/dqjOp7wulOxT8H9LYbYPXjaE0Z9bDnvZ8XywdWwg5d7qDwK8B8Zrn5s3jO6RlW1DilYWg"
"fjLcb2BJlXgXnHkvh1r2c9zz0slyzXI+EfJjKveOtapLffjNk3pb5TECsV2Bua31v8PXDraHVWy/wgWPZx+sQfiqt7lLtXSd+Dj+M+8J5PfEDeg1M3t1T5"
"7qC+Q5c/rgzHl+Nl9Eh5LujuYmofwO6JVq+VtZ1/G9xWPOs9q5M12Z09jSNzzCHPzrAqptqcXeG1POu0RxfvyLRuqt2Hj4rQY5vy44StP8/rqGd3iOYNb2"
"9LuTP/DoJHoy238vkkQHqnxV97FxBtl3G/tj7fqlWCwt0ZG60A6Ujx+0OFmGd3kHWL/S7j3oVFo/IOLfA5mGdvuHp0HQb6BFS3d9Gx4XMvW+YpaH667net"
"Tdvcjd7BFLtyPjaMe8cFcPer4SPKu3U+cLW8e5lBB2jsUOznavXlYXDbdnSGTO0FrqMh5j283AJkmPFo2bc0si6ckf7h6nUoVHsPGhuT93cPwd5pqvme3u"
"VfKih/LB767QvK9rXckyjpp6RRCOR9A+o9674ngrVZ9lPM5wuGdgeJ7qGI8bqJntTe7BF6N4Ui75T/ZsC2vLdYH4VnuHN+dUzjloB0ZPluYLMikrsC2hN6"
"CX+R13sL0X4s5eyeYFmncZsKeA/1uw+dJ6/PE89zn7cS3Nu7eBSg7V4NVhN8KrA7mn+N7r3olDwdmF/N9TqkBlYE/Yhoz2GejzK4J4Ly6br/9fTBunvyc2"
"vix3uDj+LzPdJ6cv7uzOw9yJ1v2aCyDa63q8eji5uOEDy6mvcoj4/l+16rRx/bMR5tKNzqMR+p5KOuOkg0P2WRn67uWotSPCl87l7U8/5k8BdJd6du/ih5"
"T1TPoxePY7Km8ZSe7Oi6KfQr+3PfFBK+QRDvU9ncPQy7Uc5qfZ9whMnB57dcf26g/LF7Lj+Fcj9U2TeVeTDTsf0E77Eej7w2V+3iYqMPqTifqvzbF/g8Hf"
"2tTE8PmvVk82NWFd/7+nNZWfspMvxxXR9re9T7yZ+e2w8jj1v3b368cjjzTnT7qSkznCf8WHn/cHzVk80fK3jdY7vAHbJ9uj7A9T68qJ9U3e58PdX00GPK"
"XrtUn9+bv8fqhM3FRmtbPfZ+ueRDXDL4/TGx+dTh6g/B23utwxOvxPt0/WVffw795KdWhz38fvA6FQzcfCDD3gp91Nb/ziMCD5B6L9eH6kj3rOMHHwyfrk"
"/Xe7r+Evr63ev4EQtNCkV4fmqO77US9fh14NSC1Zfvb7P2EwjhruQ+4r796fp0/UVcfx6Ada/ljI9n/l7V//8B1DOHeDmCwUoAAAAASUVORK5CYII="
)
REC_STATE  = {"rec": False, "text": "Idle", "game": None}   # 실시간 녹화 상태(웹 표시용)
for d in (DATA_DIR, UPLOAD_DIR, REC_DIR): os.makedirs(d, exist_ok=True)
FFMPEG = None
CFG    = {}

import queue as _queue
GUI_Q = _queue.Queue(maxsize=4000)
REC_STATE = {"recording": False, "encoder": "", "ready": False}
LAST_ERR = {"msg": "", "t": 0.0}
_LOGFILE = {"p": None}
def log(m):
    line = f"[{datetime.datetime.now():%H:%M:%S}] {m}"
    try: print(line, flush=True)
    except Exception: pass
    s = str(m)
    if any(k in s for k in ("error", "Error", "failed", "Failed", "exception", "Traceback")) and ("restarting" not in s):
        LAST_ERR["msg"] = s[:240]; LAST_ERR["t"] = time.time()
    if "Recording started" in s:
        REC_STATE["recording"] = True
        if "WGC" in s: REC_STATE["capture"] = "WGC"
        elif "gdigrab" in s: REC_STATE["capture"] = "GDI"
        elif "Monitor" in s: REC_STATE["capture"] = "DXGI"
    elif ("Recording stopped" in s) or ("Idle" in s) or ("Game ended" in s): REC_STATE["recording"] = False
    if "Ready." in s: REC_STATE["ready"] = True
    if s.startswith("Encoder:"): REC_STATE["encoder"] = s.split("Encoder:", 1)[1].strip()
    if "Upload complete" in s: REC_STATE["uploaded_at"] = time.time()
    try: GUI_Q.put_nowait(line)
    except Exception: pass
    try:
        if _LOGFILE["p"]:
            with open(_LOGFILE["p"], "a", encoding="utf-8", errors="replace") as f: f.write(line + "\n")
    except Exception: pass

# ===================== 크래시 로그 (전역 excepthook) =====================
CRASH_LOG = os.path.join(DATA_DIR, "crash.log")

def _write_crash(exc_type, exc, tb, where="main"):
    try:
        txt = "".join(traceback.format_exception(exc_type, exc, tb))
    except Exception:
        txt = "%s: %s" % (getattr(exc_type, "__name__", "?"), exc)
    try:
        try:   # 1MB 넘으면 뒤 절반만 남김(무한 성장 방지)
            if os.path.isfile(CRASH_LOG) and os.path.getsize(CRASH_LOG) > 1048576:
                _old = open(CRASH_LOG, encoding="utf-8", errors="replace").read()
                open(CRASH_LOG, "w", encoding="utf-8").write(_old[-524288:])
        except Exception: pass
        with open(CRASH_LOG, "a", encoding="utf-8", errors="replace") as f:
            f.write("\n===== %s | %s | thread=%s =====\n%s" %
                    (datetime.datetime.now().isoformat(timespec="seconds"), APP_VERSION, where, txt))
    except Exception:
        pass
    try:
        log("CRASH (%s): %s - details saved to crash.log" % (where, getattr(exc_type, "__name__", "?")))
    except Exception:
        pass

def _hook_main(exc_type, exc, tb):
    _write_crash(exc_type, exc, tb, "main")
    try: sys.__excepthook__(exc_type, exc, tb)
    except Exception: pass

def _hook_thread(args):
    _write_crash(args.exc_type, args.exc_value, args.exc_traceback,
                 getattr(getattr(args, "thread", None), "name", "thread"))

sys.excepthook = _hook_main
try: threading.excepthook = _hook_thread   # 데몬 스레드가 조용히 죽는 것 방지 — 전부 crash.log 에 남김
except Exception: pass

# ===================== 1. 설정 (자동 생성/탐지) =====================

def free_port(pref=8000):
    s = socket.socket()
    try:
        s.bind(("0.0.0.0", pref)); s.close(); return pref
    except OSError:
        s2 = socket.socket(); s2.bind(("0.0.0.0", 0)); p = s2.getsockname()[1]; s2.close(); return p

def _screen_size():
    """기본 모니터 작업영역(작업표시줄 제외) (left, top, width, height) px. 실패하면 None."""
    try:
        import ctypes
        u = ctypes.windll.user32
        class _R(ctypes.Structure):
            _fields_ = [("l", ctypes.c_long), ("t", ctypes.c_long), ("r", ctypes.c_long), ("b", ctypes.c_long)]
        rc = _R()
        if u.SystemParametersInfoW(0x0030, 0, ctypes.byref(rc), 0):   # SPI_GETWORKAREA
            return (rc.l, rc.t, rc.r - rc.l, rc.b - rc.t)
        return (0, 0, u.GetSystemMetrics(0), u.GetSystemMetrics(1))
    except Exception:
        return None

def open_app(url):
    """주소창·탭 없는 단독 앱 창으로 갤러리를 연다 (Edge/Chrome --app 모드).
    윈도우엔 Edge 가 항상 깔려 있어 별도 설치 불필요. 못 찾으면 일반 브라우저로 폴백."""
    try:
        cand = []
        for _n in ("msedge", "chrome"):
            _p = shutil.which(_n)
            if _p: cand.append(_p)
        for _base in (os.environ.get("ProgramFiles", r"C:\Program Files"),
                      os.environ.get("ProgramFiles(x86)", r"C:\Program Files (x86)")):
            cand.append(os.path.join(_base, "Microsoft", "Edge", "Application", "msedge.exe"))
            cand.append(os.path.join(_base, "Google", "Chrome", "Application", "chrome.exe"))
        _la = os.environ.get("LOCALAPPDATA", "")
        if _la: cand.append(os.path.join(_la, "Google", "Chrome", "Application", "chrome.exe"))
        _prof = os.path.join(DATA_DIR, "appwin")
        try: os.makedirs(_prof, exist_ok=True)
        except Exception: pass
        for _exe in cand:
            if _exe and os.path.isfile(_exe):
                _args = [_exe, "--app=" + url, "--user-data-dir=" + _prof,
                         "--no-first-run", "--no-default-browser-check"]
                _wa = _screen_size()
                if _wa:
                    _wl, _wt, _ww, _wh = _wa
                    _W = min(1280, _ww)                       # 콘텐츠(최대 1160px)에 맞춘 너비
                    _X = _wl + max(0, (_ww - _W) // 2)        # 가로 중앙
                    _args += ["--window-size=%d,%d" % (_W, _wh),   # 세로는 작업영역 꽉
                              "--window-position=%d,%d" % (_X, _wt)]
                else:
                    _args.append("--start-maximized")         # 화면 크기 못 읽으면 최대화로 폴백
                subprocess.Popen(_args)
                return True
    except Exception:
        pass
    try: webbrowser.open(url)
    except Exception: pass
    return False

def _atomic_write_json(path, data):
    """JSON을 원자적으로 저장 — 쓰는 도중 앱이 종료/크래시/정전이 나도 원본 파일이 깨지지 않는다.
    임시파일에 완전히 쓰고 fsync 로 디스크에 확정한 뒤 os.replace 로 통째 교체(전부 or 전무)."""
    tmp = path + ".tmp"
    with open(tmp, "w", encoding="utf-8") as f:
        json.dump(data, f, ensure_ascii=False, indent=2)
        f.flush()
        try: os.fsync(f.fileno())
        except Exception: pass
    os.replace(tmp, path)


def load_or_make_config():
    cfg = None
    if os.path.isfile(CONFIG_PATH):
        try:
            cfg = json.load(open(CONFIG_PATH, encoding="utf-8"))
            if not isinstance(cfg, dict):
                raise ValueError("config.json is not a JSON object")
        except Exception as e:
            # config.json 손상 → 망가진 파일은 .broken 으로 백업하고 기본값으로 재생성 (앱이 안 켜지는 것 방지)
            try: os.replace(CONFIG_PATH, CONFIG_PATH + ".broken")
            except Exception:
                try: os.remove(CONFIG_PATH)
                except Exception: pass
            log(f"config.json was corrupt — recreating with defaults (old file backed up as config.json.broken): {e}")
            cfg = None
    if cfg is None:
        cfg = {
            "mode": "all",
            "league_process": "League of Legends.exe",
            "proxy_url": "",
            "platform": "kr",
            "username": "",
            "supabase": {"url": "", "anon_key": "", "service_key": "", "bucket": "media"},
            "data_dir": "",
            "gallery_url": "",
            "encoder": "auto",   # auto | nvenc | x264
            "ui": "window",      # window | console  (window=보기 좋은 상태창, console=검은 cmd창)
            "scale": "auto",     # auto | source | 1080 | 720 | 480  (auto: NVENC면 1080p, 소프트웨어면 720p로 낮춰 끊김 방지. 원본 그대로는 "source")
            "preset": "auto",    # auto | ultrafast | superfast | veryfast | fast ...  (libx264 속도/품질)
            "output_idx": "auto",   # auto | 0 | 1 | 2  (멀티모니터면 게임 있는 모니터 번호)
            "capture": "auto",   # auto | wgc | ddagrab | gdigrab   (wgc=OBS식, 전체화면도 잡힘)
            "postgame_tail": 6,  # (GameEnd 미감지 시 폴백) 프로세스 종료 후 결과 화면을 몇 초 더 녹화할지(초)
            "result_tail": 8,  # 승리/패배(GameEnd) 화면을 몇 초 담고 정지할지(초) - 끝나고 늘어지는 것 방지
            "audio": "system",   # system = 시스템 전체 소리 | game-only = 롤 소리만(실험적, 실패 시 시스템으로 폴백)
            "port": free_port(8000),
            "fps": FPS,
            "poll_seconds": 4,
            "min_game_sec": 300,
            "keep_games": 20,    # 원본 자동 정리: 최근 N판만 보관 (0 = 무제한)
            "keep_gb": 30,       # 원본 총 용량 상한(GB). 초과분은 오래된 판부터 정리 (0 = 무제한)
        }
        _atomic_write_json(CONFIG_PATH, cfg)
        log(f"Config created → {CONFIG_PATH}")
    # service_key 영구보관: 한 번 넣으면 data 폴더에 저장 → 이후 zip 통째로 덮어써도 유지
    try:
        _sk = ((cfg.get("supabase") or {}).get("service_key") or "").strip()
        _secret = os.path.join(DATA_DIR, "penta_secret.json")
        if _sk:
            _atomic_write_json(_secret, {"service_key": _sk})
        elif os.path.isfile(_secret):
            _v = (json.load(open(_secret, encoding="utf-8")) or {}).get("service_key") or ""
            if _v: cfg.setdefault("supabase", {})["service_key"] = _v
    except Exception: pass
    return cfg

# ===================== ffmpeg 자동 다운로드 =====================
def ensure_audio():
    """pyaudiowpatch(WASAPI 루프백) 준비 — 없으면 자동 설치. 실패해도 무음으로 진행(영상엔 영향 없음)."""
    try:
        import pyaudiowpatch  # noqa
        return True
    except Exception:
        pass
    try:
        log("Setting up audio engine (pyaudiowpatch, first run)…")
        _run([sys.executable, "-m", "pip", "install", "-q", "pyaudiowpatch", "--break-system-packages"], timeout=300)
        import pyaudiowpatch  # noqa
        log("Audio engine ready.")
        return True
    except Exception as e:
        log(f"  (audio) pyaudiowpatch install failed → recording without sound. Manual: pip install pyaudiowpatch ({e})")
        return False

def ensure_ffmpeg():
    local = os.path.join(HERE, "ffmpeg.exe")
    if os.path.isfile(local): return local
    found = shutil.which("ffmpeg")
    if found: return found
    log("Downloading ffmpeg… (~80MB, first run only, 1–3 min)")
    sources = [
        ("gyan-essentials", "https://www.gyan.dev/ffmpeg/builds/ffmpeg-release-essentials.zip"),
        ("BtbN", "https://github.com/BtbN/FFmpeg-Builds/releases/download/latest/ffmpeg-master-latest-win64-gpl.zip"),
        ("gyan-full", "https://www.gyan.dev/ffmpeg/builds/ffmpeg-release-full.zip"),
    ]
    def _fetch(url, timeout=180, tries=3):
        last = None
        for _i in range(tries):
            try:
                req = urllib.request.Request(url, headers={"User-Agent": "Mozilla/5.0"})
                with urllib.request.urlopen(req, timeout=timeout) as r:
                    total = int(r.headers.get("Content-Length") or 0)
                    buf = io.BytesIO(); got = 0
                    while True:
                        chunk = r.read(262144)
                        if not chunk: break
                        buf.write(chunk); got += len(chunk)
                    if total and got < int(total * 0.99):
                        raise IOError(f"Incomplete download {got}/{total} bytes")
                    return buf.getvalue()
            except Exception as e:
                last = e
                if _i < tries - 1:
                    log(f"    Retrying {_i+1}/{tries-1} ({type(e).__name__})…"); time.sleep(2.0 * (_i + 1))
        raise last
    # BtbN 최신 릴리스에서 win64-gpl 자산을 동적으로 찾아 추가 (파일명이 또 바뀌어도 대응)
    try:
        api = urllib.request.Request("https://api.github.com/repos/BtbN/FFmpeg-Builds/releases/latest",
                                     headers={"User-Agent": "Mozilla/5.0", "Accept": "application/vnd.github+json"})
        rel = json.loads(urllib.request.urlopen(api, timeout=30).read().decode("utf-8"))
        for a in rel.get("assets", []):
            n = a.get("name", "")
            if "win64-gpl" in n and n.endswith(".zip") and "shared" not in n and "lgpl" not in n:
                sources.append(("BtbN-api", a["browser_download_url"])); break
    except Exception:
        pass
    for label, url in sources:
        try:
            data = _fetch(url)
            z = zipfile.ZipFile(io.BytesIO(data))
            member = next(n for n in z.namelist() if n.lower().endswith("/bin/ffmpeg.exe"))
            with z.open(member) as src, open(local, "wb") as dst:
                shutil.copyfileobj(src, dst)
            try:
                pm = next((n for n in z.namelist() if n.lower().endswith("/bin/ffprobe.exe")), None)
                if pm:
                    with z.open(pm) as src, open(os.path.join(HERE, "ffprobe.exe"), "wb") as dst:
                        shutil.copyfileobj(src, dst)
            except Exception:
                pass
            log(f"ffmpeg ready. (source: {label})")
            return local
        except Exception as e:
            log(f"    {label} failed: {e} → trying next source")
    log("[!] ffmpeg auto-download failed on all sources. Please download manually:")
    log("    1) Download https://www.gyan.dev/ffmpeg/builds/ffmpeg-release-essentials.zip")
    log("    2) Unzip and find  bin\\ffmpeg.exe  inside")
    log(f"    3) Copy it to this folder:  {HERE}")
    log("    4) Run penta_recorder.exe again")
    return None


# API가 죽었을 때(레이트리밋/504) 폴백할 알려진 버전들


# ===================== 인게스트 (영상 등록) =====================
_CNT = {"n": 0, "t": 0.0, "busy": False}
def _sb_count_cached():
    now = time.time()
    if (now - _CNT["t"] > 15) and not _CNT["busy"]:
        _CNT["busy"] = True
        def _refresh():
            try: _CNT["n"] = sb_count_matches()
            except Exception: pass
            finally: _CNT["t"] = time.time(); _CNT["busy"] = False
        threading.Thread(target=_refresh, daemon=True).start()
    return _CNT["n"]
def count_matches():
    return _sb_count_cached()
# ===================== 코치 리포트 (규칙 기반, API 불필요) =====================


# ===================== Supabase 클라우드 (DB + Storage) =====================
# config 의 "supabase" 를 채우면 자동으로 켜짐. 비어 있으면 전부 로컬(SQLite)로 동작.
# Supabase 공개 기본값 — anon_key 는 공개돼도 안전(RLS 가 데이터 보호). config.json 이 없거나 비어 있어도 클라우드 모드로 동작.
PROXY_DEFAULT = "https://mypenta.netlify.app"   # Riot API 프록시(Netlify Function) — config.json 없어도 분석이 자동 연결되도록 기본값
SB_DEFAULTS = {
    "url": "https://bsrvmesrygbfeqicquvq.supabase.co",
    "anon_key": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImJzcnZtZXNyeWdiZmVxaWNxdXZxIiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODIzNjA2NjQsImV4cCI6MjA5NzkzNjY2NH0.PBnGgLxvMDOK_yUQxTH11EwizEz5oJ1OWp-9I5nG8Ug",
    "bucket": "media",
}
def sb_cfg():
    s = dict(CFG.get("supabase") or {})
    for _k in ("url", "anon_key", "bucket"):
        if not s.get(_k): s[_k] = SB_DEFAULTS[_k]
    return s
def sb_writable():
    """업로드(쓰기) 가능 = url + (service_role 또는 anon 키). anon은 RLS 정책 범위 내에서 쓰기."""
    s = sb_cfg(); return bool(s.get("url") and (s.get("service_key") or s.get("anon_key")))
def cloud_state():
    """('cloud'|'readonly'|'local')"""
    s = sb_cfg()
    if s.get("url") and (s.get("service_key") or s.get("anon_key")): return "cloud"
    return "local"
def _sb_base(): return (sb_cfg().get("url") or "").rstrip("/")
def _sb_key(write=False):
    s = sb_cfg()
    return (s.get("service_key") or s.get("anon_key")) if write else (s.get("anon_key") or s.get("service_key"))
def _sb_h(write=False, body_json=True):
    k = _sb_key(write); h = {"apikey": k}
    if k and k.startswith("eyJ"): h["Authorization"] = "Bearer " + k   # legacy(JWT) 키만 Authorization; 새 publishable/secret 키는 apikey만
    if body_json: h["Content-Type"] = "application/json"
    return h
def _sb_bucket(): return sb_cfg().get("bucket") or "media"
class _ProgressReader:
    """업로드되는 동안 read마다 진행률 콜백 호출. requests가 Content-Length를 잡도록 __len__ 제공."""
    def __init__(self, f, total, cb):
        self._f = f; self._total = total; self._read = 0; self._cb = cb
    def read(self, n=-1):
        chunk = self._f.read(n)
        if chunk:
            self._read += len(chunk)
            try: self._cb(self._read, self._total)
            except Exception: pass
        return chunk
    def __len__(self):
        return self._total

def _gallery_origin():
    return (CFG.get("gallery_url") or "https://mypenta.netlify.app").strip().rstrip("/")

def sb_upload(local, path, ctype, on_progress=None, ident=None):
    """Storage 업로드 v2 → 공개 URL.
    ident=(puuid, device_secret) 가 있으면 Netlify 서명 프록시(/api/storage)로 1회용 URL 을 받아 업로드
    — service_key 가 클라이언트에 전혀 필요 없는 경로. 서명 실패 시 레거시 직접 업로드로 폴백
    (anon 정책 차단 전까지 동작; security_pass.sql 적용 후엔 서명 경로만 유효)."""
    if ident and ident[0] and ident[1]:
        try:
            return _upload_signed(local, path, ctype, ident, on_progress)
        except Exception as e:
            log("Signed upload unavailable (%s) - falling back to direct upload." % e)
    return _upload_direct(local, path, ctype, on_progress)

def _upload_signed(local, path, ctype, ident, on_progress=None):
    """서명 업로드: 시도마다 새 1회용 URL 발급(서명 토큰은 단발성) → PUT. 일시 오류만 백오프 재시도."""
    import requests
    org = _gallery_origin()
    total = os.path.getsize(local)
    _RETRYABLE = (408, 429, 500, 502, 503, 504)
    last = None
    for attempt in range(3):
        if attempt:
            wait = 3 if attempt == 1 else 8
            log("Upload retry %d/2 in %ds... (%s)" % (attempt, wait, last))
            time.sleep(wait)
        try:
            r = requests.post(org + "/api/storage",
                              json={"action": "sign-upload", "puuid": ident[0], "secret": ident[1],
                                    "paths": [path]},
                              timeout=25)
            if r.status_code == 401:
                raise RuntimeError("sign 401: identity not registered or bad secret")
            if r.status_code != 200:
                if r.status_code in _RETRYABLE:
                    last = "sign %s" % r.status_code; continue
                raise RuntimeError("sign %s: %s" % (r.status_code, (r.text or "")[:160]))
            items = (r.json() or {}).get("items") or []
            if not items or not items[0].get("uploadUrl"):
                raise RuntimeError("sign: empty response")
            up = items[0]["uploadUrl"]; pub = items[0].get("publicUrl") or ""
            with open(local, "rb") as f:
                body = _ProgressReader(f, total, on_progress) if (on_progress and total) else f
                r2 = requests.put(up, data=body,
                                  headers={"Content-Type": ctype, "x-upsert": "true"},
                                  timeout=(10, 3600))
            if r2.status_code in (200, 201):
                return pub or ("%s/storage/v1/object/public/%s/%s" % (_sb_base(), _sb_bucket(), path))
            if r2.status_code not in _RETRYABLE:
                raise RuntimeError("put %s: %s" % (r2.status_code, (r2.text or "")[:160]))
            last = "put %s" % r2.status_code
        except requests.RequestException as e:
            last = e.__class__.__name__
    raise RuntimeError("signed upload failed after retries: %s" % last)

def _upload_direct(local, path, ctype, on_progress=None):
    """레거시 직접 업로드(anon/service 키). security_pass.sql 적용 후에는 차단됨 — 서명 경로가 표준."""
    import requests
    base = _sb_base(); bk = _sb_bucket()
    _hh = _sb_h(write=True, body_json=False); _hh["Content-Type"] = ctype; _hh["x-upsert"] = "true"
    total = os.path.getsize(local)
    _RETRYABLE = (408, 429, 500, 502, 503, 504)   # 일시 오류만 재시도(4xx 권한 문제는 즉시 실패)
    last_err = None
    for attempt in range(3):                      # 즉시 1회 + 백오프(3s, 8s) 2회
        if attempt:
            wait = 3 if attempt == 1 else 8
            log("Upload retry %d/2 in %ds... (%s)" % (attempt, wait, last_err))
            time.sleep(wait)
        try:
            with open(local, "rb") as f:          # 재시도마다 파일을 처음부터 다시 읽음
                body = _ProgressReader(f, total, on_progress) if (on_progress and total) else f
                r = requests.post("%s/storage/v1/object/%s/%s" % (base, bk, path), data=body,
                                  headers=_hh, timeout=(10, 3600))
            if r.status_code in (200, 201):
                return "%s/storage/v1/object/public/%s/%s" % (base, bk, path)
            if r.status_code not in _RETRYABLE:
                raise RuntimeError("storage %s: %s" % (r.status_code, r.text[:200]))
            last_err = "storage %s" % r.status_code
        except requests.RequestException as e:
            last_err = e.__class__.__name__       # 타임아웃/연결 끊김 등
    raise RuntimeError("storage upload failed after retries: %s" % last_err)
def cloud_selftest():
    """작은 파일을 실제 업로드해 업로드 권한을 즉시 진단(전체 게임 없이). 결과를 GUI 로그에 남긴다."""
    import tempfile
    p = os.path.join(tempfile.gettempdir(), "penta_selftest.txt")
    try:
        with open(p, "w", encoding="utf-8") as _f: _f.write("penta")
        sb_upload(p, "_selftest/ping.txt", "text/plain")      # x-upsert 라 매번 같은 파일 덮어씀(누적 없음)
        log("Cloud upload self-test: OK \u2713  (\uc5c5\ub85c\ub4dc \uad8c\ud55c \uc815\uc0c1)")
    except Exception as e:
        _em = str(e)
        log(f"Cloud upload self-test FAILED: {e}")            # 정확한 Supabase 응답을 그대로 노출
        if ("row-level security" in _em) or (" 403" in _em) or ("403:" in _em) or ("Unauthorized" in _em):
            log("\u2192 Direct upload is blocked by policy (expected after security_pass.sql). Game uploads use the signed route (/api/storage) automatically.")
    finally:
        try: os.remove(p)
        except Exception: pass
# ===================== Tier 1 신뢰성: 로컬 원본 정리 + 업로드 재시도 큐 =====================
RETRY_QUEUE_PATH = os.path.join(DATA_DIR, "upload_retry.json")
UPDATE_INFO = {}   # 새 버전 발견 시 {"tag": "vX.Y.Z", "url": 다운로드 페이지} — GUI 배너가 읽음

def _busy_now():
    return bool(REC_STATE.get("recording") or REC_STATE.get("uploading") or REC_STATE.get("preparing"))

def disk_free_gb(path=None):
    """대상 경로 드라이브의 여유 공간(GB). 실패 시 None."""
    try:
        import shutil as _sh
        return _sh.disk_usage(path or REC_DIR).free / 1073741824
    except Exception:
        return None

def _parse_ver(v):
    """'v1.2.3' / '1.2.3' → (1,2,3). 형식이 아니면 None (dev 포함)."""
    try:
        parts = str(v).strip().lstrip("vV").split(".")
        if len(parts) != 3: return None
        return tuple(int(p) for p in parts)
    except Exception:
        return None

def check_update():
    """GitHub Releases 최신 태그와 비교 → 새 버전이면 UPDATE_INFO 채움 + 로그. 실패는 조용히 무시(오프라인 등)."""
    cur = _parse_ver(APP_VERSION)
    if not cur:
        return   # 소스 실행/버전 미주입 → 확인 생략
    try:
        import requests
        r = requests.get("https://api.github.com/repos/choisungho-collab/PENTA/releases/latest",
                         headers={"Accept": "application/vnd.github+json"}, timeout=8)
        if r.status_code != 200:
            return
        j = r.json() or {}
        tag = str(j.get("tag_name") or "")
        new = _parse_ver(tag)
        if new and new > cur:
            UPDATE_INFO["tag"] = tag
            UPDATE_INFO["url"] = (CFG.get("gallery_url") or "https://mypenta.netlify.app").rstrip("/") + "/download.html"
            log("Update available: %s (you have %s). Get it from the download page." % (tag, APP_VERSION))
    except Exception:
        pass

def _rq_load():
    try:
        q = json.load(open(RETRY_QUEUE_PATH, encoding="utf-8"))
        return q if isinstance(q, list) else []
    except Exception:
        return []

def _rq_save(q):
    try: _atomic_write_json(RETRY_QUEUE_PATH, q)
    except Exception: pass

def enqueue_failed_upload(entry):
    """업로드 실패분을 큐에 저장 → 다음 실행 때 자동 재시도. 같은 id는 최신으로 갱신."""
    q = [e for e in _rq_load() if e.get("id") != entry.get("id")]
    entry["attempts"] = 0; entry["queued_at"] = time.time()
    q.append(entry); _rq_save(q)
    log("Upload saved to retry queue - will retry on next launch. (pending: %d)" % len(q))

def process_retry_queue():
    """시작 시 실패 큐 처리: 재업로드 성공 → 제거 / 파일 없음·6회째 실패 → 포기."""
    q = _rq_load()
    if not q:
        return
    if not sb_writable():
        log("Retry queue: %d pending, but cloud is not configured - keeping for later." % len(q)); return
    log("Retry queue: %d pending upload(s). Retrying now..." % len(q))
    rest = []
    for e in q:
        vid = e.get("video_file"); row = e.get("row") or {}; eid = e.get("id") or "?"
        e["attempts"] = int(e.get("attempts") or 0) + 1
        if not (vid and os.path.isfile(vid)):
            log("Retry dropped (file missing): %s" % eid); continue
        if e["attempts"] > 5:
            log("Retry dropped (too many attempts): %s" % eid); continue
        try:
            safe = e.get("safe") or ""
            rid = e.get("riot_id") or ""
            _ident = riot_key(rid); _sec = device_secret() if _ident else None
            _idp = (_ident, _sec) if (_ident and _sec) else None
            if _idp:   # 서명 업로드 전 identities 등록(멱등) — verify_device 통과 조건
                try:
                    sb_rpc("register_identity", {"p_puuid": _ident, "p_secret": _sec, "p_name": rid, "p_icon": None})
                except Exception:
                    pass
            row["video"] = sb_upload(vid, "videos/%s.mp4" % safe, "video/mp4", ident=_idp)
            th = e.get("thumb_file")
            if th and os.path.isfile(th):
                row["thumb"] = sb_upload(th, "thumbs/%s.jpg" % safe, "image/jpeg", ident=_idp)
            done = False
            if _idp:   # 소유자 행 등록 우선, 실패 시 익명 RPC 폴백
                try:
                    sb_rpc("upload_match", {"p_puuid": _ident, "p_secret": _sec, "p_row": row})
                    done = True
                except Exception:
                    pass
            if not done:
                sb_insert_match(row)
            log("Retry upload OK: %s" % eid)
        except Exception as ex:
            log("Retry upload failed (%s): %s" % (eid, ex))
            rest.append(e)
    _rq_save(rest)

def cleanup_recordings(manual=False):
    """원본 보관 정책: 최근 keep_games판 + 총 keep_gb GB 상한 초과분을 오래된 순으로 삭제.
    안전장치: 녹화/업로드 중이면 건너뜀 / 30분 내 파일 보존 / 재시도 큐가 참조하는 파일 보존."""
    if _busy_now():
        if manual: log("Cleanup skipped: recording or upload in progress. Try again later.")
        return
    keep_n = int(CFG.get("keep_games", 20) or 0)
    keep_gb = float(CFG.get("keep_gb", 30) or 0)
    if keep_n <= 0 and keep_gb <= 0:
        if manual: log("Cleanup skipped: auto-clean is off (keep_games=0, keep_gb=0).")
        return
    protected = set()   # 재시도 큐가 참조하는 파일·폴더는 절대 삭제 금지
    for e in _rq_load():
        for k in ("video_file", "thumb_file"):
            v = e.get(k)
            if v:
                protected.add(os.path.normcase(os.path.abspath(v)))
                protected.add(os.path.normcase(os.path.abspath(os.path.dirname(v))))
    def _prot(p):
        return os.path.normcase(os.path.abspath(p)) in protected
    now = time.time(); removed = 0; freed = 0
    def _rm(p):
        nonlocal removed, freed
        if _prot(p): return
        try:
            if os.path.isdir(p):
                sz = 0
                for dp, _dns, fns in os.walk(p):
                    for fn in fns:
                        try: sz += os.path.getsize(os.path.join(dp, fn))
                        except OSError: pass
                import shutil as _sh
                _sh.rmtree(p, ignore_errors=True)
            else:
                sz = os.path.getsize(p); os.remove(p)
            removed += 1; freed += sz
        except OSError:
            pass
    try:
        vids = []
        for n in os.listdir(REC_DIR):
            p = os.path.join(REC_DIR, n)
            if n.lower().endswith(".mp4") and os.path.isfile(p):
                m = os.path.getmtime(p)
                if now - m > 1800: vids.append((m, p))   # 30분 내 파일은 집계에서 제외(=보존)
        vids.sort(reverse=True)                          # 최신 먼저
        drop = vids[keep_n:] if keep_n > 0 else []
        keep = vids[:keep_n] if keep_n > 0 else list(vids)
        if keep_gb > 0:                                  # 남긴 것도 총량 초과분은 오래된 것부터(최신 1판은 항상 보존)
            cap = keep_gb * 1073741824
            tot = 0
            for _m, p in keep:
                try: tot += os.path.getsize(p)
                except OSError: pass
            while len(keep) > 1 and tot > cap:
                m, p = keep.pop()                        # 끝 = 가장 오래된 판
                try: tot -= os.path.getsize(p)
                except OSError: pass
                drop.append((m, p))
        for _m, p in drop: _rm(p)
        for n in os.listdir(REC_DIR):                    # 하루 지난 임시 오디오/로그 정리
            p = os.path.join(REC_DIR, n)
            if os.path.isfile(p) and n.lower().endswith(".wav") and now - os.path.getmtime(p) > 86400:
                _rm(p)
        subs = []                                        # 업로드 산출물(preview/thumb) 폴더도 같은 N 기준
        for n in os.listdir(UPLOAD_DIR):
            p = os.path.join(UPLOAD_DIR, n)
            if os.path.isdir(p):
                m = os.path.getmtime(p)
                if now - m > 1800: subs.append((m, p))
        subs.sort(reverse=True)
        for _m, p in (subs[keep_n:] if keep_n > 0 else []):
            _rm(p)
    except Exception as e:
        log("Cleanup error: %s" % e); return
    if removed or manual:
        log("Cleanup done: %d item(s) removed, %.2f GB freed. (keep: %s games / %s GB cap)"
            % (removed, freed / 1073741824,
               (str(keep_n) if keep_n > 0 else "all"),
               (("%g" % keep_gb) if keep_gb > 0 else "no")))

def sb_insert_match(row):
    """익명 행 등록: 검증 RPC(upload_match_anon) 우선 — anon 직접 insert 는 security_pass.sql 이후 차단됨."""
    try:
        return sb_rpc("upload_match_anon", {"p_row": row})
    except Exception as e:
        log("Anon RPC insert unavailable (%s) - trying legacy direct insert." % str(e)[:120])
    return _insert_match_direct(row)

def _insert_match_direct(row):
    import requests
    r = requests.post(_sb_base() + "/rest/v1/matches",
                      headers={**_sb_h(write=True), "Prefer": "resolution=merge-duplicates,return=minimal"},
                      data=json.dumps(row, ensure_ascii=False).encode("utf-8"), timeout=60)
    if r.status_code not in (200, 201, 204):
        raise RuntimeError("insert %s: %s" % (r.status_code, r.text[:200]))
def sb_count_matches():
    import requests
    r = requests.get(_sb_base() + "/rest/v1/matches?select=id",
                     headers={**_sb_h(body_json=False), "Prefer": "count=exact", "Range": "0-0"}, timeout=30)
    cr = r.headers.get("content-range", "")
    try: return int(cr.split("/")[-1])
    except Exception:
        try: return len(r.json())
        except Exception: return 0


# ===================== 소유권/로그인 (기기 비밀키 + RPC) =====================
# 신원 = 라이엇 PUUID. 인증 = 이 기기가 생성/보관하는 비밀키(서버엔 sha256 해시만 저장).
# PUUID 는 게임 후 Riot 프록시(resolve_match)에서 얻어 한 번 등록하면 영구 보관된다.
DEVICE_PATH = os.path.join(DATA_DIR, "penta_device.json")

def _device_load():
    try:
        if os.path.isfile(DEVICE_PATH):
            d = json.load(open(DEVICE_PATH, encoding="utf-8"))
            if isinstance(d, dict): return d
    except Exception: pass
    return {}

def _device_save(d):
    try: _atomic_write_json(DEVICE_PATH, d)
    except Exception: pass

def device_secret():
    """기기 비밀키(없으면 생성, 영구보관). 서버엔 sha256 해시만 올라간다."""
    d = _device_load()
    s = (d.get("secret") or "").strip()
    if len(s) < 20:
        s = secrets.token_urlsafe(32)
        d["secret"] = s; _device_save(d)
    return s

def device_get(key, default=None):
    return _device_load().get(key, default)

def device_set(**kw):
    d = _device_load(); d.update(kw); _device_save(d)

def riot_key(rid):
    """Riot ID(gameName#tagline)를 계정 신원 키로 정규화 — 대소문자/공백 차이를 무시한다.
    이 키가 곧 계정. PC를 가리지 않고, 같은 Riot ID면 어느 기기에서 올려도 한 계정으로 묶인다."""
    return (rid or "").strip().lower()

def _bind_account(rid):
    """게임에서 Riot ID를 확인하면 즉시 계정으로 묶는다 — riot_id 저장 + (클라우드면) 신원 등록.
    업로드를 끝까지 안 해도 게임 시작 직후 Archive 로그인이 되도록 한다."""
    if not rid: return
    try: device_set(riot_id=rid)
    except Exception: pass
    if cloud_state() == "cloud":
        def _reg():
            try: sb_rpc("register_identity", {"p_puuid": riot_key(rid), "p_secret": device_secret(), "p_name": rid, "p_icon": None})
            except Exception: pass
        threading.Thread(target=_reg, daemon=True).start()

def sb_rpc(fn, payload, timeout=30):
    """Supabase RPC(/rest/v1/rpc/{fn}) 호출 → 결과(JSON). 실패 시 예외."""
    import requests
    r = requests.post(_sb_base() + "/rest/v1/rpc/" + fn,
                      headers=_sb_h(write=True),
                      data=json.dumps(payload, ensure_ascii=False).encode("utf-8"), timeout=timeout)
    if r.status_code not in (200, 201, 204):
        raise RuntimeError("rpc %s %s: %s" % (fn, r.status_code, r.text[:200]))
    try: return r.json()
    except Exception: return None


def _trim_lead(video_path, game_len_sec, lead=6.0):
    """로비/로딩 구간을 잘라 카운트다운(게임 시작 ~6초 전)부터 시작하게. 게임은 영상 끝에서 game_len_sec 길이라 끝에서 역산(-sseof). 앞에서 잘라낸 초를 반환(멀티뷰 정렬용)."""
    if not (FFMPEG and video_path and game_len_sec): return 0.0
    try:
        keep = float(game_len_sec) + lead
        orig = _video_dur(video_path) or 0.0
        tmp = video_path + ".trim.mp4"
        r = _run([FFMPEG, "-y", "-loglevel", "error", "-sseof", f"-{keep:.2f}",
                  "-i", video_path, "-c", "copy", "-avoid_negative_ts", "make_zero", tmp],
                 capture_output=True)
        if r.returncode == 0 and os.path.isfile(tmp) and os.path.getsize(tmp) > 1024:
            os.replace(tmp, video_path)
            log("Trimmed the lobby/loading intro so it starts at the countdown")
            return max(0.0, orig - keep) if orig else 0.0
        else:
            try: os.remove(tmp)
            except OSError: pass
    except Exception as e:
        log(f"Skipped trimming (keeping original): {e}")
    return 0.0


# ===================== 영상 처리 =====================

def make_thumb(video, out, at=None):
    if not FFMPEG: return False
    cands = []
    if at is not None and at > 0: cands.append(f"{float(at):.2f}")
    cands += ["120", "60", "20", "5", "1", "0.5"]
    for ts in cands:
        try:
            _run([FFMPEG, "-y", "-loglevel", "error", "-ss", ts, "-i", video,
                            "-frames:v", "1", "-vf", "scale=640:-2", out], timeout=30)
            if os.path.isfile(out) and os.path.getsize(out) > 2000:
                return True
        except Exception:
            pass
    return False


# ===================== 6. 레코더 (ffmpeg) =====================
_ENC_CACHE = None
_ENC_IS_SW = False   # 소프트웨어(libx264) 인코딩 여부 → 다운스케일 판단에 사용
_HW_ENCODERS = [   # 자동 선택 우선순위: NVIDIA → AMD → Intel. pix_fmt 은 인코더별로 다름(QSV 는 nv12 필수).
    ("nvenc", "h264_nvenc", ["-c:v", "h264_nvenc", "-preset", "p6", "-rc", "vbr", "-cq", "18", "-b:v", "0", "-pix_fmt", "yuv420p"], "NVENC"),
    ("amf",   "h264_amf",   ["-c:v", "h264_amf", "-quality", "quality", "-rc", "vbr_peak", "-b:v", "12M", "-maxrate", "20M", "-pix_fmt", "yuv420p"], "AMD AMF"),
    ("qsv",   "h264_qsv",   ["-c:v", "h264_qsv", "-global_quality", "20", "-pix_fmt", "nv12"], "Intel QSV"),
]
def _hw_ok(disp, args):
    """실제 사용할 것과 동일한 인자로 720p 1초 인코딩 테스트 — 목록에 있어도 런타임 실패면 다음 후보로."""
    try:
        r = _run([FFMPEG, "-hide_banner", "-loglevel", "error",
                  "-f", "lavfi", "-i", "color=c=black:s=1280x720:r=30:d=1",
                  *args, "-f", "null", "-"],
                 capture_output=True, text=True, timeout=30)
        if r.returncode != 0:
            errs = [l for l in (r.stderr or "").splitlines() if l.strip()]
            if errs: log("  %s error: %s" % (disp, "  /  ".join(errs[-2:])))
            return False
        return True
    except Exception as e:
        log("  %s test exception: %s" % (disp, e))
        return False

def _encoder_args():
    """인코더 자동 선택 — GPU 후보(NVENC→AMF→QSV)를 실제 인코딩 테스트로 검증, 전부 실패하면 x264."""
    global _ENC_CACHE, _ENC_IS_SW
    if _ENC_CACHE is not None: return _ENC_CACHE
    pref = (CFG.get("encoder") or "auto").lower()
    have = ""
    try:
        have = _run([FFMPEG, "-hide_banner", "-encoders"],
                              capture_output=True, text=True, timeout=15).stdout or ""
    except Exception: pass
    chosen = None
    if pref in ("x264", "libx264", "software", "cpu"):
        pass                                              # 명시적 CPU 선택
    elif pref in ("nvenc", "amf", "qsv"):                 # 명시적 GPU 선택 — 테스트 없이 존중(실패 시 에러가 그대로 보임)
        for key, codec, args, disp in _HW_ENCODERS:
            if key == pref:
                chosen = (list(args), disp); break
    else:                                                 # auto — 순서대로 실제 인코딩 테스트
        for key, codec, args, disp in _HW_ENCODERS:
            if codec not in have:
                continue
            if _hw_ok(disp, args):
                chosen = (list(args), disp); break
            log("  %s is listed but failed the encode test - trying next encoder." % disp)
    if chosen:
        _ENC_IS_SW = False
        _ENC_CACHE, name = chosen
    else:
        _ENC_IS_SW = True
        preset = (CFG.get("preset") or "auto").lower()
        if preset in ("auto", ""): preset = "superfast"   # 소프트웨어는 게임 끊김 방지 위해 가벼운 프리셋
        _ENC_CACHE = ["-c:v", "libx264", "-preset", preset, "-crf", "21", "-pix_fmt", "yuv420p"]; name = f"x264 ({preset})"
    log(f"Encoder: {name}")
    return _ENC_CACHE

def _probe_height(src):
    """ffprobe로 소스 영상 세로 해상도(px). 실패하면 0(안전하게 스케일 적용)."""
    try:
        fp = os.path.join(HERE, "ffprobe.exe")
        if not os.path.isfile(fp): fp = "ffprobe"
        r = _run([fp, "-v", "error", "-select_streams", "v:0",
                  "-show_entries", "stream=height", "-of", "csv=p=0", src],
                 capture_output=True, text=True, timeout=20)
        out = (r.stdout or "").strip()
        return int(out.splitlines()[0]) if out else 0
    except Exception:
        return 0


def make_preview(src, dst):
    """원본을 갤러리용으로 재인코딩 — 최대 1080p(초과분만 다운스케일, 업스케일 안 함) + '또렷한' 화질.
    화질 노브: config.json 의 preview_cq(NVENC, 낮을수록 고화질, 기본 26) / preview_crf(x264, 기본 26).
    값을 더 낮추면(예: cq18/crf19) 더 선명·용량↑. 성공 시 dst, 실패 시 None(원본 업로드로 폴백)."""
    if not (FFMPEG and src and os.path.isfile(src)): return None
    enc = _encoder_args() or []
    cq  = str(CFG.get("preview_cq", 26))    # NVENC 품질(0~51, 낮을수록 고화질). 기본 26 = 적당 화질 + 더 작은 용량
    crf = str(CFG.get("preview_crf", 26))   # x264 품질(0~51, 낮을수록 고화질). 기본 26
    if "h264_nvenc" in enc:
        # p6(고품질 프리셋) + VBR 기반 CQ. maxrate 로 최대 비트레이트만 막아 용량 폭주 방지.
        venc = ["-c:v", "h264_nvenc", "-preset", "p6", "-rc", "vbr", "-cq", cq,
                "-b:v", "0", "-maxrate", "16M", "-bufsize", "32M", "-pix_fmt", "yuv420p"]
    elif "h264_amf" in enc:
        venc = ["-c:v", "h264_amf", "-quality", "quality", "-rc", "vbr_peak",
                "-b:v", "10M", "-maxrate", "16M", "-pix_fmt", "yuv420p"]
    elif "h264_qsv" in enc:
        venc = ["-c:v", "h264_qsv", "-global_quality", crf, "-pix_fmt", "nv12"]
    else:
        venc = ["-c:v", "libx264", "-preset", "veryfast", "-crf", crf, "-pix_fmt", "yuv420p"]
    sh = _probe_height(src)
    vf = [] if (sh and sh <= 1080) else ["-vf", "scale=-2:'min(1080,ih)':flags=lanczos"]
    hws = ([["-hwaccel", "cuda"], []] if "h264_nvenc" in enc else [[]])   # NVENC: GPU decode first, CPU fallback
    try:
        log("Compressing for gallery (1080p, sharper)\u2026")
        for _hw in hws:
            r = _run([FFMPEG, "-y", "-loglevel", "error", *_hw, "-i", src,
                      *venc, *vf,
                      "-c:a", "aac", "-b:a", "160k", "-movflags", "+faststart", dst],
                     timeout=5400)
            if r.returncode == 0 and os.path.isfile(dst) and os.path.getsize(dst) > 0:
                return dst
            if _hw: log("GPU-decode attempt failed (code %s) - retrying with CPU decode." % r.returncode)
        log("Gallery re-encode returned code %s \u2014 uploading original instead." % r.returncode)
    except Exception as e:
        log("Gallery re-encode failed (%s) \u2014 uploading original instead." % e)
    return None


def _target_height(src_h=0):
    """소프트웨어 인코딩이면 부하를 줄이려 다운스케일할 목표 높이. None이면 원본 유지."""
    _encoder_args()  # _ENC_IS_SW 확정
    pref = str(CFG.get("scale") or "auto").lower()
    if pref in ("source", "원본", "full", "native", "off", "0"): return None
    if pref in ("1080", "1080p"): th = 1080
    elif pref in ("720", "720p"): th = 720
    elif pref in ("480", "480p"): th = 480
    elif pref in ("1440", "1440p"): th = 1440
    else:  # auto: 인코더 무관 1080p (가독성 우선 — 720p로 안 떨어뜨림; 원본 그대로는 scale="source", CPU 부하 크면 수동 720p 선택)
        th = 1080
    if th is None: return None
    if src_h and src_h <= th: return None   # 업스케일 금지
    return th

def _scale_vf(src_h=0):
    """(-vf 인자 리스트, filter_complex 체인에 붙일 문자열) 반환. 다운스케일 불필요하면 둘 다 비움."""
    th = _target_height(src_h)
    if not th: return [], ""
    expr = f"scale=-2:'min({th},ih)':flags=fast_bilinear"
    return ["-vf", expr], "," + expr

class Recorder:
    # ddagrab = Desktop Duplication API. 윈10/11에선 '전체화면(독점)'도 잡힙니다.
    # gdigrab = 옛날 GDI 방식. 전체화면 독점에선 검은 화면이라 보조용.
    def __init__(self, fps):
        self.fps = fps; self.proc = None; self.path = None
        self.mode = "ddagrab"; self.output_idx = 0; self.verified = False; self.warned_black = False
        self.backend = "ffmpeg"; self.verified_backend = None
        self._wgc_control = None; self._wgc_state = None
        self._t_start = 0.0; self.last_seconds = 0.0   # 직전 녹화 길이(초) — 메뉴 클립/실제 게임 구분용
        self._aud = None; self._vt0 = None   # 오디오(WASAPI 루프백) 상태 + 영상 첫 프레임 시각
    def _cmd(self, out, mode, output_idx=0):
        enc = _encoder_args()
        vf, chain = _scale_vf(0)   # 데스크톱 높이를 모르므로 min() 식이 런타임에 처리(업스케일 안 함)
        tail = [*enc, "-movflags", "+faststart", out]   # pix_fmt 은 인코더 인자에 포함(QSV=nv12)
        if mode == "ddagrab":
            return [FFMPEG, "-y", "-loglevel", "error",
                    "-filter_complex", f"ddagrab=output_idx={output_idx}:framerate={self.fps},hwdownload,format=bgra{chain}", *tail]
        return [FFMPEG, "-y", "-loglevel", "error", "-f", "gdigrab",
                "-framerate", str(self.fps), "-i", "desktop", *vf, *tail]
    def _spawn(self, mode, output_idx=0):
        self.path = os.path.join(REC_DIR, f"clip_{datetime.datetime.now():%Y%m%d_%H%M%S}.mp4")
        self.proc = subprocess.Popen(self._cmd(self.path, mode, output_idx),
                      stdin=subprocess.PIPE, stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL,
                      creationflags=getattr(subprocess, "CREATE_NO_WINDOW", 0))
        self._vt0 = time.time()
    def _alive(self):
        return self.proc is not None and self.proc.poll() is None
    def _capturing(self, secs=3.0, floor=40000):
        # 녹화 파일이 실제로 커지면 = 화면이 잡히는 것. 검은 화면이면 거의 안 커짐.
        try: s0 = os.path.getsize(self.path)
        except OSError: s0 = 0
        time.sleep(secs)
        try: s1 = os.path.getsize(self.path)
        except OSError: s1 = 0
        return (s1 - s0) >= floor
    def _kill(self):
        if self.proc:
            try: self.proc.terminate(); self.proc.wait(timeout=5)
            except Exception:
                try: self.proc.kill()
                except Exception: pass
        self.proc = None
        try:  # 빈(검은) 클립은 삭제
            if self.path and os.path.isfile(self.path) and os.path.getsize(self.path) < 40000:
                os.remove(self.path)
        except OSError: pass
    def _recording(self):
        if self.backend == "wgc":
            st = self._wgc_state or {}
            ft = st.get("feeder")
            return bool(ft and ft.is_alive())   # 피더 스레드가 죽으면 녹화가 끊긴 것
        return self._alive()

    def _start_audio(self):
        """시스템 사운드(게임 소리)를 WASAPI 루프백으로 WAV에 병렬 녹음.
        Windows 내장 기능이라 Stereo Mix·가상케이블 없이 어떤 PC에서도 동작.
        실패하면 무음으로 진행(영상 녹화는 영향 없음)."""
        self._stop_audio(discard=True); self._vt0 = None
        self.audio_path = os.path.join(REC_DIR, f"audio_{datetime.datetime.now():%Y%m%d_%H%M%S}.wav")
        _gameonly = str(CFG.get("audio", "system")).lower() in ("game-only", "game", "gameonly", "game_only", "lol", "\ub86c\ub9cc")
        box = {"stop": threading.Event(), "t0": None, "thread": None, "ok": False, "path": self.audio_path,
               "pid": (_lol_game_pid() if _gameonly else None)}
        def worker():
            if box.get("pid"):
                try:
                    _capture_process_audio(box["pid"], box); return   # 게임 소리만 캡처 성공 → 끝까지 녹음
                except Exception as e:
                    log(f"  (audio) game-only capture failed \u2192 using system sound ({e})")
                    box["ok"] = False; box["t0"] = None
                    try:
                        if os.path.isfile(box["path"]): os.remove(box["path"])
                    except Exception: pass
                    # ↓ 시스템 사운드(디바이스 루프백)로 폴백
            try:
                import pyaudiowpatch as pa, wave
            except Exception as e:
                log(f"  (audio) pyaudiowpatch not installed \u2192 recording without sound. Install: pip install pyaudiowpatch ({e})"); return
            p = stream = wf = None
            try:
                p = pa.PyAudio()
                try:
                    dev = p.get_default_wasapi_loopback()           # 기본 출력장치의 루프백
                except Exception:
                    wi = p.get_host_api_info_by_type(pa.paWASAPI)   # 폴백: 직접 탐색
                    spk = p.get_device_info_by_index(wi["defaultOutputDevice"]); dev = None
                    for lb in p.get_loopback_device_info_generator():
                        if spk.get("name","") in lb.get("name",""): dev = lb; break
                    if dev is None: raise RuntimeError("WASAPI loopback device not found")
                ch = int(dev.get("maxInputChannels") or 2) or 2
                rate = int(dev.get("defaultSampleRate") or 48000) or 48000
                wf = wave.open(box["path"], "wb"); wf.setnchannels(ch); wf.setsampwidth(2); wf.setframerate(rate)
                stream = p.open(format=pa.paInt16, channels=ch, rate=rate, input=True,
                                input_device_index=dev["index"], frames_per_buffer=2048)
                box["t0"] = time.time(); box["ok"] = True
                log(f"  ♪ Audio recording started ({str(dev.get('name','?'))[:26]} · {rate}Hz {ch}ch)")
                while not box["stop"].is_set():
                    try: wf.writeframes(stream.read(2048, exception_on_overflow=False))
                    except Exception: break
            except Exception as e:
                log(f"  (audio) capture failed → recording without sound: {e}")
            finally:
                for fn in (lambda: stream and stream.stop_stream(), lambda: stream and stream.close(),
                           lambda: wf and wf.close(), lambda: p and p.terminate()):
                    try: fn()
                    except Exception: pass
        t = threading.Thread(target=worker, daemon=True); box["thread"] = t; t.start()
        self._aud = box

    def _stop_audio(self, discard=False):
        box = self._aud; self._aud = None
        if not box: return (None, None)
        try: box["stop"].set()
        except Exception: pass
        th = box.get("thread")
        if th:
            try: th.join(timeout=8)
            except Exception: pass
        path = box.get("path"); t0 = box.get("t0")
        if discard:
            try:
                if path and os.path.isfile(path): os.remove(path)
            except OSError: pass
            return (None, None)
        return (path if box.get("ok") else None, t0)

    def _finalize(self):
        """영상 클립 + 병렬 녹음한 소리를 합쳐 최종 mp4. 소리 없으면 영상만 그대로."""
        vid = self.path if (self.path and os.path.isfile(self.path)) else None
        wav, at0 = self._stop_audio()
        if not vid:
            try:
                if wav and os.path.isfile(wav): os.remove(wav)
            except OSError: pass
            return None
        if not (wav and os.path.isfile(wav) and os.path.getsize(wav) > 2000):
            return vid                                  # 소리 없음 → 영상만(기존 동작)
        if not FFMPEG:
            return vid
        try:
            out = (vid[:-4] if vid.lower().endswith(".mp4") else vid) + "_av.mp4"
            voff = 0.0
            if self._vt0 and at0 and self._vt0 > at0:
                voff = min(10.0, self._vt0 - at0)       # 영상이 늦게 시작한 만큼 소리 앞부분을 잘라 싱크
            cmd = [FFMPEG, "-y", "-loglevel", "error"]
            if voff > 0.05: cmd += ["-ss", f"{voff:.3f}"]
            cmd += ["-i", wav, "-i", vid, "-map", "1:v:0", "-map", "0:a:0",
                    "-c:v", "copy", "-c:a", "aac", "-b:a", "192k",
                    "-movflags", "+faststart", "-shortest", out]
            _run(cmd, timeout=900)
            if os.path.isfile(out) and os.path.getsize(out) > 40000:
                for f in (vid, wav):
                    try: os.remove(f)
                    except OSError: pass
                self.path = out; log("■ Audio merged"); return out
            log("  (audio) merge result empty → using video only")
            try:
                if os.path.isfile(out): os.remove(out)
            except OSError: pass
            return vid
        except Exception as e:
            log(f"  (audio) merge failed → video only: {e}"); return vid

    def _start_wgc(self, verify=True):
        """WGC(OBS식)로 프레임을 받아 ffmpeg로 인코딩. 정지화면이어도 직전 프레임을 고정 fps로 계속 먹임(전체화면 게임도 잡힘)."""
        # windows_capture는 import 시 cv2(OpenCV)를 부르지만, 우리는 frame.frame_buffer(numpy)만 쓰고 cv2는 안 쓴다.
        # OpenCV(수십 MB) 번들을 피하려고, 진짜 cv2가 없으면 빈 스텁을 끼워 import만 통과시킨다(→ GPU 캡처로 CPU 절감).
        if "cv2" not in sys.modules:
            try:
                import cv2  # noqa: F401  (진짜 OpenCV가 있으면 그대로 사용)
            except Exception:
                import types as _types
                sys.modules["cv2"] = _types.ModuleType("cv2")
        try:
            from windows_capture import WindowsCapture
        except ImportError:
            try:
                log("Installing WGC engine (windows-capture)…")
                _run([sys.executable, "-m", "pip", "install", "-q", "windows-capture", "--break-system-packages"], timeout=240)
                from windows_capture import WindowsCapture
            except Exception as e:
                log("  WGC engine not installed here \u2014 using GPU desktop capture (ddagrab) instead."); return False
        try:
            import numpy as _np
        except Exception as e:
            log(f"  WGC unavailable (numpy missing: {e})"); return False
        self.path = os.path.join(REC_DIR, f"clip_{datetime.datetime.now():%Y%m%d_%H%M%S}.mp4")
        enc = _encoder_args(); pathx = self.path; fps = self.fps
        shared = {"buf": None, "wh": None, "n": 0, "err": None}
        stop_ev = threading.Event(); proc_box = {"p": None}
        # Pick the monitor from config.output_idx (ddagrab is 0-based; windows-capture is 1-based → +1). "auto" → primary (1).
        _ci = CFG.get("output_idx", "auto")
        midx = (int(_ci) + 1) if (isinstance(_ci, int) or (isinstance(_ci, str) and _ci.isdigit())) else 1
        def _build_and_start(border):
            """주어진 draw_border로 캡처 세션 생성+이벤트 등록+시작. (cap, control) 반환."""
            cap = WindowsCapture(cursor_capture=None, draw_border=border, monitor_index=midx, window_name=None)
            @cap.event
            def on_frame_arrived(frame, capture_control):
                if stop_ev.is_set():
                    try: capture_control.stop()
                    except Exception: pass
                    return
                try:
                    shared["buf"] = frame.frame_buffer          # numpy 처리는 피더에서 (콜백은 최대한 단순하게)
                    shared["wh"] = (frame.width, frame.height)
                    shared["n"] = shared.get("n", 0) + 1
                except Exception as e:
                    if shared.get("err") is None: shared["err"] = repr(e)
            @cap.event
            def on_closed():
                pass
            return cap, cap.start_free_threaded()

        def feeder():
            t0 = time.time()
            while shared["buf"] is None and time.time() - t0 < 3.0 and not stop_ev.is_set():
                time.sleep(0.05)
            if shared["buf"] is None: return            # 프레임이 하나도 안 옴 = 캡처 실패
            w, h = shared["wh"]
            vf, _chain = _scale_vf(h)
            cmd = [FFMPEG, "-y", "-loglevel", "error",
                   "-f", "rawvideo", "-pixel_format", "bgra", "-video_size", f"{w}x{h}", "-framerate", str(fps),
                   "-i", "pipe:", *vf, *enc, "-movflags", "+faststart", pathx]
            if vf: log(f"  Lowering CPU load: capturing {h}p → encoding at {_target_height(h)}p")
            errlog = os.path.join(REC_DIR, "wgc_ffmpeg.log")
            try: _ef = open(errlog, "w", encoding="utf-8", errors="replace")
            except Exception: _ef = subprocess.DEVNULL
            try:
                p = subprocess.Popen(cmd, stdin=subprocess.PIPE, stdout=subprocess.DEVNULL,
                                     stderr=_ef, creationflags=getattr(subprocess, "CREATE_NO_WINDOW", 0))
            except Exception:
                return
            proc_box["p"] = p; self._vt0 = time.time()
            interval = 1.0 / max(1, fps); nxt = time.time()
            while not stop_ev.is_set():
                b = shared["buf"]
                if b is not None:
                    try:
                        if b.shape[1] != w: b = b[:, :w]            # 스트라이드 보정
                        p.stdin.write(_np.ascontiguousarray(b).tobytes())
                    except Exception: break
                nxt += interval; d = nxt - time.time()
                if d > 0: time.sleep(d)
                else: nxt = time.time()
            try: p.stdin.close()
            except Exception: pass
            try: p.wait(timeout=15)
            except Exception:
                try: p.terminate()
                except Exception: pass

        # 1순위: 테두리 없는 WGC(가장 가볍고 깔끔). 일부 OS(Win10/구버전 Win11)는 이 토글을 미지원해 예외가 난다.
        # ★ 성능 우선: ddagrab(GPU)은 hwdownload 로 매 프레임을 GPU→CPU 로 복사해 WGC보다 무겁다(인게임 FPS 저하 가능).
        #   따라서 borderless 가 안 되면 ddagrab 으로 떨어뜨리지 말고, WGC 기본(None)으로 — 여전히 GPU 기반이라 가볍다.
        #   (그 대신 일부 빌드는 노란 테두리가 남을 수 있음. 노란선이 싫고 부하를 감수하면 config.json "capture":"ddagrab".)
        try:
            cap, control = _build_and_start(False)        # 테두리 없는 WGC
        except Exception:
            try:
                cap, control = _build_and_start(None)     # WGC 기본(가벼움) — 빌드에 따라 노란 테두리가 남을 수 있음
                log("  WGC: this build keeps the default capture border (kept WGC for low overhead).")
            except Exception:
                log("  WGC not usable here \u2014 using GPU desktop capture (ddagrab) instead.")
                return False
        ft = threading.Thread(target=feeder, daemon=True); ft.start()
        self._wgc_control = control
        self._wgc_state = {"stop": stop_ev, "feeder": ft, "proc_box": proc_box}
        self.backend = "wgc"
        if not verify:
            log(f"● Recording started (WGC · Monitor {midx})"); return True
        # --- WGC 검증 ---
        # WGC는 프레임 카운터로 '화면 잡힘'을 직접 확인할 수 있다. 파일 크기는 ffmpeg가 쓰는 중인지 확인용.
        # (NVENC는 첫 키프레임을 측정 전에 쓰고 정적 화면에선 이후 증가가 작아, '델타 40KB' 방식은 오판함 → 절대 크기로 판단)
        def _fflog():
            try:
                _lp = os.path.join(REC_DIR, "wgc_ffmpeg.log")
                if os.path.isfile(_lp):
                    _ls = [l for l in open(_lp, encoding="utf-8", errors="replace").read().strip().splitlines() if l.strip()]
                    return " | ".join(_ls[-3:])
            except Exception: pass
            return ""
        def _sz():
            try: return os.path.getsize(self.path)
            except OSError: return 0
        time.sleep(3.5)   # NVENC 첫 키프레임 + 인코딩 시작 시간 확보
        n1 = shared.get("n", 0)
        pp = proc_box.get("p"); alive = (pp is not None and pp.poll() is None)
        ferr = _fflog()
        ok = (n1 >= 15) and alive and (not ferr)   # 프레임 들어옴 + ffmpeg 정상 동작
        if ok and _sz() >= 8000:
            log(f"● Recording started (WGC · Monitor {midx} — capture verified)"); return True
        if ok:                                       # 파일이 아직 작으면(정적 화면) 잠깐 더 대기 후 재확인
            time.sleep(2.5)
            if _sz() >= 8000:
                log(f"● Recording started (WGC · Monitor {midx} — capture verified)"); return True
        stop_ev.set()
        try: control.stop()
        except Exception: pass
        try: ft.join(timeout=5)
        except Exception: pass
        self.backend = "ffmpeg"; self._wgc_control = None; self._wgc_state = None
        log("  WGC produced no frames (frames:{}, ffmpeg:{}, file:{}B, err:{}) → trying another method".format(
            n1, "alive" if alive else "dead", _sz(), ferr or "none"))
        return False

    def start(self):
        if self._recording(): return True
        self._t_start = time.time()   # 새 클립 시작 시각
        self._start_audio()   # 시스템 사운드 병렬 녹음(백엔드 무관, PC 안 탐)
        capmode = (CFG.get("capture") or "auto").lower()
        # 검증된 방식 빠른 재시작
        if self.verified:
            try:
                if self.verified_backend == "wgc":
                    if self._start_wgc(verify=False): return True
                else:
                    self._spawn(self.mode, self.output_idx); time.sleep(1.0)
                    if self._alive():
                        log(f"● Recording started ({'Monitor '+str(self.output_idx) if self.mode=='ddagrab' else 'gdigrab'})"); return True
            except Exception: pass
            self.verified = False
        # 1순위: WGC (auto/wgc) — 전체화면도 잡히는 OBS식 엔진
        if capmode in ("auto", "wgc"):
            try:
                if self._start_wgc(verify=True):
                    self.verified = True; self.verified_backend = "wgc"; return True
            except Exception:
                log("  WGC not usable here \u2014 using GPU desktop capture (ddagrab) instead.")
            if capmode == "wgc":
                log("  WGC unavailable \u2014 falling back to ddagrab/gdigrab.")
        # 2순위: ddagrab(모니터 0/1/2) → gdigrab
        ci = CFG.get("output_idx", "auto")
        if isinstance(ci, int) or (isinstance(ci, str) and ci.isdigit()):
            candidates = [("ddagrab", int(ci)), ("gdigrab", 0)]
        else:
            candidates = [("ddagrab", 0), ("ddagrab", 1), ("ddagrab", 2), ("gdigrab", 0)]
        for mode, idx in candidates:
            try:
                self._spawn(mode, idx); time.sleep(2.0)
                if not self._alive():
                    continue
                if self._capturing():
                    self.mode = mode; self.output_idx = idx; self.backend = "ffmpeg"
                    self.verified = True; self.verified_backend = "ffmpeg"; self.warned_black = False
                    log(f"● Recording started ({'Monitor '+str(idx) if mode=='ddagrab' else 'gdigrab'}) — capture verified")
                    return True
                self._kill()
            except Exception as e:
                log(f"Recording start error ({mode} #{idx}): {e}"); self._kill()
        if not self.warned_black:
            self.warned_black = True
            log("[!] Couldn't verify capture (the screen may be black). Recording continues anyway.")
            log("    • Play a game first and check the recording (it may be a false alarm from the loading screen)")
            log("    • Set \"capture\" to \"wgc\" in config.json, or run League in 'windowed (fullscreen)' mode")
        try:
            self._spawn("ddagrab", 0); time.sleep(1.0); self.mode = "ddagrab"; self.output_idx = 0; self.backend = "ffmpeg"
            return self._alive()
        except Exception:
            self.proc = None; return False
    def stop(self):
        self.last_seconds = (time.time() - self._t_start) if self._t_start else 0.0
        if self.backend == "wgc":
            st = self._wgc_state or {}
            ev = st.get("stop")
            if ev: ev.set()
            try:
                if self._wgc_control: self._wgc_control.stop()
            except Exception: pass
            ft = st.get("feeder")
            if ft:
                try: ft.join(timeout=20)   # 피더가 ffmpeg stdin 닫고 마무리
                except Exception: pass
            self.backend = "ffmpeg"; self._wgc_control = None; self._wgc_state = None
            log("■ Recording stopped")
            return self._finalize()
        if not self.proc: return None
        p = self.proc; self.proc = None
        try:
            if p.poll() is None:
                try: p.stdin.write(b"q"); p.stdin.flush()
                except Exception: pass
                try: p.wait(timeout=12)
                except subprocess.TimeoutExpired: p.terminate()
        except Exception: pass
        log("■ Recording stopped")
        return self._finalize()

def _lol_game_pid():
    """League of Legends.exe(게임 인스턴스) PID. 없으면 None."""
    try:
        for p in psutil.process_iter(["name", "pid"]):
            try:
                if (p.info["name"] or "").lower() == "league of legends.exe":
                    return int(p.info["pid"])
            except (psutil.NoSuchProcess, psutil.AccessDenied):
                pass
    except Exception:
        pass
    return None


def _capture_process_audio(pid, box):
    """Win10(2004+) 프로세스 루프백 API로 특정 PID 오디오만 box['path'] WAV에 녹음.
    성공 시 box['ok']=True 로 두고 box['stop'] 까지 녹음. 실패하면 예외를 던져
    호출부가 시스템 사운드(디바이스 루프백)로 폴백하게 한다. (실험적)"""
    import ctypes, wave, time as _t
    import ctypes.wintypes as wt
    from ctypes import (POINTER, byref, c_void_p, c_uint32, c_int32, c_uint64,
                        Structure, HRESULT, c_wchar_p, c_byte, c_uint16, c_ulong, cast, WINFUNCTYPE, sizeof)

    ole32 = ctypes.windll.ole32
    mmd = ctypes.windll.mmdevapi
    ole32.CoInitializeEx(None, 0)   # MTA
    try:
        class GUID(Structure):
            _fields_ = [("a", c_uint32), ("b", c_uint16), ("c", c_uint16), ("d", c_byte * 8)]
        def G(sg):
            g = GUID(); ole32.CLSIDFromString(c_wchar_p(sg), byref(g)); return g
        IID_AC  = G("{1CB9AD4C-DBFA-4c32-B178-C2F568A703B2}")   # IAudioClient
        IID_CAP = G("{C8ADBD64-E71E-48a0-A4DE-185C395CD317}")   # IAudioCaptureClient

        def call(p, idx, rest, argt, *a):   # vtable 인덱스로 COM 호출
            vt = cast(p, POINTER(c_void_p))[0]
            fn = cast(vt, POINTER(c_void_p))[idx]
            return WINFUNCTYPE(rest, c_void_p, *argt)(fn)(p, *a)

        class PROC_LB(Structure):
            _fields_ = [("pid", wt.DWORD), ("mode", c_int32)]
        class ACTP(Structure):
            _fields_ = [("atype", c_int32), ("plb", PROC_LB)]
        class BLOB(Structure):
            _fields_ = [("cb", c_uint32), ("p", c_void_p)]
        class PROPV(Structure):
            _fields_ = [("vt", c_uint16), ("r1", c_uint16), ("r2", c_uint16), ("r3", c_uint16),
                        ("blob", BLOB), ("pad", c_byte * 8)]
        class WFX(Structure):
            _fields_ = [("tag", c_uint16), ("ch", c_uint16), ("sps", c_uint32),
                        ("abps", c_uint32), ("ba", c_uint16), ("bps", c_uint16), ("cb", c_uint16)]

        # 완료 핸들러 (IActivateAudioInterfaceCompletionHandler): QI/AddRef/Release/ActivateCompleted
        done = {"f": False}
        QIp = WINFUNCTYPE(HRESULT, c_void_p, POINTER(GUID), POINTER(c_void_p))
        ULp = WINFUNCTYPE(c_ulong, c_void_p)
        ACp = WINFUNCTYPE(HRESULT, c_void_p, c_void_p)
        def _qi(this, riid, ppv): ppv[0] = this; return 0
        def _au(this): return 1
        def _re(this): return 1
        def _ac(this, op): done["f"] = True; return 0
        _f = (QIp(_qi), ULp(_au), ULp(_re), ACp(_ac))   # 참조 유지
        VT = (c_void_p * 4)(cast(_f[0], c_void_p), cast(_f[1], c_void_p), cast(_f[2], c_void_p), cast(_f[3], c_void_p))
        class HOBJ(Structure):
            _fields_ = [("vtbl", c_void_p)]
        hobj = HOBJ(); hobj.vtbl = cast(byref(VT), c_void_p)

        ap = ACTP(); ap.atype = 1   # PROCESS_LOOPBACK
        ap.plb.pid = pid; ap.plb.mode = 0   # INCLUDE_TARGET_PROCESS_TREE
        pv = PROPV(); pv.vt = 65   # VT_BLOB
        pv.blob.cb = sizeof(ACTP); pv.blob.p = cast(byref(ap), c_void_p)

        op = c_void_p()
        hr = mmd.ActivateAudioInterfaceAsync(c_wchar_p("VAD\\Process_Loopback"),
                                             byref(IID_AC), byref(pv), cast(byref(hobj), c_void_p), byref(op))
        if hr != 0: raise RuntimeError("Activate 0x%X" % (hr & 0xffffffff))
        for _ in range(600):
            if done["f"]: break
            _t.sleep(0.005)
        if not done["f"]: raise RuntimeError("activation timeout")

        hres = HRESULT(); iface = c_void_p()
        call(op, 3, HRESULT, [POINTER(HRESULT), POINTER(c_void_p)], byref(hres), byref(iface))   # GetActivateResult
        if (hres.value & 0xffffffff) != 0 or not iface.value: raise RuntimeError("GetActivateResult 0x%X" % (hres.value & 0xffffffff))
        acl = iface

        SPS = 48000
        fmt = WFX(1, 2, SPS, SPS * 4, 4, 16, 0)   # PCM 48k 2ch 16bit
        hr = call(acl, 3, HRESULT,
                  [c_int32, c_uint32, c_uint64, c_uint64, c_void_p, c_void_p],
                  0, 0x00020000, c_uint64(3000000), c_uint64(0), cast(byref(fmt), c_void_p), None)   # Initialize(SHARED, LOOPBACK)
        if hr != 0: raise RuntimeError("Initialize 0x%X" % (hr & 0xffffffff))

        cap = c_void_p()
        hr = call(acl, 14, HRESULT, [POINTER(GUID), POINTER(c_void_p)], byref(IID_CAP), byref(cap))   # GetService
        if hr != 0 or not cap.value: raise RuntimeError("GetService 0x%X" % (hr & 0xffffffff))
        hr = call(acl, 10, HRESULT, [])   # Start
        if hr != 0: raise RuntimeError("Start 0x%X" % (hr & 0xffffffff))

        wf = wave.open(box["path"], "wb"); wf.setnchannels(2); wf.setsampwidth(2); wf.setframerate(SPS)
        box["t0"] = _t.time(); box["ok"] = True
        log("  \u266a Audio (game only) started \u2014 League of Legends \u00b7 48000Hz 2ch")
        BPF = 4
        try:
            while not box["stop"].is_set():
                pkt = c_uint32(0); call(cap, 5, HRESULT, [POINTER(c_uint32)], byref(pkt))   # GetNextPacketSize
                if pkt.value == 0:
                    _t.sleep(0.008); continue
                while pkt.value > 0:
                    pdata = c_void_p(); nfr = c_uint32(); fl = c_uint32()
                    hr = call(cap, 3, HRESULT,
                              [POINTER(c_void_p), POINTER(c_uint32), POINTER(c_uint32), POINTER(c_uint64), POINTER(c_uint64)],
                              byref(pdata), byref(nfr), byref(fl), None, None)   # GetBuffer
                    if hr != 0: break
                    n = nfr.value
                    if n:
                        if fl.value & 0x2:   # SILENT
                            wf.writeframes(b"\x00" * (n * BPF))
                        elif pdata.value:
                            wf.writeframes(bytes((c_byte * (n * BPF)).from_address(pdata.value)))
                    call(cap, 4, HRESULT, [c_uint32], nfr)   # ReleaseBuffer
                    pkt = c_uint32(0); call(cap, 5, HRESULT, [POINTER(c_uint32)], byref(pkt))
        finally:
            try: call(acl, 11, HRESULT, [])   # Stop
            except Exception: pass
            try: wf.close()
            except Exception: pass
    finally:
        try: ole32.CoUninitialize()
        except Exception: pass


def sc_running(name):
    n = name.lower()
    for p in psutil.process_iter(["name"]):
        try:
            if (p.info["name"] or "").lower() == n: return True
        except (psutil.NoSuchProcess, psutil.AccessDenied): pass
    return False


_LCU = {"port": None, "token": None, "t": 0.0, "warned": False}
def _lcu_creds():
    """LeagueClientUx.exe 명령줄에서 LCU 포트/토큰을 읽는다(30초 캐시). 실패하면 (None, None)."""
    now = time.time()
    if _LCU["port"] and (now - _LCU["t"] < 30): return _LCU["port"], _LCU["token"]
    try:
        for p in psutil.process_iter(["name"]):
            try:
                if (p.info["name"] or "").lower() != "leagueclientux.exe": continue
                cl = p.cmdline()
            except (psutil.NoSuchProcess, psutil.AccessDenied): continue
            port = token = None
            for a in cl:
                if a.startswith("--app-port="): port = a.split("=", 1)[1]
                elif a.startswith("--remoting-auth-token="): token = a.split("=", 1)[1]
            if port and token:
                _LCU.update(port=port, token=token, t=now); return port, token
    except Exception: pass
    _LCU.update(port=None, token=None, t=now); return None, None

def lcu_in_champ_select():
    """LCU API로 챔피언 선택 중인지 확인. 실패/아니면 False (→ 게임 프로세스 감지로 폴백)."""
    port, token = _lcu_creds()
    if not (port and token): return False
    try:
        import requests, base64
        try:
            requests.packages.urllib3.disable_warnings(requests.packages.urllib3.exceptions.InsecureRequestWarning)
        except Exception: pass
        auth = base64.b64encode(("riot:" + token).encode()).decode()
        r = requests.get("https://127.0.0.1:%s/lol-champ-select/v1/session" % port,
                         headers={"Authorization": "Basic " + auth}, verify=False, timeout=2)
        return r.status_code == 200   # 챔프셀럭이면 200, 아니면 404
    except Exception:
        return False


def _sec_mmss(s):
    s = int(s or 0)
    return f"{s//60}:{s%60:02d}"

def _video_dur(video_path):
    """ffprobe로 영상 실제 길이(초)를 잰다. 분석을 못 했을 때 길이 판단용. 실패하면 0."""
    try:
        fp = os.path.join(HERE, "ffprobe.exe")
        if not os.path.isfile(fp): fp = "ffprobe"
        out = subprocess.run([fp, "-v", "error", "-show_entries", "format=duration",
                              "-of", "default=nw=1:nk=1", video_path],
                             capture_output=True, text=True, timeout=30).stdout.strip()
        return float(out) if out else 0.0
    except Exception:
        return 0.0

def _discard(video_path, reason):
    REC_STATE["preparing"] = False
    REC_STATE["skipped_at"] = time.time()   # GUI 상태바에 몇 초간 '저장 안 함' 알림
    REC_STATE["skipped_why"] = reason
    log(f"  Discarding (not saving) - {reason}")
    try:
        if video_path and os.path.isfile(video_path): os.remove(video_path)
    except OSError: pass

def _live_gid(analysis, start_ts):
    """Live 자체분석 경기의 공통 ID — matchId가 없을 때, 같은 게임을 녹화한 사람끼리 같은 값이 나오게 한다.
    같은 게임은 참가자 10명과 날짜가 동일하므로 (정렬한 참가자 이름 + 날짜)를 해시한다.
    (일반 매치메이킹에서 같은 10명이 같은 날 다시 만날 확률은 사실상 0이라 충돌 걱정이 없다.)"""
    names = sorted([(p.get("name") or "") for p in (analysis.get("players") or []) if p.get("name")])
    try:
        day = datetime.datetime.fromtimestamp(start_ts).strftime("%Y%m%d")
    except Exception:
        day = datetime.datetime.now().strftime("%Y%m%d")
    key = "|".join(names) + "|" + day
    return "live_" + hashlib.sha1(key.encode("utf-8")).hexdigest()[:12]


def ingest_lol(video_path, riot_id, start_ts, end_ts, proxy_url, platform="kr", live_data=None, started_cs=False, vt0=None):
    # 화면 녹화 + (게임 종료 후) Riot 매치를 연결해 분석·업로드한다.
    # Riot 매치 연결이 안 되더라도(개발키 만료/레이트리밋/타이밍 등) 영상은 업로드하고 아카이브에 올린다.
    if not video_path or not os.path.isfile(video_path) or os.path.getsize(video_path) < 10000:
        log("Video is empty → skipping registration."); return
    if not riot_id:
        return _discard(video_path, "Couldn't verify your Riot ID (may not be a game)")

    # --- Riot 매치 연결 시도 (실패해도 영상은 올린다; 분석만 비움) ---
    REC_STATE["preparing"] = True   # 업로드 준비중: 분석·트림·썸네일·인코딩 (업로드 직전까지)
    match, puuid, analysis, mid = None, None, {}, ""
    if proxy_url:
        try:
            match, puuid = penta_lol.resolve_match(proxy_url, riot_id, start_ts, end_ts, platform)
        except Exception as e:
            log(f"  Match lookup failed ({e}) — uploading the video without analysis.")
        if match:
            mid = (match.get("metadata") or {}).get("matchId") or str((match.get("info") or {}).get("gameId") or "")
            try:
                timeline = penta_lol.proxy_get(proxy_url, "timeline", matchId=mid, platform=platform)
                analysis = penta_lol.analyze_match(match, timeline) or {}
            except Exception as e:
                log(f"  Analysis failed ({e}) — uploading the video without analysis."); analysis = {}
        else:
            log("  No Riot match linked (API key/timing) — uploading the video without analysis.")
    else:
        log("  proxy_url not set — Riot analysis skipped.")

    # Riot 분석이 비었으면(키 만료/레이트리밋/타이밍) Live Client 스냅샷으로 자체 분석 — 키 없이 동작
    if not analysis.get("players") and live_data and live_data.get("snaps"):
        try:
            _mn = (riot_id or "").split("#")[0]
            _la = penta_lol.analyze_live(live_data.get("snaps"), live_data.get("events"), _mn) or {}
            if _la.get("players"):
                analysis = _la
                log(f"  Self-analysis from Live Client ({len(_la['players'])} players, no Riot key needed).")
        except Exception as e:
            log(f"  Live self-analysis failed ({e}).")

    # --- 게임 길이: 분석값이 있으면 그걸, 없으면 영상 실제 길이로 ---
    dur = analysis.get("duration") or 0
    if dur and dur > 100000: dur = dur / 1000.0    # 과거 게임은 길이가 ms일 수 있어 보정
    if not dur:
        dur = _video_dur(video_path)
    if dur and dur < CFG.get("min_game_sec", 300):
        return _discard(video_path, f"Game too short ({int(dur)}s)")

    # --- ID: 매치ID가 있으면 그걸, 없으면(Live 자체분석) 같은 게임 녹화자끼리 묶이도록 참가자 기반 ID, 그도 없으면 임시 ID ---
    if mid:
        gid = mid
    elif analysis.get("players"):
        gid = _live_gid(analysis, start_ts)   # 같은 게임을 녹화한 사람끼리 멀티뷰로 묶임
    else:
        gid = "local_" + datetime.datetime.now().strftime("%Y%m%d_%H%M%S")
    # 시점별 고유 행 id — 스토리지 키로도 쓰이므로 반드시 ASCII (한글/특수문자 → Supabase InvalidKey 방지)
    if puuid:
        _viewer = puuid
    else:
        _nm = "".join(ch for ch in (riot_id or "") if ch.isascii() and ch.isalnum())[:24]
        _viewer = (_nm + "_" if _nm else "") + hashlib.sha1((riot_id or "").encode("utf-8")).hexdigest()[:10]
    row_id = gid + "__" + _viewer
    safe = "".join(ch if (ch.isascii() and (ch.isalnum() or ch in "._-")) else "_" for ch in row_id)   # 키 안전망: 무조건 ASCII
    size = os.path.getsize(video_path)
    base = os.path.join(UPLOAD_DIR, safe); os.makedirs(base, exist_ok=True)
    trimmed = 0.0
    try:
        # 로비/로딩/챔프선택 구간을 잘라 모든 영상이 '게임 시작 카운트다운' 근처에서 시작하도록.
        # (예전엔 게임 감지 녹화만 트림 → 챔프선택부터 녹화한 영상은 로딩이 남아 멀티뷰가 어긋났음)
        # _trim_lead 는 영상 끝에서 game_len_sec 을 역산하므로 두 경우 모두 동일하게 안전하게 동작.
        if dur and analysis: trimmed = _trim_lead(video_path, dur) or 0.0
    except Exception: pass
    if not sb_writable():
        REC_STATE["preparing"] = False
        log("Cloud (Supabase) not configured → keeping locally only."); return
    tmp_thumb = os.path.join(base, "thumb.jpg"); has_thumb = make_thumb(video_path, tmp_thumb)
    # 갤러리용 프리뷰(최대 1080p, 또렷한 화질)만 업로드. 원본(고화질)은 PC에 그대로 보관(다운로드 없음).
    preview = os.path.join(base, "preview.mp4")
    up_src = make_preview(video_path, preview) or video_path
    up_size = os.path.getsize(up_src)
    if up_src != video_path:
        log("Gallery video ready (1080p) \u2014 %d MB. Full-quality original kept on your PC." % (up_size // 1048576))
    REC_STATE["preparing"] = False; REC_STATE["uploading"] = True; REC_STATE["upload_pct"] = 0
    def _up_cb(done, total):
        p = int(done * 100 / total) if total else 0
        if p != REC_STATE.get("upload_pct"): REC_STATE["upload_pct"] = p
    try:
        players = analysis.get("players") or []
        me = next((p for p in players if puuid and p.get("puuid") == puuid), None)
        if not me and riot_id:
            _mn2 = riot_id.split("#")[0]
            me = next((p for p in players if p.get("name") == _mn2), None)
        won = (me or {}).get("win")
        saver = (me or {}).get("name") or riot_id.split("#")[0]
        try:
            _snaps = (live_data or {}).get("snaps") or []
            _anchor = vt0 if (vt0 and start_ts and vt0 >= start_ts) else start_ts   # 영상 첫 프레임 시각(_vt0) 기준 → 캡처/인코더 워밍업 간극 제거(없거나 stale면 start_ts로 폴백)
            _cs = [float(s.get("t") or 0) - (float(s["w"]) - _anchor) for s in _snaps if s.get("w") and _anchor]
            if _cs:
                _cs.sort(); analysis["g0"] = round(_cs[len(_cs)//2] + (trimmed or 0.0), 2)   # 영상 t=0의 gameTime(멀티뷰 정밀 정렬용)
        except Exception: pass
        _row = {
            "id": row_id, "match_id": gid, "uploader": saver,
            "uploaded": datetime.datetime.now().isoformat(timespec="seconds"),
            "video": None, "thumb": None, "replay": None,   # URL은 업로드 성공 후 채움(실패 시 재시도 큐에 그대로 저장)
            "video_size": up_size or 0,
            "map": "Summoner's Rift", "matchup": None,
            "length": _sec_mmss(dur), "length_sec": int(dur or 0),
            "type": str(analysis.get("queue") or ""),
            "winner": analysis.get("win_team"), "saver": saver,
            "np": len(players), "players": players, "won": won,
            "analysis": analysis,
        }
        log("Uploading video to cloud\u2026 (%s)" % (("%.1f GB" % (up_size / 1073741824)) if up_size >= 1073741824 else ("%d MB" % (up_size // 1048576))))
        _idp = None   # 서명 업로드용 신원 (로그인 필요 없음 — 기기 비밀키로 증명)
        try:
            _ik = riot_key(riot_id)
            if _ik:
                _idp = (_ik, device_secret())
                try:   # verify_device 가 통과하려면 identities 에 등록돼 있어야 함(멱등)
                    sb_rpc("register_identity", {"p_puuid": _ik, "p_secret": _idp[1], "p_name": riot_id, "p_icon": None})
                except Exception:
                    pass
        except Exception:
            _idp = None
        video_url = sb_upload(up_src, f"videos/{safe}.mp4", "video/mp4", on_progress=_up_cb, ident=_idp)
        thumb_url = sb_upload(tmp_thumb, f"thumbs/{safe}.jpg", "image/jpeg", ident=_idp) if has_thumb else None
        _row["video"] = video_url; _row["thumb"] = thumb_url
        _ident = riot_key(riot_id)   # 계정 = Riot ID. PC 안 가리고 같은 ID면 한 계정.
        if _ident:   # 이 PC를 인가 기기로 등록하고 소유자(riot_key) 박아 업로드. 실패 시에만 익명 폴백.
            try:
                _sec = device_secret()
                sb_rpc("register_identity", {"p_puuid": _ident, "p_secret": _sec, "p_name": riot_id, "p_icon": None})
                device_set(riot_id=riot_id)
                sb_rpc("upload_match", {"p_puuid": _ident, "p_secret": _sec, "p_row": _row})
            except Exception as _e:
                log(f"  Owner upload failed ({_e}) \u2014 falling back to anonymous upload."); sb_insert_match(_row)
        else:
            sb_insert_match(_row)
        log(f"Upload complete \u2713 \u2014 now in your gallery. ({('matchId ' + gid) if mid else 'video only, no analysis'})")
    except Exception as e:
        _em = str(e)
        log(f"Upload failed: {e}")          # ← 원본 Supabase 응답을 항상 표시(진단용; 더 이상 가리지 않음)
        if ("row-level security" in _em) or ("Unauthorized" in _em) or (" 403" in _em) or ("403:" in _em):
            log("\u2192 Upload blocked by storage policy. Check: Netlify env SUPABASE_SERVICE_KEY + storage.js deployed (signed route /api/storage). Video is kept locally and queued for retry.")
        try:   # 실패분은 큐에 저장 → 다음 실행 때 자동 재업로드 (파일은 정리 대상에서 보호됨)
            enqueue_failed_upload({"id": row_id, "safe": safe, "riot_id": riot_id or "",
                                   "video_file": up_src, "thumb_file": (tmp_thumb if has_thumb else None),
                                   "row": _row})
        except Exception as _qe:
            log("Retry-queue save failed: %s" % _qe)
    finally:
        REC_STATE["uploading"] = False; REC_STATE["preparing"] = False; REC_STATE["upload_pct"] = 0
        try: cleanup_recordings()   # 업로드 사이클이 끝날 때마다 보관 정책 적용
        except Exception: pass

def recorder_loop(cfg):
    proc = cfg.get("league_process", "League of Legends.exe")
    proxy = (cfg.get("proxy_url") or "").strip() or PROXY_DEFAULT; platform = cfg.get("platform", "kr")
    poll = float(cfg.get("poll_seconds", 4)); rec = Recorder(int(cfg.get("fps", FPS)))
    active = False; riot_id = None; start_ts = None; last_hb = 0.0; saw_game = False; cs_grace = 0.0; started_cs = False; ended_at = 0.0
    live_snaps = []; live_evts = []; last_snap_t = -999.0   # 게임 중 Live Client 스냅샷 수집용
    try: ensure_audio()
    except Exception: pass
    if not proxy:
        log("Note: proxy_url in config.json is empty. Set your Netlify proxy URL to connect analysis.")
    def _startup_maintenance():   # 준비를 막지 않도록 백그라운드에서: 버전 확인 → 실패 큐 재업로드 → 보관 정책 정리
        if _parse_ver(APP_VERSION): log("Version: %s" % APP_VERSION)
        try: check_update()
        except Exception: pass
        try: process_retry_queue()
        except Exception as e: log("Retry queue error: %s" % e)
        try: cleanup_recordings()
        except Exception: pass
    threading.Thread(target=_startup_maintenance, daemon=True).start()
    log("Ready. Start a League of Legends game and it records automatically. (Keep this window open.)")
    while True:
        try:
            run = sc_running(proc)   # 게임 인스턴스(League of Legends.exe) = 인게임
            try: in_cs = lcu_in_champ_select()   # LCU: 챔피언 선택 중? (실패하면 False → 게임 감지로 폴백)
            except Exception: in_cs = False

            # ── 녹화 시작: 게임(로딩 화면)부터. 밴픽(챔피언 선택)은 녹화하지 않음 ──
            if run and not active:
                if run:
                    log("Game detected. Checking your info\u2026")
                    for _ in range(20):
                        if penta_lol.game_active():
                            riot_id = penta_lol.my_riot_id() or riot_id; break
                        time.sleep(1)
                    _bind_account(riot_id)
                    started_cs = False
                else:
                    log("Champion select detected \u2014 recording from draft.")
                    started_cs = True
                _free = disk_free_gb()
                if _free is not None and _free < 3.0:
                    log("WARNING: low disk space - %.1f GB free. Recording may fail. Cleaning old originals..." % _free)
                    try: cleanup_recordings()
                    except Exception: pass
                start_ts = time.time(); active = rec.start(); last_hb = time.time()
                REC_STATE["skipped_at"] = 0; REC_STATE["uploaded_at"] = 0   # 새 녹화 시작 → 이전 알림 지움
                saw_game = run; cs_grace = 0.0; ended_at = 0.0
                live_snaps = []; live_evts = []; last_snap_t = -999.0

            if active and run:
                saw_game = True; cs_grace = 0.0
                if not rec._recording():
                    log("Recording stream dropped \u2192 restarting automatically.")
                    active = rec.start()
                if not riot_id and penta_lol.game_active():
                    riot_id = penta_lol.my_riot_id(); _bind_account(riot_id)
                # Live Client 스냅샷(약 25초 간격) + 이벤트(매 루프) 수집
                try:
                    snap = penta_lol.live_snapshot()
                    if snap and (float(snap.get("t") or 0) - last_snap_t) >= 25:
                        snap["w"] = time.time(); live_snaps.append(snap); last_snap_t = float(snap.get("t") or 0)
                    _ev = penta_lol.live_events()      # 매 루프(4초) 폴링 → 게임 끝 GameEnd(승패) 안 놓침
                    if _ev:
                        if len(_ev) >= len(live_evts): live_evts = _ev   # 누락 방지: 가장 긴(누적) 이벤트 목록 유지 → 타워/전령 등 오브젝트 안 놓침
                        if not ended_at and any((e.get("EventName") == "GameEnd") for e in _ev):
                            ended_at = time.time(); log("Victory/Defeat screen detected - finishing recording.")
                except Exception: pass

            # 게임 종료: "계속" 클릭(프로세스 종료) 또는 GameEnd(승리/패배 화면) 후 잠깐 - 둘 중 먼저. 늘어짐 방지.
            _game_over = active and saw_game and ((not run) or (ended_at and (time.time()-ended_at) >= float(cfg.get("result_tail", 8))))
            if _game_over:
                # GameEnd로 결과 화면을 이미 담았으면 추가 tail 생략, 아니면(감지 실패) 기존처럼 잠깐 더.
                _tail = (0.0 if ended_at else float(cfg.get("postgame_tail", 6)))
                if _tail > 0:
                    log("Game over \u2014 capturing post-game screen\u2026")   # (매직 문자열 없음 → 녹화 상태 유지)
                    time.sleep(_tail)
                log("Game ended \u2014 recorded %d:%02d." % divmod(int(time.time()-(start_ts or time.time())),60))
                _vt0 = rec._vt0; vid = rec.stop(); active = False; rec.verified = False; saw_game = False; cs_grace = 0.0; ended_at = 0.0
                end_ts = time.time()
                if vid and os.path.isfile(vid):
                    threading.Thread(target=ingest_lol, args=(vid, riot_id, start_ts, end_ts, proxy, platform), kwargs={"live_data": {"snaps": list(live_snaps), "events": list(live_evts)}, "started_cs": started_cs, "vt0": _vt0}, daemon=True).start()
                riot_id = None; start_ts = None; started_cs = False; log("Idle.")

            elif active and not run and in_cs:
                cs_grace = 0.0   # 아직 챔피언 선택 → 계속 녹화
                if not rec._recording(): active = rec.start()

            elif active and not run:
                # 챔피언 선택은 끝났는데 게임 프로세스가 아직 없음 = 로딩 중 or 닷지/취소
                cs_grace += poll
                if cs_grace > 120:   # 2분간 게임이 안 뜨면 닷지로 보고 클립 폐기(ingest 안 함)
                    log("Champion select ended without a game (dodge?) \u2014 discarding clip.")
                    _v = rec.stop(); active = False; saw_game = False; cs_grace = 0.0; started_cs = False
                    try:
                        if _v and os.path.isfile(_v): os.remove(_v)
                    except Exception: pass
                    riot_id = None; start_ts = None; log("Idle.")
                elif not rec._recording():
                    active = rec.start()

            # ── 웹 표시용 실시간 상태 갱신 ──
            if active and rec._recording():
                REC_STATE.update(rec=True, text=("Recording" if saw_game else "Recording (champ select)"))
                if start_ts and (time.time()-last_hb)>=300:    # 녹화 중 5분마다 진행 로그(타임라인에 살아있음)
                    last_hb=time.time(); log("Recording\u2026 %d:%02d elapsed." % divmod(int(time.time()-start_ts),60))
            elif run:
                REC_STATE.update(rec=False, text="Game detected")
            elif in_cs:
                REC_STATE.update(rec=False, text="Champion select (recording starts at game)")
            else:
                REC_STATE.update(rec=False, text="Idle \u2014 auto-records when a game starts")
            time.sleep(poll)
        except KeyboardInterrupt:
            log("Quitting."); rec.stop(); break
        except Exception:
            log("Error:\n" + traceback.format_exc()); time.sleep(poll)

# ===================== main =====================

_MUTEX = None
def _single_instance():
    """이미 실행 중이면 False (자동 실행 + 수동 더블클릭 겹침 방지)."""
    global _MUTEX
    if sys.platform != "win32": return True
    try:
        import ctypes
        _MUTEX = ctypes.windll.kernel32.CreateMutexW(None, False, "PENTA_Recorder_SingleInstance")
        return ctypes.windll.kernel32.GetLastError() != 183   # 183 = ERROR_ALREADY_EXISTS
    except Exception:
        return True

def _autostart_cmd():
    """자동 실행에 등록할 명령. 빌드된 .exe(frozen)일 때만 반환 (개발 모드는 등록 안 함)."""
    if getattr(sys, "frozen", False):
        return '"%s"' % os.path.abspath(sys.executable)
    return None

def set_autostart(enable):
    """윈도우 시작 시 자동 실행 등록/해제 (HKCU Run). 실패해도 앱엔 영향 없음."""
    if sys.platform != "win32": return False
    try:
        import winreg
        key = winreg.OpenKey(winreg.HKEY_CURRENT_USER,
                             r"Software\Microsoft\Windows\CurrentVersion\Run",
                             0, winreg.KEY_SET_VALUE | winreg.KEY_QUERY_VALUE)
        if enable:
            cmd = _autostart_cmd()
            if not cmd: winreg.CloseKey(key); return False
            winreg.SetValueEx(key, "PENTA", 0, winreg.REG_SZ, cmd)
            log("Registered to run at Windows startup (set autostart to false in config.json to disable)")
        else:
            try: winreg.DeleteValue(key, "PENTA")
            except FileNotFoundError: pass
        winreg.CloseKey(key)
        return True
    except Exception as e:
        log(f"Skipped autostart setup: {e}")
        return False

def is_autostart():
    if sys.platform != "win32": return False
    try:
        import winreg
        key = winreg.OpenKey(winreg.HKEY_CURRENT_USER,
                             r"Software\Microsoft\Windows\CurrentVersion\Run",
                             0, winreg.KEY_QUERY_VALUE)
        v, _ = winreg.QueryValueEx(key, "PENTA"); winreg.CloseKey(key)
        return bool(v)
    except Exception:
        return False

def _apply_autostart(cfg):
    """더 이상 시작프로그램에 자동 등록하지 않는다. 이전 버전이 등록해 둔 항목이 있으면 제거한다."""
    if not getattr(sys, "frozen", False): return
    try:
        if is_autostart():
            set_autostart(False)
            log("Removed PENTA from Windows startup programs.")
    except Exception:
        pass

def _hide_console():
    if sys.platform != "win32": return
    try:
        import ctypes
        hwnd = ctypes.windll.kernel32.GetConsoleWindow()
        if hwnd: ctypes.windll.user32.ShowWindow(hwnd, 0)   # SW_HIDE
    except Exception: pass

def _load_recorder_fonts():
    """Register bundled .ttf fonts so Tkinter can use Sora / IBM Plex Mono (Windows only)."""
    if sys.platform != "win32": return
    try:
        import ctypes
        base = getattr(sys, "_MEIPASS", os.path.dirname(os.path.abspath(__file__)))
        fdir = os.path.join(base, "fonts")
        if not os.path.isdir(fdir): return
        for fn in os.listdir(fdir):
            if fn.lower().endswith((".ttf", ".otf")):
                ctypes.windll.gdi32.AddFontResourceExW(os.path.join(fdir, fn), 0x10, 0)
    except Exception: pass


def start_login_bridge(url):
    """127.0.0.1 미니 서버: 웹의 [이 PC의 레코더로 로그인] 버튼이 여기로 코드를 받아 즉시 로그인."""
    import http.server
    allowed = {"https://mypenta.netlify.app"}
    try:
        from urllib.parse import urlparse
        _u = urlparse(url if "://" in (url or "") else "https://" + (url or ""))
        if _u.scheme and _u.netloc: allowed.add(_u.scheme + "://" + _u.netloc)
    except Exception: pass
    class _H(http.server.BaseHTTPRequestHandler):
        def log_message(self, *a): pass
        def do_GET(self):
            if not self.path.startswith("/login"):
                self.send_response(404); self.end_headers(); return
            body = {}
            try:
                _pu = riot_key(device_get("riot_id")); _sec = device_secret()
                if _pu and _sec and cloud_state() == "cloud":
                    _code = secrets.token_urlsafe(24)
                    sb_rpc("issue_login_code", {"p_puuid": _pu, "p_secret": _sec, "p_code": _code})
                    body = {"code": _code}
                elif not _pu:
                    body = {"error": "no identity yet - play one game first"}
                else:
                    body = {"error": "cloud not configured"}
            except Exception as e:
                body = {"error": str(e)}
            data = json.dumps(body).encode("utf-8")
            _o = self.headers.get("Origin") or ""
            self.send_response(200)
            self.send_header("Access-Control-Allow-Origin", _o if _o in allowed else "null")
            self.send_header("Content-Type", "application/json")
            self.send_header("Content-Length", str(len(data)))
            self.end_headers()
            try: self.wfile.write(data)
            except Exception: pass
    for _port in (47821, 47822, 47823):
        try:
            _srv = http.server.ThreadingHTTPServer(("127.0.0.1", _port), _H)
            threading.Thread(target=_srv.serve_forever, daemon=True).start()
            log("Web login bridge ready on 127.0.0.1:%d" % _port)
            return _srv
        except Exception:
            continue
    log("Web login bridge could not start (ports busy) - Archive button still works")
    return None


def run_gui(cfg, url):
    """Compact status bar. Shows status only; expands the log when something needs attention."""
    import tkinter as tk
    import tkinter.font as _tkfont
    BG="#080A0E"; SURF="#161B23"
    INK="#ECE7DD"; INK2="#C2BBAD"; SUB="#D7D0C1"; DIM="#867F71"; FAINT="#564F44"
    GOLD="#DEC79C"; GOLD2="#EFDDBC"; REC="#FF5470"; TEAL="#52C3AC"; BLUE="#6BA6FF"; LINE="#1F252E"; LINE2="#2B323C"
    W=466
    _load_recorder_fonts()
    root=tk.Tk(); root.title("myPENTA"); root.configure(bg=BG)
    try: root.iconphoto(True, tk.PhotoImage(data=_PENTA_ICON))
    except Exception: pass
    try:
        _fam=set(_tkfont.families())
        def _pick(*c):
            for f in c:
                if f in _fam: return f
            return c[-1]
    except Exception:
        def _pick(*c): return c[0]
    SG  =_pick("Sora","Segoe UI")
    SG_M=_pick("Sora Medium","Sora","Segoe UI Semibold","Segoe UI")
    SG_S=_pick("Sora SemiBold","Sora","Segoe UI Semibold","Segoe UI")
    PLEX=_pick("IBM Plex Mono","Consolas","Segoe UI")
    UI=SG; SEMI=SG_S; MON=PLEX
    BASE_H, SET_H, LOG_H = 160, 222, 210
    root.geometry(f"{W}x{BASE_H}"); root.resizable(False, True)
    st={"log":False,"settings":False}

    # === Top accent: a thin line turns red while recording (visible even if the window is half-covered) ===
    topacc=tk.Frame(root,bg=BG,height=2); topacc.pack(fill="x")

    # === Ambient scene: the Summoner's Rift sits behind the status + toolbar; it warms to Baron red while recording. ===
    # Everything in the compact bar is drawn ON this canvas so text/buttons float over the art (no opaque boxes).
    SCENE_W,SCENE_H=466,150
    try:
        _img_cool=tk.PhotoImage(data=_RIFT_COOL_B64); _img_warm=tk.PhotoImage(data=_RIFT_WARM_B64)
    except Exception:
        _img_cool=_img_warm=None
    try: _img_scrim=tk.PhotoImage(data=_SCRIM_B64)
    except Exception: _img_scrim=None
    cv=tk.Canvas(root,width=SCENE_W,height=SCENE_H,bg=BG,highlightthickness=0,bd=0); cv.pack(fill="x")
    cv._imgs=(_img_cool,_img_warm,_img_scrim)            # keep refs from GC
    _bgitem=cv.create_image(0,0,anchor="nw",image=_img_cool) if _img_cool is not None else None
    if _img_scrim is not None: cv.create_image(0,0,anchor="nw",image=_img_scrim)   # 상/하단 막: 텍스트보다 아래, 크리스털(중앙)은 투명
    _tint=cv.create_rectangle(0,0,SCENE_W,SCENE_H,fill=BLUE,outline="",stipple="gray25",state="hidden")   # 업로드 단계 배경 틴트(준비중~업로드중)
    _SC={"warm":False}
    _ICONS={}                                  # filled when the toolbar icons are built (open-state highlight)

    # status line drawn on the canvas: light + status + sub (left) | preset toggle + cloud (right)
    _LY=30
    halo=cv.create_oval(15,_LY-7,29,_LY+7,fill="#3a2f18",outline="")   # 점 뒤 은은한 글로우(상태색에 맞춰 변함)
    did=cv.create_oval(18,_LY-4,26,_LY+4,fill=GOLD,outline="")
    status_lbl=cv.create_text(33,_LY,anchor="w",text="Starting\u2026",fill=INK,font=(SG_S,12))
    sub_lbl=cv.create_text(120,_LY+1,anchor="w",text="",fill=SUB,font=(SG_M,9))
    _cs=cloud_state()
    _cmap={"cloud":(TEAL,"\u2601 Cloud"),"readonly":(GOLD,"\u26a0 Key needed"),"local":(INK2,"\u25cf Local")}
    _cc,_ct=_cmap.get(_cs,_cmap["local"])
    if _cs=="cloud":                                  # 연결됨 → 틸 배지: 작은 클라우드 아이콘 + Cloud(9px)
        _ctxt=cv.create_text(SCENE_W-17,_LY,anchor="e",text="Cloud",fill="#7adcc6",font=(SG_M,9))
        _ctb=cv.bbox(_ctxt)
        _cico=cv.create_text(_ctb[0]-2,_LY,anchor="e",text="\u2601",fill="#7adcc6",font=(SG_M,7))   # 아이콘만 작게
        _cib=cv.bbox(_cico)
        x1,y1,x2,y2,rr=_cib[0]-6,_LY-8,_ctb[2]+6,_LY+8,8
        _pill=cv.create_polygon([x1+rr,y1,x2-rr,y1,x2,y1,x2,y1+rr,x2,y2-rr,x2,y2,x2-rr,y2,x1+rr,y2,x1,y2,x1,y2-rr,x1,y1+rr,x1,y1],smooth=True,fill="#12231e",outline="#46a08f",width=1)
        cv.tag_raise(_ctxt); cv.tag_raise(_cico)      # 텍스트/아이콘을 pill 위로
        cloud_lbl=_pill                               # poll이 opt를 pill 기준으로 배치
    else:
        cloud_lbl=cv.create_text(SCENE_W-17,_LY,anchor="e",text=_ct,fill=_cc,font=(SG_M,9))
    opt_lbl=cv.create_text(SCENE_W-92,_LY,anchor="e",text="",fill=GOLD2,font=(PLEX,9))
    cv.tag_bind(opt_lbl,"<Button-1>",lambda e: toggle_settings())
    cv.tag_bind(opt_lbl,"<Enter>",lambda e:(cv.itemconfig(opt_lbl,fill="#FFFFFF"),cv.config(cursor="hand2")))
    cv.tag_bind(opt_lbl,"<Leave>",lambda e:(cv.itemconfig(opt_lbl,fill=GOLD if st["settings"] else GOLD2),cv.config(cursor="")))

    # === Log area (hidden until needed) ===
    logwrap=tk.Frame(root,bg=BG)
    errbar=tk.Label(logwrap,text="",bg="#3A1E18",fg="#FFB4A6",font=(UI,9),anchor="w",padx=11,pady=6,justify="left",wraplength=W-44)
    logtxt=tk.Text(logwrap,bg="#0C0D10",fg=DIM,font=(MON,9),bd=0,padx=11,pady=8,height=8,wrap="word",state="disabled")

    # === Update banner (새 버전 발견 시 하단에 표시, 클릭 → 다운로드 페이지) ===
    UPD_H=34
    updbar=tk.Label(root,text="",bg="#2A2410",fg=GOLD,font=(UI,9,"bold"),anchor="w",padx=12,pady=7,cursor="hand2")
    updbar.bind("<Button-1>",lambda e: open_app(UPDATE_INFO.get("url") or url))

    # === Callbacks ===
    def open_gallery():
        def _go():
            try:
                _pu = riot_key(device_get("riot_id")); _sec = device_secret()
                if _pu and _sec and cloud_state() == "cloud":
                    _code = secrets.token_urlsafe(24)
                    sb_rpc("issue_login_code", {"p_puuid": _pu, "p_secret": _sec, "p_code": _code})
                    open_app(url + "/#code=" + _code); return
                else:
                    log("Login skipped: riot_id=" + ("set" if _pu else "MISSING (play one game first)") + ", cloud=" + cloud_state())
            except Exception as e:
                log(f"Login code skipped (gallery still opens): {e}")
            try: open_app(url)
            except Exception: pass
        threading.Thread(target=_go, daemon=True).start()
    def open_folder():
        try:
            if sys.platform=="win32": os.startfile(REC_DIR)
        except Exception: pass
    def do_quit():
        try:   # 녹화/업로드 중 실수로 닫아 판을 날리는 것 방지
            if REC_STATE.get("recording") or REC_STATE.get("uploading") or REC_STATE.get("preparing"):
                from tkinter import messagebox
                _what = "Recording" if REC_STATE.get("recording") else "Upload"
                if not messagebox.askyesno("myPENTA",
                        _what + " is in progress.\nQuit anyway? The current game may be lost.",
                        parent=root):
                    return
        except Exception:
            pass
        try: root.destroy()
        except Exception: pass
        os._exit(0)
    def _save_cfg():
        try: _atomic_write_json(CONFIG_PATH, cfg)
        except Exception as e: log(f"Failed to save settings: {e}")

    # === Settings panel (collapsible) ===
    PANEL="#0B0C0F"
    optwrap=tk.Frame(root,bg=PANEL,highlightbackground=LINE,highlightthickness=1)
    tk.Label(optwrap,text="RECORDING SETTINGS",bg=PANEL,fg=INK2,font=(SEMI,8,"bold")).pack(anchor="w",padx=15,pady=(12,6))
    SCALE_OPTS=[("Auto (best)","auto"),("Source","source"),("1080p","1080"),("720p","720"),("480p","480")]
    ENC_OPTS=[("Auto (GPU first)","auto"),("GPU \u00b7 NVIDIA NVENC","nvenc"),("GPU \u00b7 AMD AMF","amf"),("GPU \u00b7 Intel QSV","qsv"),("CPU \u00b7 x264","x264")]
    def opt_row(label, opts, key):
        row=tk.Frame(optwrap,bg=PANEL); row.pack(fill="x",padx=15,pady=3)
        tk.Label(row,text=label,bg=PANEL,fg=DIM,font=(UI,9),width=8,anchor="w").pack(side="left")
        cur=str(cfg.get(key,"auto")); m={l:v for l,v in opts}
        curlbl=next((l for l,v in opts if v==cur), opts[0][0])
        var=tk.StringVar(value=curlbl)
        def on_sel(lbl,k=key,mp=m,lb=label):
            cfg[k]=mp[lbl]; _save_cfg(); log(f"Setting: {lb} \u2192 {lbl} (applies to next recording)")
        om=tk.OptionMenu(row,var,*[l for l,_ in opts],command=on_sel)
        om.config(bg="#181B21",fg=INK,font=(UI,9),activebackground="#23272F",activeforeground=INK,relief="flat",bd=0,highlightthickness=1,highlightbackground=LINE2,anchor="w",padx=11,pady=4,cursor="hand2")
        try: om["menu"].config(bg=SURF,fg=INK,activebackground=GOLD,activeforeground="#080A0E",font=(UI,9),bd=0,activeborderwidth=0)
        except Exception: pass
        om.pack(side="left",fill="x",expand=True)
    opt_row("Quality",SCALE_OPTS,"scale")
    opt_row("Encoder",ENC_OPTS,"encoder")
    KEEP_OPTS=[("Last 20 games","20"),("Last 10 games","10"),("Last 50 games","50"),("Keep all (no cleanup)","0")]
    opt_row("Keep",KEEP_OPTS,"keep_games")
    tk.Label(optwrap,text="Auto = best quality, GPU-accelerated so your game stays smooth",bg=PANEL,fg=FAINT,font=(UI,8),wraplength=W-50,justify="left").pack(anchor="w",padx=15,pady=(6,4))
    _cleanrow=tk.Frame(optwrap,bg=PANEL); _cleanrow.pack(fill="x",padx=15,pady=(0,12))
    def _do_clean():
        threading.Thread(target=cleanup_recordings,kwargs={"manual":True},daemon=True).start()
    tk.Button(_cleanrow,text="Clean up originals now",command=_do_clean,bg="#181B21",fg=INK,font=(UI,9),relief="flat",bd=0,highlightthickness=1,highlightbackground=LINE2,activebackground="#23272F",activeforeground=INK,cursor="hand2",padx=11,pady=4).pack(side="left")
    _fr=disk_free_gb()
    tk.Label(_cleanrow,text=("Disk free: %.0f GB" % _fr) if _fr is not None else "",bg=PANEL,fg=FAINT,font=(UI,8)).pack(side="left",padx=10)

    # === Panel toggle + resize ===
    def _resize():
        h=BASE_H+(SET_H if st["settings"] else 0)+(LOG_H if st["log"] else 0)+(UPD_H if st.get("upd") else 0)
        root.geometry(f"{W}x{h}")
    def set_log(open_):
        if open_ and st["settings"]: set_settings(False)
        st["log"]=open_
        if open_:
            logwrap.pack(fill="both",expand=True,padx=12,pady=(0,8))
            if LAST_ERR.get("msg"): errbar.config(text="\u26a0 "+LAST_ERR["msg"]); errbar.pack(fill="x",pady=(0,5))
            else: errbar.pack_forget()
            logtxt.pack(fill="both",expand=True)
        else:
            logwrap.pack_forget()
        _li=_ICONS.get("log")
        if _li:
            try: cv.itemconfig(_li[0],fill=GOLD if open_ else ICON_FG); cv.itemconfig(_li[1],outline=ICON_ON if open_ else ICON_BORD)
            except Exception: pass
        _resize()
    def set_settings(open_):
        if open_ and st["log"]: set_log(False)
        st["settings"]=open_
        if open_: optwrap.pack(fill="x",padx=13,pady=(3,2)); cv.itemconfig(opt_lbl,fill=GOLD)
        else: optwrap.pack_forget(); cv.itemconfig(opt_lbl,fill=GOLD2)
        _si=_ICONS.get("set")
        if _si:
            try: cv.itemconfig(_si[0],fill=GOLD if open_ else ICON_FG); cv.itemconfig(_si[1],outline=ICON_ON if open_ else ICON_BORD)
            except Exception: pass
        _resize()
    def toggle_log(): set_log(not st["log"])
    def toggle_settings(): set_settings(not st["settings"])

    # === Toolbar drawn on the scene canvas (bottom edge), over the scrim. Quit = window close (X). ===
    ICON_FG="#9A9282"; ICON_BORD="#232A34"; ICON_ON="#4A4131"
    _BY=SCENE_H-24
    def _rrect(x1,y1,x2,y2,r):
        return cv.create_polygon([x1+r,y1,x2-r,y1,x2,y1,x2,y1+r,x2,y2-r,x2,y2,x2-r,y2,x1+r,y2,x1,y2,x1,y2-r,x1,y1+r,x1,y1],smooth=True)
    def _ctip(item,text):
        tip={"w":None}
        def show(_e):
            if tip["w"] or not text: return
            try:
                bx=cv.bbox(item); x=cv.winfo_rootx()+(bx[0]+bx[2])//2-len(text)*3; y=cv.winfo_rooty()+bx[1]-23
                w=tk.Toplevel(cv); w.wm_overrideredirect(True); w.configure(bg=LINE2)
                tk.Label(w,text=text,bg="#1B2129",fg=INK,font=(SG,8),padx=7,pady=2).pack(padx=1,pady=1)
                w.wm_geometry("+%d+%d"%(max(0,x),max(0,y))); tip["w"]=w
            except Exception: pass
        def hide(_e):
            if tip["w"]:
                try: tip["w"].destroy()
                except Exception: pass
                tip["w"]=None
        cv.tag_bind(item,"<Enter>",show,add="+"); cv.tag_bind(item,"<Leave>",hide,add="+")
    def _ic_archive(cx,cy,col):                       # 갤러리 = 2x2 격자
        s=2.3; out=[]
        for dx in (-(s+0.7),0.7):
            for dy in (-(s+0.7),0.7):
                out.append(cv.create_rectangle(cx+dx,cy+dy,cx+dx+s,cy+dy+s,fill=col,outline=""))
        return out
    def _ic_folder(cx,cy,col):                        # 폴더 실루엣(왼쪽 탭)
        return [cv.create_polygon([cx-5,cy+4,cx-5,cy-4,cx-1,cy-4,cx,cy-2,cx+5,cy-2,cx+5,cy+4],fill=col,outline="")]
    def _cbtn(text,x,cmd,gold=False,icon=None):
        if gold: fg="#DEC79C"; fgh="#EFDDBC"; fill="#16130c"; fillh="#1e1a10"; outl="#5d4f2e"; outlh="#7a6838"
        else:    fg=SUB; fgh=INK; fill="#10141b"; fillh="#181d26"; outl=LINE2; outlh="#3A434F"
        pad=11; ix=x+pad
        icon_items=icon(ix+4,_BY,fg) if icon else []
        t=cv.create_text(ix+(13 if icon else 0),_BY,anchor="w",text=text,fill=fg,font=(SG_S,9))
        x2=cv.bbox(t)[2]+pad; r=_rrect(x,_BY-13,x2,_BY+13,8)
        cv.itemconfig(r,fill=fill,outline=outl,width=1); cv.tag_lower(r,t)
        for it in icon_items: cv.tag_raise(it)                                   # 아이콘을 버튼 배경 위로
        hi=None  # 아웃라인 골드 → 상단 하이라이트 불필요
        g="g%d"%t
        for it in [t,r,*icon_items]+([hi] if hi else []): cv.addtag_withtag(g,it)
        def _en(_e):
            cv.itemconfig(t,fill=fgh); cv.itemconfig(r,fill=fillh,outline=outlh)
            for it in icon_items: cv.itemconfig(it,fill=fgh)
            cv.config(cursor="hand2")
        def _lv(_e):
            cv.itemconfig(t,fill=fg); cv.itemconfig(r,fill=fill,outline=outl)
            for it in icon_items: cv.itemconfig(it,fill=fg)
            cv.config(cursor="")
        cv.tag_bind(g,"<Button-1>",lambda e: cmd()); cv.tag_bind(g,"<Enter>",_en); cv.tag_bind(g,"<Leave>",_lv)
        return cv.bbox(r)[2]
    def _cicon(glyph,xr,cmd,statekey,tip):
        t=cv.create_text(xr-16,_BY,text=glyph,fill=ICON_FG,font=(SG_S,12))
        r=_rrect(xr-30,_BY-13,xr-2,_BY+13,7); cv.itemconfig(r,fill="#10141b",outline=ICON_BORD,width=1); cv.tag_lower(r,t)
        g="g%d"%t; cv.addtag_withtag(g,t); cv.addtag_withtag(g,r)
        def _act(): return st.get(statekey)
        cv.tag_bind(g,"<Button-1>",lambda e: cmd())
        cv.tag_bind(g,"<Enter>",lambda e:(cv.itemconfig(t,fill=GOLD2 if _act() else GOLD),cv.itemconfig(r,outline="#3A434F"),cv.config(cursor="hand2")))
        cv.tag_bind(g,"<Leave>",lambda e:(cv.itemconfig(t,fill=GOLD if _act() else ICON_FG),cv.itemconfig(r,outline=ICON_ON if _act() else ICON_BORD),cv.config(cursor="")))
        _ctip(r,tip)
        return (t,r)
    _bx=16
    _bx=_cbtn("Archive",_bx,open_gallery,gold=True,icon=_ic_archive)+8
    _cbtn("Open folder",_bx,open_folder,icon=_ic_folder)
    _ICONS["log"]=_cicon("\u25A4",SCENE_W-16,toggle_log,"log","Log")
    _ICONS["set"]=_cicon("\u2699",SCENE_W-52,toggle_settings,"settings","Settings")
    root.protocol("WM_DELETE_WINDOW", do_quit)   # ✕ → 종료. 최소화(_)는 일반 작업표시줄.

    def _prep_and_run():
        global FFMPEG
        try:
            if not FFMPEG: FFMPEG=ensure_ffmpeg()
        except Exception as e:
            log(f"Tool setup issue: {e}")
        if not FFMPEG:
            log("\u26a0 ffmpeg setup failed \u2014 check your internet connection and restart."); return
        if cloud_state()=="cloud": cloud_selftest()    # 시작 시 업로드 권한 즉시 진단(게임 없이)
        recorder_loop(cfg)
    threading.Thread(target=_prep_and_run,daemon=True).start()

    _rec={"since":None,"blink":False,"psince":None}
    def poll():
        if UPDATE_INFO.get("tag") and not st.get("upd"):
            st["upd"]=True
            updbar.config(text="New version "+UPDATE_INFO["tag"]+" is available - click here to download")
            updbar.pack(fill="x",side="bottom")
            _resize()
        appended=False
        for _ in range(150):
            try: line=GUI_Q.get_nowait()
            except Exception: break
            logtxt.config(state="normal"); logtxt.insert("end",line+"\n"); appended=True   # 패널이 닫혀 있어도 항상 기록 → 열면 전체 타임라인이 보임
        if appended:
            n=int(logtxt.index("end-1c").split(".")[0])
            if n>300: logtxt.delete("1.0",f"{n-300}.0")
            logtxt.see("end"); logtxt.config(state="disabled")
        _rng=REC_STATE.get("recording"); _now=time.time(); _up=REC_STATE.get("uploaded_at",0)
        if _rng and not _rec.get("was_rec"):       # 녹화 시작 순간 → 로그창 자동으로 열어 활동을 보여줌(에러처럼)
            if not st["log"]: set_log(True)
        _rec["was_rec"]=_rng
        _uploading=bool(REC_STATE.get("uploading")); _preparing=bool(REC_STATE.get("preparing")); _doneToast=bool(_up and (_now-_up<5))
        # 단계 구분은 '상단 얇은 라인 색'으로만: 녹화=빨강, 업로드=파랑, 완료=틸. (배경 틴트는 제거 — 화면을 안 건드려 깔끔)
        if _rng: _acc=REC
        elif _uploading or _preparing: _acc=BLUE
        elif _doneToast: _acc=TEAL
        else: _acc=BG
        topacc.config(bg=_acc)
        try:
            cv.itemconfig(_tint,state="hidden")   # 업로드 중에도 배경은 그대로 — 상단 라인 색 + "Uploading %" 텍스트만으로 표시
        except Exception: pass
        _warm=bool(_rng)                          # 녹화 중에는 협곡이 바론 핏빛으로 물듦
        if _bgitem is not None and _warm!=_SC["warm"]:
            try: cv.itemconfig(_bgitem,image=(_img_warm if _warm else _img_cool)); _SC["warm"]=_warm
            except Exception: pass
        if _rng:
            if _rec["since"] is None: _rec["since"]=_now
            _rec["psince"]=None
            _rec["blink"]=not _rec["blink"]
            cv.itemconfig(did,fill=(REC if _rec["blink"] else "#3a2230")); cv.itemconfig(halo,fill="#2e1622")
            _el=int(_now-_rec["since"]); _mm,_ss=divmod(_el,60)
            cv.itemconfig(status_lbl,text="Recording",fill=REC); cv.itemconfig(sub_lbl,text="%d:%02d"%(_mm,_ss),fill=INK)
        elif REC_STATE.get("uploading"):   # 업로드 중 — 진행률(파란 점멸)
            _rec["since"]=None
            if _rec["psince"] is None: _rec["psince"]=_now
            _rec["blink"]=not _rec["blink"]
            cv.itemconfig(did,fill=(BLUE if _rec["blink"] else "#22344f")); cv.itemconfig(halo,fill="#162640")
            _pct=REC_STATE.get("upload_pct") or 0
            _pm,_ps=divmod(int(_now-_rec["psince"]),60)
            cv.itemconfig(status_lbl,text=("Uploading %d%%"%_pct) if _pct>0 else "Uploading",fill=BLUE)
            cv.itemconfig(sub_lbl,text="%d:%02d"%(_pm,_ps),fill=SUB)
        elif _preparing:   # 업로드 준비중 — 분석·트림·인코딩(파란 점멸, 진행률 없음)
            _rec["since"]=None
            if _rec["psince"] is None: _rec["psince"]=_now
            _rec["blink"]=not _rec["blink"]
            cv.itemconfig(did,fill=(BLUE if _rec["blink"] else "#22344f")); cv.itemconfig(halo,fill="#162640")
            _pm,_ps=divmod(int(_now-_rec["psince"]),60)
            cv.itemconfig(status_lbl,text="Processing\u2026",fill=BLUE); cv.itemconfig(sub_lbl,text="%d:%02d"%(_pm,_ps),fill=SUB)
        elif REC_STATE.get("skipped_at") and (_now-REC_STATE.get("skipped_at",0) < 6):   # 저장 안 함 알림(6초)
            _rec["since"]=None; _rec["blink"]=False; _rec["psince"]=None
            _why=REC_STATE.get("skipped_why") or "not saved"
            cv.itemconfig(did,fill="#C8A86A"); cv.itemconfig(halo,fill="#3a2f18")
            cv.itemconfig(status_lbl,text="Not saved",fill="#DEC79C"); cv.itemconfig(sub_lbl,text=_why[:44],fill=SUB)
        elif _up and (_now-_up < 5):   # 업로드 완료 토스트(5초)
            _rec["since"]=None; _rec["blink"]=False; _rec["psince"]=None
            cv.itemconfig(did,fill=TEAL); cv.itemconfig(halo,fill="#163029"); cv.itemconfig(status_lbl,text="Uploaded \u2713",fill=TEAL); cv.itemconfig(sub_lbl,text="added to your archive",fill=SUB)
        else:
            _rec["since"]=None; _rec["blink"]=False; _rec["psince"]=None
            cv.itemconfig(did,fill=GOLD); cv.itemconfig(halo,fill="#3a2f18")
            if REC_STATE.get("ready"):
                cv.itemconfig(status_lbl,text="Ready",fill=INK); cv.itemconfig(sub_lbl,text="auto-records",fill=SUB)
            else:
                cv.itemconfig(status_lbl,text="Preparing\u2026",fill=INK); cv.itemconfig(sub_lbl,text="setting up tools",fill=SUB)
        try:
            _st=cv.itemcget(status_lbl,"text")          # 글자가 바뀔 때만 재배치 → 매 프레임 1px 흔들림(왔다갔다) 방지
            if _st!=_rec.get("txt"):
                _rec["txt"]=_st; _b=cv.bbox(status_lbl)
                if _b: cv.coords(sub_lbl,_b[2]+11,_LY+1)
        except Exception: pass
        ea=(REC_STATE.get("encoder") or "").lower()
        if "nvenc" in ea: enc="NVENC"; is_sw=False
        elif "amf" in ea: enc="AMF"; is_sw=False
        elif "qsv" in ea: enc="QSV"; is_sw=False
        elif ("x264" in ea) or ("264" in ea): enc="x264"; is_sw=True
        else:
            ec=str(cfg.get("encoder","auto")).lower()
            if ec=="x264": enc="x264"; is_sw=True
            else: enc="NVENC"; is_sw=False
        try: _sh=root.winfo_screenheight()
        except Exception: _sh=1080
        _scl=str(cfg.get("scale","auto")).lower().rstrip("p")
        if _scl in ("source","native","full","off","0"): th=_sh
        elif _scl in ("1080","720","480","1440"): th=int(_scl)
        else: th=(720 if is_sw else min(1080,_sh))   # auto: NVENC면 1080p, 소프트웨어면 720p (실제 인코딩과 동일)
        if th>_sh: th=_sh
        _optxt=f"{th}p \u00b7 {enc}"
        if _optxt!=_rec.get("opt"):                       # 해상도/인코더 라벨도 바뀔 때만 재배치 → 흔들림 방지
            _rec["opt"]=_optxt; cv.itemconfig(opt_lbl,text=_optxt)
            try:
                _cb=cv.bbox(cloud_lbl)
                if _cb: cv.coords(opt_lbl,_cb[0]-13,_LY)
            except Exception: pass
        if LAST_ERR.get("msg") and (time.time()-LAST_ERR.get("t",0)<8):
            if not st["log"]: set_log(True)
            else: errbar.config(text="\u26a0 "+LAST_ERR["msg"])
        root.after(500,poll)

    try: root.update()
    except Exception: pass
    try:  # 내용에 맞춰 창 높이 자동 — 푸터(Settings/Log/Quit)가 빈 공간 없이 바로 아래 붙도록
        root.update_idletasks(); _rh=root.winfo_reqheight()
        if _rh>=60: BASE_H=_rh; root.geometry(f"{W}x{BASE_H}")
    except Exception: pass
    if sys.platform=="win32": _hide_console()
    poll()
    try: root.mainloop()
    except Exception as ex: log(f"GUI closed: {ex}")


def _print_status():
    s = sb_cfg(); st = cloud_state()
    print("\n" + "=" * 50)
    print("  myPENTA status check")
    print("=" * 50)
    print(f"  Data folder  : {DATA_DIR}")
    print("-" * 50)
    print(f"  Supabase URL : {s.get('url') or '(none)'}")
    print(f"  anon_key     : {'set' if s.get('anon_key') else 'missing'}")
    print(f"  service_key  : {'set (legacy - no longer required)' if s.get('service_key') else 'not set (OK - uploads use signed route)'}")
    print(f"  bucket       : {s.get('bucket') or 'media'}")
    verdict = {"cloud": "\u2601 Cloud ON (uploads enabled)",
               "readonly": "\u26a0 Read-only (service_key needed)",
               "local": "\u25cf Local only"}[st]
    print(f"\n  → {verdict}")
    if s.get("url") and (s.get("service_key") or s.get("anon_key")):
        print("\n  Testing Supabase connection...")
        try:
            import requests
            r = requests.get(_sb_base() + "/rest/v1/matches?select=id&limit=1", headers=_sb_h(), timeout=12)
            if r.status_code < 300:
                print("  \u2713 Connected — read from 'matches' table OK")
                try:
                    r2 = requests.get(_sb_base() + "/rest/v1/matches?select=id",
                                      headers={**_sb_h(), "Prefer": "count=exact", "Range": "0-0"}, timeout=12)
                    cr = r2.headers.get("content-range", "")
                    if "/" in cr: print(f"    \u2601 Matches stored in the cloud: {cr.split('/')[-1]}")
                except Exception: pass
                if s.get("service_key"):
                    try:
                        rb = requests.get(_sb_base() + "/storage/v1/bucket/" + (_sb_bucket()),
                                          headers=_sb_h(write=True), timeout=12)
                        if rb.status_code < 300: print(f"  \u2713 Storage bucket '{_sb_bucket()}' reachable (uploads ready)")
                        else: print(f"  \u2717 Bucket access failed: HTTP {rb.status_code} — check bucket name/key")
                    except Exception as e: print(f"  \u2717 Bucket test error: {e}")
            else:
                print(f"  \u2717 Connection failed: HTTP {r.status_code} — {r.text[:140]}")
                print("    (Key may be wrong or the table may be missing. Make sure schema.sql has been run.)")
        except Exception as e:
            print(f"  \u2717 Connection test error: {e}")
    print("=" * 50)

def main():
    global FFMPEG, CFG
    cfg = load_or_make_config(); CFG = cfg
    try: _apply_autostart(cfg)
    except Exception: pass
    if "--status" in sys.argv or "--check" in sys.argv:
        _print_status()
        try: input("\nPress Enter to exit...")
        except Exception: pass
        return
    # 이미 실행 중이면(자동 실행 + 수동 실행 겹침) 갤러리만 열고 종료
    if not _single_instance():
        try: open_app((cfg.get("gallery_url") or "https://mypenta.netlify.app/").rstrip("/"))
        except Exception: pass
        return
    try: _LOGFILE["p"] = os.path.join(DATA_DIR, "recorder.log")
    except Exception: pass
    mode = cfg.get("mode", "all")
    use_gui = (mode == "all" and sys.platform == "win32" and cfg.get("ui", "window") != "console")
    print("=" * 56); print(f"  myPENTA — League of Legends auto-recorder — mode: {mode}"); print("=" * 56)
    _cst = cloud_state()
    if _cst == "cloud":
        log(f"☁ Cloud ON — saving & sharing via Supabase({_sb_base()}).")
    elif _cst == "readonly":
        log("⚠ Cloud read-only — supabase.service_key in config.json is empty. Fill it and restart to enable uploads.")
    else:
        log("● Local mode — saved on this PC only. (Fill supabase in config.json to enable cloud)")
    if mode in ("all", "recorder"):
        if not use_gui:               # GUI면 창부터 띄우고 백그라운드에서 받음(첫 실행이 멈춘 듯 안 보이게)
            FFMPEG = ensure_ffmpeg()
            if not FFMPEG:
                _safe_input("\nCan't record without ffmpeg. Press Enter to exit..."); return
    if mode in ("all", "server") and not use_gui:
        if not FFMPEG: FFMPEG = ensure_ffmpeg()
    url = (cfg.get("gallery_url") or "https://mypenta.netlify.app/").rstrip("/")
    try: start_login_bridge(url)
    except Exception as _e: log("Login bridge failed to start: %s" % _e)
    if mode == "all":
        log(f"Archive → {url}")
        # 시작 시 브라우저로 아카이브를 자동으로 열지 않음(번잡함). 아카이브는 GUI 버튼/트레이 메뉴로만 연다.
        # 보기 좋은 상태창(GUI). 윈도우 + tkinter 가능하면 GUI로, 아니면 콘솔로.
        if sys.platform == "win32" and (cfg.get("ui", "window") != "console"):
            try:
                import tkinter  # noqa: F401  (가용성 확인)
                run_gui(cfg, url); return
            except Exception as e:
                log(f"GUI unavailable ({e}) → continuing in console mode")
    if mode in ("all", "recorder"):
        if not FFMPEG: FFMPEG = ensure_ffmpeg()
        print("-" * 56); recorder_loop(cfg)

if __name__ == "__main__":
    main()
