# (고급) 원격 서버 분리 버전

루트의 올인원(START.bat + sc_recorder.py)이면 대부분 충분합니다.
이 폴더는 나중에 **서버를 다른 PC/호스팅에 따로 두고** 여러 명이 올리는 구조로 갈 때 쓰는 모듈형 버전이에요.

- `server.py` : 업로드 받는 서버 + 갤러리 (호스팅에 올림)
- `agent.py`  : 녹화 PC에서 도는 클라이언트 (OBS 사용 버전)
- 자세한 건 필요할 때 도와드릴게요.
